# Architecture Decision Records

Index of ADRs for the Project Scaffold. Format: [MADR](https://adr.github.io/madr/)-style (Context / Decision / Consequences / Alternatives). First ADRs for this repo — no prior `docs/adr/` existed (noted in the [scaffold design spec](../superpowers/specs/2026-09-03-project-scaffold-design.md)).

| # | Title | Status |
|---|---|---|
| [0001](0001-full-erd-scope-over-validated-mvp.md) | Full 07 ERD scope over validated MVP | Accepted |
| [0002](0002-backend-framework-orm-migrations.md) | Backend framework/ORM/migrations: FastAPI + PostgreSQL + SQLAlchemy 2.0 + Alembic | Accepted |
| [0003](0003-auth-token-strategy.md) | Auth & token strategy: JWT + DB-revocable refresh + AIAgent API key | Accepted |
| [0004](0004-rbac-design.md) | RBAC: dependency-injected permission checks over shared Actor | Accepted |
| [0005](0005-traceability-link-dedicated-join-tables.md) | TraceabilityLink as dedicated join tables, not a generic polymorphic table | Accepted |
| [0006](0006-test-condition-optional.md) | TestCondition is optional, not mandatory | Accepted |
| [0007](0007-real-multi-tenancy.md) | Real multi-org multi-tenancy (not collapsed to one row) | Accepted |
| [0008](0008-uuid-primary-keys.md) | All primary keys are UUIDs, no auto-increment | Accepted |
| [0009](0009-frontend-stack.md) | Frontend stack: Vite + React Router + TanStack Query + RHF + Zod + ~~Tailwind~~ | Partially superseded by 0012 |
| [0010](0010-single-port-docker-compose-topology.md) | Single-port Docker Compose topology, dev+prod profiles | Accepted |
| [0011](0011-login-rate-limiting.md) | Login rate limiting: DB-backed per-(IP, email) throttle, no Redis | Accepted |
| [0012](0012-coreui-design-system.md) | CoreUI for React as the project's design system (replaces Tailwind) | Superseded by 0042 |
| [0013](0013-refresh-token-rotation-policy.md) | Refresh token rotation & session-persistence policy: rotate-on-use, absolute-expiry-inherited, org re-check on refresh | Accepted |
| [0014](0014-logout-session-revocation-policy.md) | Logout: idempotent single-session revocation, client-side-clear-always | Accepted |
| [0015](0015-ai-agent-credential-mechanics.md) | AI agent credential mechanics: key format, key_prefix lookup, minimal-RBAC-now, 404-vs-403 org-scoped precedent | Accepted |
| [0016](0016-organization-bootstrap-creation-flow.md) | Organization bootstrap & creation flow: signup vs. `POST /orgs` split, bootstrap-closes-after-first-org, advisory-lock concurrency guard, any-org permission gate | Accepted |
| [0017](0017-project-creation-flow.md) | Project creation flow: bespoke org-path-scoped create vs. row-resolved read/update, unconditional creator `test_manager` role auto-assignment, `standards_profile` inheritance from `Organization.default_standards_profile` | Accepted |
| [0018](0018-admin-shell-sidebar-layout.md) | Admin shell layout: CoreUI `CSidebar`/`CSidebarNav` + `CHeader` persistent shell, single nav-item list, CoreUI's own responsive collapse (no custom logic) | Partially superseded by 0020, 0037, 0042 |
| [0019](0019-release-creation-flow.md) | Release creation flow: project-path-scoped bespoke create/list vs. row-resolved single-fetch/audit-query, `test_manager` RBAC bundle extension, triple-permission gate + nested-executions shape for the release→cycles audit query, pinned `NULLS LAST` sort | Accepted |
| [0020](0020-admin-shell-full-template-parity.md) | Full CoreUI free-admin-template parity: breadcrumb, footer, dashboard widgets, dark/light mode toggle, UI-element reference pages (template scaffolding, no FR/NFR backing) | Partially superseded by 0042, 0052 |
| [0021](0021-role-assignment-creation-flow.md) | RoleAssignment creation flow: 422-not-404 for cross-org body fields, OrgMembership precondition for User grantees, list endpoint scope, `has_permission` project_id enforcement fix | Accepted |
| [0022](0022-generic-crud-router-factory.md) | Generic CRUD router factory: per-entity org-resolution chains, row-resolved item routes over `require_permission`, scoped-list required filter, global-catalog `has_permission_in_any_org` fallback | Accepted |
| [0023](0023-frontend-shared-component-location.md) | Frontend shared-component location (`components/shared/` vs. `components/crud/` vs. page-local) & `FormField`'s `CFormFeedback`+`invalid` error-display convention | Accepted |
| [0024](0024-public-landing-page.md) | Public landing page replaces root scaffold-verification page: bare-bones content, delete-not-relocate the old health-check route, authenticated-visitor redirect reuses Login's own logic | Superseded by 0035 |
| [0025](0025-requirement-title-field.md) | `Requirement.title` schema gap-fill & inclusion in generic-CRUD `search_fields` (no new mechanism, reuses ADR-0022's `?q=`) | Accepted |
| [0026](0026-sidebar-dark-color-scheme.md) | Sidebar dark color scheme (`CSidebar colorScheme="dark"`) matching the CoreUI free-template demo, static and independent of the FR-SHELL-4 app-wide light/dark toggle | Partially superseded by 0042 |
| [0027](0027-generic-admin-crud-ui-and-backend-completion.md) | Generic admin CRUD UI (entity registry + field-config, scope-selector, `usePermissions`) + backend completion (`TestExecution`/`TestLog`/4 link tables, `GET /orgs/{org_id}/permissions/mine`); 28-page final scope, 4 structural exclusions | Partially superseded by 0053 (field-shape half) |
| [0028](0028-req3-test-condition-rigor-path-bespoke-routes.md) | REQ-3 rigor-path bespoke routes (TestCondition/TestCase atomic create+link), generic `TestCondition` create removed (link-table gap it left open), `test_manager` RBAC bundle extension for `test_condition.*`/`test_case.*` | Accepted |
| [0029](0029-testcase-resolver-direct-link-fallback.md) | `TestCase` org-resolver direct-link fallback (REQ-2 gap-fill): `RequirementTestCaseLink` added as a 3rd branch, checked before the `TestSuiteTestCase` fallback — a direct-link `TestCase` had no resolver path and 404'd as "orphaned" right after creation | Accepted |
| [0030](0030-req4-test-suite-membership-bespoke-routes.md) | REQ-4 TestSuite membership bespoke routes (`TestSuiteTestCase` add/remove/live-list) — cross-project add rejected (`422`), duplicate add is `409` (third meaning this API gives the code), no RBAC bundle change needed | Accepted |
| [0031](0031-plan1-test-plan-membership-and-status-transition-routes.md) | PLAN-1 TestPlan↔TestSuite membership bespoke routes (`TestPlanTestSuite` add/remove/live-list) + new coverage query (`GET /test-plans/{id}/test-cases`) + `status`-transition legality guard on the generic `PATCH` (fourth `409` meaning), no RBAC bundle change needed | Accepted |
| [0032](0032-plan2-entry-exit-criteria-visibility.md) | PLAN-2 `EntryExitCriteria` visibility: full-CRUD section on `TestPlanDetail` (reuses generic factory verbatim, zero new backend routes) + nested `exit_criteria` field on `GET /releases/{id}/test-cycles` (quadruple permission gate), no RBAC bundle change needed | Accepted |
| [0033](0033-mcp-server-architecture.md) | First-party MCP server: streamable-HTTP transport, ASGI sub-app mount on existing FastAPI under `/mcp`, direct-call tool dispatch into existing route handlers (bypasses FastAPI `Depends` defaults), per-tool bearer-header resolution reusing `get_current_actor`'s `_resolve_agent_actor` — same validation/permission path as REST, no parallel weaker code path (MCP-1 AC1) | Accepted |
| [0033](0033-plan3-test-cycle-creation-and-execution-scope-check.md) | PLAN-3 `TestCycle` creation (`POST /test-plans/{id}/test-cycles`, cross-project `422` on Release/Environment) + `POST /test-cycles/{id}/executions` (PLAN-3's execution scope-check, reuses PLAN-1's coverage-query join; also FR-EXEC-1's own create route), generic `TestExecution` create removed, `test_manager` RBAC bundle extension for `environment.*`/`test_execution.create`+`.read` | Accepted |
| [0034](0034-exec-1-test-execution-recording-dashboard.md) | EXEC-1 TestExecution recording screen (`TestCycleDetail`, bespoke) + live dashboard aggregation reusing FR-SHELL-3's "existing generic-list `total`, no new route" precedent, test-case picker scoped to PLAN-1's coverage query, no RBAC change | Accepted |
| [0035](0035-dash-1-root-redirect-and-dashboard-placeholder.md) | DASH-1 root route becomes a pure `accessToken`-only auth-state redirect (spinner / `/login` / `/dashboard`), deletes the LANDING-1 public landing page outright, new empty `Dashboard` placeholder screen — fixes the AUTH-2 reload-while-logged-in gap by no longer depending on `orgContext` | Accepted |
| [0036](0036-shell-6-organization-switcher-header-dropdown.md) | SHELL-6 organization switcher header dropdown (`AppHeader`, lazy-fetched, always-org-root navigation) + new `GET /auth/me/orgs` endpoint (reuses `OrgSummary`, active-memberships-only, no schema change) | Accepted |
| [0037](0037-shell-components-raw-html-not-coreui-react.md) | Shell chrome (`AppShell`/`AppHeader`/`AppSidebar`/`AppFooter`/`AppBreadcrumb` only) moves off `@coreui/react` components to raw HTML/Bootstrap classes — CSS/design-token layer unaffected, incremental scope, partially supersedes ADR-0012/ADR-0018 | Superseded by 0042 |
| [0038](0038-exec-2-append-only-test-log.md) | EXEC-2 append-only `TestLog`: generic `post_update_hook` on `CrudEntityConfig` (status-change log on `result` correction), log-on-create, two new bespoke routes (`POST /executions/{id}/comments`, `GET /executions/{id}/logs`), `agent_action` overrides the human event type for an `AIAgent` caller, `_actor_membership_exists` bug-fix promoted to `crud_factory.py`, `TestCycleDetail` "History" modal — no schema/RBAC change | Accepted |
| [0039](0039-dash-2-org-home-dashboard-relabel-and-project-table.md) | DASH-2 relabels `OrgHome` "Dashboard" (heading/sidebar+icon/breadcrumb only, route unchanged — accepted, unresolved naming overlap with ADR-0035's separate `/dashboard` placeholder) + Project table gains ID column, Edit modal (name+standards_profile together, replacing inline click-to-edit), Delete button+confirm modal (first frontend caller of ADR-0022's already-shipped `DELETE /projects/{id}`), client-side search/sort/pagination over the already-fully-fetched list — no backend/schema/RBAC change; found during verification that Delete 409s for every UI-created Project (see ADR-0040) | Accepted |
| [0040](0040-role-assignment-project-cascade-delete.md) | `role_assignment.project_id` FK changes `RESTRICT` → `CASCADE` — fixes ADR-0039's found defect (every UI-created Project's auto-granted creator `RoleAssignment` permanently blocked `DELETE /projects/{id}`); scoped to this one FK only, every other `project_id`-FK'd entity keeps `RESTRICT` | Accepted |
| [0041](0041-ds-2-table-container-shared-pagination.md) | DS-2 shared `Table` container (`frontend/src/container/Table.tsx` — new `container/` location convention, amends ADR-0023): table + pagination + page-size selector (10/25/50/100), server and client pagination modes, reused across `EntityTable`, `OrgHome`/Dashboard Project table (replaces ADR-0039's own bespoke pagination chrome, keeps its client-side search/sort), `RoleAssignmentsPanel`, `OrgMembers`, `ProjectDetail`'s outer tables, `TestPlanDetail`; backend `page_size` max 25 → 100 (hardcoded literal, default unchanged); `GET /orgs/{org_id}/role-assignments` gains pagination (breaking response-shape change, no existing external caller) | Partially superseded by 0061 |
| [0042](0042-adminlte-design-system.md) | **AdminLTE v4 (`admin-lte@4.9.1`) + `bootstrap@^5.3.8` + Font Awesome Free replaces CoreUI as the project's design system** (supersedes ADR-0012, ADR-0037): CSS entry `bootstrap.min.css` → `adminlte.min.css` → `all.min.css` in `main.tsx`, every `@coreui/*` package removed; icons move `@coreui/icons`/`CIcon` → Font Awesome `<i className="fa-solid fa-*">`, resolving PR #42's blank-glyph `Icon`-atom open question; **raw HTML/JSX against the CSS repo-wide** (ADR-0037's 5-file pattern generalized, no component-library wrapper anywhere); **AdminLTE's own JS plugins and jQuery are NOT vendored** — sidebar collapse (`sidebar-collapse`/`sidebar-open` on `<body>`, `matchMedia("(max-width: 991.98px)")`, self-rendered `.sidebar-overlay`), treeview (`menu-open`/`nav-treeview`), color mode (`data-bs-theme` + `lte-theme` localStorage key), dropdowns/modals/toasts are all plain React state against AdminLTE's literal class contract; **big-bang scope, explicitly reversing ADR-0037's incremental stance** because the two Bootstrap-family stylesheets cannot coexist (ADR-0012's own Tailwind-vs-CoreUI cascade argument, turned on itself), so a phase boundary would be a broken-cascade boundary; Tailwind stays removed and RHF + Zod still own form state (ADR-0009 unchanged). Accepted trade-offs: `sidebar-dark-*` does not exist in v4 (zero occurrences in the shipped CSS — a real visual change, partially supersedes ADR-0026), every saved color-mode preference is orphaned once by the localStorage key change, `btn-ghost-*` survives as an unstyled class, modals are hand-rolled without focus-trapping, and AdminLTE's `"Source Sans 3"` falls through to the system stack | Partially superseded by 0054 |
| [0043](0043-atomic-design-tiering-for-frontend-components.md) | Atomic-design tiering (`atoms`/`molecules`/`organisms`/`templates`) replaces `components/shared/`/`components/crud/` (supersedes ADR-0023's directory-location decisions): `FormField`/`FeaturedCard`/`WidgetStatsTile` → `molecules/`; `FkAutocomplete`/`ScopeSelector` → `molecules/`; `EntityTable`/`EntityForm` → `organisms/`; `AppBreadcrumb`/`AppFooter`/`AppHeader`/`AppSidebar` → `organisms/`; `AppShell` → `templates/`; pure location move, no export/prop/behavior change; `container/`'s ADR-0041 axis untouched | Accepted |
| [0044](0044-exec-3-raise-defect-from-execution.md) | EXEC-3 raise-a-`Defect` bespoke create route (`POST /executions/{id}/defects`, atomic `Defect`+`TestCaseDefectLink` insert, `422` if the execution's `result != fail`) + new `GET /test-cases/{id}/defects` ordered read (most-recent-first, not in the original API Document scope) + `test_manager` RBAC bundle extension for `defect.create` and (`test_manager`+`tester`) `test_case_defect_link.read`, fifth ad hoc extension for `test_manager`; frontend built directly against AdminLTE/ADR-0042 raw HTML (branched pre-migration, reconciled at merge, never shipped a `@coreui/*` import) | Accepted |
| [0045](0045-ds-3-infobox-widget-consolidation.md) | DS-3 shared `InfoBox` component (`components/molecules/info-box/` — renumbered from an initial ADR-0043 draft after a real collision with the atomic-design-tiering ADR was found merging into `main`; relocated to `molecules/` to match that ADR's own new convention) using AdminLTE v4's own documented Info Box markup (`.info-box`/`.info-box-icon`/`.info-box-content`), retires two independently-hand-rolled stat tiles — `WidgetStatsTile` (`OrgHome`'s Project-count/active-Org-Member-count widgets) and `TestCycleDetail`'s local `StatTile` (4 execution-dashboard tiles) — repo-wide scope per direct CTO instruction; all existing `data-testid`s and count-sourcing logic (FR-SHELL-3/NFR-27, FR-EXEC-1 AC2/NFR-46) unchanged, `ActiveMemberCountWidget` gains an icon it didn't have before (Info Box's canonical shape has no icon-omitted variant) | Accepted |
| [0046](0046-shell-7-sidebar-mini-org-crud-restructure.md) | SHELL-7 sidebar-mini layout (`sidebar-mini` added to `AppShell`'s constant body-class set, AdminLTE's own icon-only-collapsed-with-hover-expand modifier, no new state machine) + org-scoped CRUD nav restructure (`AppSidebar`'s single flat 8-entity `Admin` group splits into 3 named, individually-iconed groups — Access Control/Catalogs/Organization — presentation-layer only, `orgScopedEntities` registry itself unchanged); `UI Elements` (ADR-0020) untouched, out of scope. **Renumbered from an initial `0044` draft (2026-09-09)** — a real collision was found against `main`, which had independently merged its own unrelated `ADR-0044` (EXEC-3) and `0045` (DS-3) while this branch was still in flight. Docs landed 2026-09-08; implementation + all tests landed same branch same day; this row is being merged into `main` 2026-09-09. | Accepted |
| [0047](0047-proj-4-projects-page-sidebar-entry.md) | PROJ-4 gives Project CRUD its own sidebar-reachable page (amends [ADR-0039](0039-dash-2-org-home-dashboard-relabel-and-project-table.md)'s placement on "Dashboard", not its Project-CRUD behavior): new `/orgs/:orgId/projects` route + `ProjectsPage.tsx` (the table/modals moved verbatim out of `OrgHome.tsx`, no code rewritten), new flat "Projects" sidebar nav item (no icon — DASH-2/TC-SHELL-021's "Dashboard is the only icon item" invariant left unchanged, not expanded), "Dashboard" trims to the 2 stat widgets + `RoleAssignmentsPanel`, Project-count widget becomes a link to the new page; no backend/schema/RBAC change — every route already shipped under ADR-0017/ADR-0022. Numbered `0047`, not `0046` — `0046` was already claimed by SHELL-7, unmerged but in flight on its own branch when this number was picked (SHELL-7 has since merged, PR #48, confirming `0047` stayed next-free — re-checked against `origin/main` immediately before this docs pass) | Superseded by 0060 |
| [0048](0048-brand-1-logo-brand-system.md) | BRAND-1 logo/brand system: checkmark-in-shield mark, one `currentColor` SVG pair (`logo-mark.svg`/`logo-full.svg`, no per-theme duplicate files) reused across login (`BrandLogo`, fixes its long-standing hardcoded "AdminLTE" text bug), header (small mark), sidebar (AdminLTE's own `.brand-image-xl`/`.brand-image-xs`+`.logo-xl`/`.logo-xs` cross-fade, zero new JS, already-active since SHELL-7's `sidebar-mini`) and a new SVG-only favicon (its own fixed non-`currentColor` palette, browser chrome has no app theme to inherit); no new brand color — reuses stock Bootstrap `--bs-primary`; no backend/database/API surface | Accepted |
| [0049](0049-frontend-co-locate-unit-tests.md) | FRONTEND-1 Vitest unit tests move from `frontend/tests/` (mirroring `src/` shape) to live co-located next to their source under `frontend/src/` — every `*.test.{ts,tsx}` lands beside the file it covers (e.g. `src/components/atoms/button/button.test.tsx` next to `button.tsx`), per-story multi-file test naming (`ProjectDetail.test.tsx` + `ProjectDetail.TestConditions.test.tsx` + …) lands in the same folder as `ProjectDetail.tsx`; relative imports shorten to `./<sibling>` or `./` via the existing `index.ts` barrel; `frontend/tests/` → `frontend/test-setup/` (renamed, now contains only the jsdom-polyfill `setup.ts`, `vite.config.ts`'s `setupFiles` path updated to match); `frontend/CLAUDE.md`'s pre-existing "tests live in `frontend/tests/`" paragraph rewritten in place; historical plan docs (`docs/superpowers/plans/*.md`) carrying now-stale `frontend/tests/...` paths deliberately NOT rewritten (historical record preserved — see ADR-0049 Consequences); E2E (Playwright) tests under `e2e/tests/` unaffected (different runner, different folder); no backend/database/API/UI surface change. Numbered `0049`/`11.32`/`§40`/`NFR-54`, confirmed next-free against `origin/main` immediately before this docs pass | Accepted |
| [0050](0050-shell-9-project-scope-nav-context-resolution.md) | SHELL-9 resolves org nav context on project-scoped routes (`/projects/:projectId/...`, which carry no `:orgId` route param): new `useResolvedOrgId()` hook (route param, else `GET /projects/{id}` fetch + read `org_id`, generalizing `useAdminRouteContext`'s own pre-existing admin-only version of the same idea) consumed by `AppSidebar`/`AppBreadcrumb`; breadcrumb gains a resolved `Projects → {project name}` root on all 5 project-scoped route patterns, replacing a bare unlinked "Project" label. No backend/schema/RBAC change — reuses `GET /projects/{id}` (ADR-0017/ADR-0022) verbatim. **Renumbered twice the same day (2026-09-09)** — first from an initial `0048` draft after colliding with BRAND-1's own `0048` claim, then from `0049` after colliding with FRONTEND-1's own independent `0049` claim (both landed on `origin/main` while this branch was still in flight — three separate sessions each racing to claim the same next-free ADR number, a real, repeated instance of this repo's own documented numbering-collision risk). **Sidebar-content decision amended same day by ADR-0051 below** — this ADR's own text is left as-written | Accepted |
| [0051](0051-shell-10-project-scope-entity-nav.md) | SHELL-10 amends ADR-0050 Decision §2 — project-scoped routes stop rendering the org nav and instead get a distinct project-mode nav: new `PROJECT_ENTITY_GROUPS` (Test Design/Test Planning/Execution & Defects/Setup, 13 of 20 `projectScopedEntities`, 7 explicitly excluded — reached via their parent entity instead), each linking to its already-shipped `/projects/:projectId/admin/<entity>` route; 2 "back" links at the bottom (Project Overview, Back to Projects); `useResolvedOrgId()` gains a `mode: "org"\|"project"` field as the explicit signal `AppSidebar` needed to tell the two route kinds apart. No backend/schema/RBAC change | Partially superseded by 0062 |
| [0052](0052-remove-ui-elements-nav-group.md) | REMOVE-UI-1 retire the org-scoped "UI Elements" nav group + Colors/Typography/Icons reference pages (ADR-0020's template-parity scaffolding cleaned up per ADR-0020's own "low-risk cleanup" forward-pointer) — `AppSidebar`'s `navGroups` array loses the `ui-elements` group, `App.tsx`'s 3 corresponding React Router routes deleted, `frontend/src/pages/ui-elements/*` files removed; frontend-only, no backend/database/API change; `AppSidebar.test.tsx` fixture swap from `sidebar-nav-group-ui-elements` → `sidebar-nav-group-access-control` for the treeview contract test. **Renumbered from an initial `0049` draft (2026-09-09)** — three independent stories (FRONTEND-1/SHELL-9/SHELL-10) all landed on `origin/main` while this branch was in flight, each claiming the next-free ADR numbers independently. Confirmed `0052`/`11.35`/`§43`/`TC-SHELL-039` next-free against `origin/main` immediately before this docs-pass merge | Accepted |
| [0053](0053-tabler-install-phase-1-cdn.md) | Tabler v1.5.1 installed via CDN in `frontend/index.html` (`<link>`+`<script>`, jsDelivr) — Phase 1 (install only) of a planned AdminLTE→Tabler design-system swap; CDN chosen over the npm package per explicit CTO instruction; Tabler's stylesheet placed *before* AdminLTE/Bootstrap/Font Awesome's own tags so AdminLTE continues to win every conflicting selector — zero visible change to any existing screen. **Does not supersede ADR-0042** — AdminLTE remains the live design system in full; component migration is explicitly out of scope until separately instructed. No backend/database/API surface | Accepted (Phase 1 only) |
| [0054](0054-tabler-shell-migration-phase-2.md) | Tabler shell migration Phase 2 — `AppSidebar`/`AppHeader`/`AppShell`/`AppFooter` rewritten to Tabler v1.5.1's own "Sidebar layout" markup (`.page` flow layout replaces AdminLTE's `.app-wrapper` CSS grid; `navbar-nav`/Bootstrap-dropdown groups replace `sidebar-menu`/`nav-treeview`); `frontend/index.html`'s Tabler `<link>` moves after `main.tsx`'s CSS imports, flipping the cascade so Tabler wins project-wide (a deliberate, accepted repo-wide visual shift on every unmigrated page, confirmed via `AskUserQuestion` with the CTO); AdminLTE's `sidebar-mini` icon-rail fold retired outright, no Tabler equivalent built this pass; every other screen stays on AdminLTE markup until a separately-scoped future pass migrates it. **Partially supersedes ADR-0042** (AdminLTE remains live for every screen this ADR doesn't touch — mirrors ADR-0042's own two-step supersession of ADR-0012). No backend/database/API surface | Accepted |
| [0055](0055-admin-3-backend-driven-entity-schema.md) | ADMIN-3 backend becomes the single source of truth for admin-CRUD **field shape** (partially supersedes ADR-0027) — new per-entity `GET /entities/{resource}/schema` route (hybrid derivation: type/required/enum-values auto from Pydantic, FK ref+label-field/badge-colors/label-overrides/`showInTable` declared explicitly via a new `CrudEntityConfig.field_meta`/`FieldMeta`), new backend `entity_registry.py` collecting all 27 `CrudEntityConfig` instances; frontend fetches instead of importing `entityConfigs/*.ts` (28 files ported+deleted in one pass — the 27 registered entities plus `release.ts`, which becomes the one static exception; no pilot), long `staleTime`. Navigation/routing/sidebar grouping stays frontend-static — this ADR narrows to field shape only, the actual drift surface (`Requirement.title`, ADR-0025, missing from the frontend form for 4 days after shipping, found live 2026-09-09). **Renumbered from an initial `0053` draft (2026-09-11)** — `main` had independently merged its own unrelated `ADR-0053`/`0054` (Tabler CDN install + shell migration) while this branch was in flight | Accepted |
| [0056](0056-admin-4-generic-admin-crud-column-sort.md) | ADMIN-4 column sort for the generic admin CRUD list (extends ADR-0022's factory) — `?sort=<field>`/`?sort=-<field>` on the generic `list` route (`apply_sort`, pure query-builder, 422 on unknown/unsortable field), `FieldMeta.sortable` (default true) served as `"sortable"` on every `GET /entities/{resource}/schema` field entry (ADR-0055) — the sort allow-list is derived from that same schema function, not a second hand-kept list; frontend `EntityTable` renders a clickable sort header (`fa-sort`/`-up`/`-down`) when `onSortChange` is passed, `EntityListPage` owns the click-to-sort toggle (unsorted→asc→desc→unsorted) and resets to page 1 on change. `Release` (the one static-config exception, ADR-0055) is not sortable — its list route is 100% bespoke, out of scope this pass | Accepted |
| [0057](0057-admin-crud-pages-relocated-to-container.md) | Generic admin CRUD pages relocated `pages/admin/EntityListPage.tsx`/`EntityFormPage.tsx` → `container/entity-crud/EntityListPage/`/`EntityFormPage/` (each own per-screen subfolder + `index.ts` barrel, matching `pages/<tier>/<X>/<X>.tsx` convention), per direct instruction — pure location change, no route/prop/behavior change; `registry.ts`/`useAdminRouteContext.ts`/`useEntitySchema.ts`/`useEntityScope.ts` stay in `pages/admin/` (broader callers); honestly notes this widens ADR-0041's `container/` state-ownership axis rather than fitting it cleanly (each page has exactly one caller, not "many screens"); `tsc --noEmit` clean, 506/507 Vitest passing (1 pre-existing unrelated full-suite-only flake, confirmed passes alone). Same pass also adds `entityCrudRoutes(basePath)` (`container/entity-crud/index.ts`) — one call wires both List/Add/Edit/Delete routes for a scope, `App.tsx`'s 4 hand-written `<Route>`s collapse to 2 calls | Accepted |
| [0058](0058-project-generic-admin-org-scoped.md) | `Project` added to `orgScopedEntities` — generic admin surface reachable at `/orgs/:orgId/admin/projects` (list/edit/delete, no create — bespoke ADR-0017 create unaffected), scope resolved straight from `:orgId` via the pre-existing entity-agnostic `scope_field`/`useEntityScope` mechanism, no new filtering concept built. New `ORG_EXCLUDED_ENTITY_KEYS` (mirrors `PROJECT_EXCLUDED_ENTITY_KEYS`) keeps it out of the sidebar — ADR-0047's bespoke `ProjectsPage` stays the sole nav path. No backend/schema/RBAC change (`scope_field="org_id"` already existed) | Accepted |
| [0059](0059-project-generic-admin-create.md) | Generic admin surface's "New" button now works for `Project` — reuses the existing bespoke `POST /orgs/{org_id}/projects` (ADR-0017) unchanged via `entityConfigs/overrides.ts`'s `createPath` mechanism (`Release`'s own precedent), not a new `post_create_hook` (rejected — the route's pre-insert 404/permission/inheritance logic doesn't fit a post-write hook shape). `create_schema` added to `_PROJECT_FACTORY_CONFIG` (schema-metadata only, no competing route registered) + `"create"` added to `full_methods`. Found and fixed a real gap: `useAdminRouteContext`'s `routeParams.orgId` was the raw (sometimes-`undefined`) URL param, not the resolved value the new `createPath` interpolation needs. Deliberately collapsed ADR-0017 Q3's `standards_profile` omitted-vs-explicit-null distinction into one case (`EntityForm` can't express "omit this key," only "send null") — `TC-PROJ-007` amended in place to match. Verified against a live isolated stack: backend integration `test_projects.py` 18/18, backend unit 467/467, frontend `tsc`/Vitest 73/73 files clean | Accepted |
| [0060](0060-projects-page-retired-generic-surface.md) | **Supersedes [ADR-0047](0047-proj-4-projects-page-sidebar-entry.md).** Bespoke `ProjectsPage` (549 lines + its own Vitest suite) deleted outright — `/orgs/:orgId/projects` now renders the generic `EntityListPage`/`EntityFormPage` (new `entityKeyOverride` prop, for a route with no `:entity` segment), same URL/nav item. Backend `search_fields=("name",)` closes the search gap; new generic `EntityConfig.detailPath`/`detailLinkField` + `EntityTable` support restores the row-name-links-to-`ProjectDetail` navigation `ProjectsPage` had, reusably (not a one-off hack). **Found and fixed a real, retroactive bug**: `useEntityScope`'s branch order checked `scopeResolution` before a directly-satisfiable plain `scopeField`, so `Project`'s always-present `scopeResolution` (needed only on its *other*, project-scoped admin route) silently disabled scope resolution on any route with `:orgId` but no `:projectId` — meaning ADR-0058's own org-scoped admin route was *already* broken since it shipped. Fixed by reordering; new dedicated test encodes the real backend-shaped config no prior mock fixture matched. Found live, not by any mock — verified via a real isolated-stack browser click-through end to end. Backend integration 18/18, unit 467/467, frontend `tsc`/Vitest 73/73 files/496/496, 2 edited e2e specs pass clean individually | Accepted |
| [0061](0061-table-pagination-always-renders.md) | **Partially supersedes [ADR-0041](0041-ds-2-table-container-shared-pagination.md)'s `showPaginationRow = totalPages > 1` gate** — `Table`'s pagination row (and page-size selector) now always renders, single page or not; Previous/Next still disable correctly via `currentPage`/`totalPages`. Pagination markup extracted into a reusable `components/molecules/pagination/` (`Pagination`) molecule, composed by `Table.tsx`. New "Showing X to Y of Z items" summary (`totalItems` prop, `Table.tsx` always supplies it). `EntityTable`'s pagination now sits in a real `.card-footer` (new `Table` prop `footerClassName`). TC-DS-009/TC-DS-014 corrected in place — no live-browser verification (no login credential available this session) | Accepted |
| [0062](0062-sidebar-overview-always-renders-at-top.md) | **Partially supersedes [ADR-0051](0051-shell-10-project-scope-entity-nav.md)'s Decision §3** — project-mode sidebar's "Project Overview" renamed "Overview" and moved from the bottom "back" links to the top of the nav (`navItems`, same slot org-mode's "Dashboard" uses); no longer suppressed while already on `/projects/:projectId` itself — relies on `NavLink`'s own active-class styling instead, same posture as "Dashboard." Found and fixed a real gap the old suppression logic had been masking: the item wasn't gated on `orgId` (only `projectId`), so it rendered alone during the pending/404 window instead of staying empty with the rest of the nav. TC-SHELL-029/TC-SHELL-030 corrected in place; one e2e assertion updated, untested live (no login credential available this session) | Accepted |
| [0063](0063-mcp-integration-screen.md) | New "MCP Integration" screen at `/orgs/:orgId/mcp` and `/projects/:projectId/mcp` (one shared component, mode resolved via `useResolvedOrgId()`, no `scope` prop) — explains ADR-0033's MCP server and gives verified client-connection snippets (Claude Code, Codex CLI, Cursor, generic MCP-HTTP fallback); org mode adds live `AIAgent` key issue/list/revoke, reusing ADR-0015's existing `create`/`revoke` routes plus one new `GET /orgs/{org_id}/agents` list route (same gate order, permission code reused — `ai_agent.create`, not a new `.read` code, no RBAC migration). Sidebar nav item both modes | Accepted |
| [0064](0064-dash-3-dashboard-org-list-and-chooser.md) | DASH-3 resolves ADR-0035's own deferred "org-scoped or global" question — `/dashboard` stays global but now fetches `GET /auth/me/orgs` (no backend change) fresh on every mount and branches on the count: 0 orgs → empty state + "Create organization" CTA; exactly 1 org → immediate auto-`navigate()` to `/orgs/{id}`, no click; 2+ orgs → "Select an organization" card list. `/orgs/pick`/`OrgPicker.tsx` retired outright — post-login/signup/accept-invite redirect always targets `/dashboard` now, which owns all org-count branching in one place instead of split across `Login.tsx`'s own effect and a separate screen. Does NOT resolve the separate `/dashboard`-vs-`OrgHome` "Dashboard" naming overlap (ADR-0039/NFR-49) — only incidentally softens it for the 2+-org case. No backend/schema/RBAC change. **Numbering collision with MCP-4 found via `git fetch` immediately before merging** — originally drafted as `0063`, MCP-4 had already claimed it on `main`; renumbered to `0064`, the actual next-free value | Accepted |
| [0065](0065-mcp-5-full-crud-all-entities.md) | **Supersedes [ADR-0033](0033-mcp-server-architecture.md)'s MCP-2/MCP-3 hand-wired-tool backlog.** MCP-5 — full list/get/create/update/delete across every entity, mirroring exactly the methods REST already grants per entity, never more. Registry-driven, not hand-wired: 5 reflective tools (`list_entities`/`get_entity`/`create_entity`/`update_entity`/`delete_entity`) + `describe_entity` (wraps ADR-0055's `derive_entity_schema`), dispatched via a new `backend/app/mcp/tool_registry.py` mapping every entity's `resource` slug to either a generic-factory config (ADR-0022's `ALL_ENTITY_CONFIGS`, ADR-0055) or a bespoke route handler (~13 mutating bespoke routes, API Doc §4) — one source of truth both surfaces read, so a future route/method change needs one registry edit, not a parallel MCP-side one. `crud_factory.py`'s `list` closure refactored off raw `Request.query_params` into a plain-arg function callable from both the REST route and the MCP tool. **Found and fixed a real, pre-existing defect blocking this story outright**: `crud_factory.py`'s own `_fetch_and_gate`/`_resolve_scope_for_write` gates called the older, `User`-only `_org_membership_exists` rather than the `AIAgent`-aware `_actor_membership_exists` the module already defined but never used — every one of the 27 generic-factory entities had 404'd unconditionally for any `AIAgent` caller since ADR-0022 shipped, invisible until an MCP tool actually tried to reach the generic factory. No REST-visible behavior change for human callers (regression-tested). **Implementation landed same branch, same day** — see WBS 11.45's own addendum | Partially superseded by 0068 |
| [0066](0066-admin-5-seed-test-level-catalog.md) | ADMIN-5 seeds the `test_level` global-catalog table with the 5 ISTQB CTFL v4.0.1 test levels (Component/Component Integration/System/System Integration/Acceptance Testing) via a new idempotent Alembic data migration (existence-checked by `name`, symmetric `downgrade()` deletes exactly those 5 rows), closing FR-ADMIN-1 AC1's own pre-existing "seeded via Alembic data migration" claim for `TestLevel` — root `CLAUDE.md` had flagged this as an open product decision since 2026-09-06. `TestType`/`TestDesignTechnique` remain unseeded, deliberately out of scope (no canonical vocabulary decided yet for either). No schema/API/UI change — the pre-existing `GET /test-levels` route and `TestCase` create-form dropdowns simply stop being empty | Accepted |
| [0067](0067-testtype-catalog-default-seed.md) | `TestType` catalog ships with a permanent default seed — 5 ISTQB CTFL v4.0.1-aligned rows (`Functional Testing`, `Non-functional Testing`, `Black-box Testing`, `White-box Testing`, `Confirmation Testing`) via an idempotent Alembic data migration (existence-checked by `name`, same pattern as `34053c46f9fc_seed_rbac_system_roles.py`; `downgrade()` deletes only these 5 by name). Closes the exact gap root `CLAUDE.md`'s testing notes flag ("TestLevel/TestType ship with full CRUD but zero seeded rows... an open product decision for a future story/ADR"), for `TestType` specifically — resolves the other half of the decision ADR-0066/ADMIN-5 (above) resolved for `TestLevel`; `TestDesignTechnique` remains unseeded, a separate future decision. No schema/API/UI change. **Renumbered from an initial `0066` draft (2026-09-14)** — `origin/main` had independently merged ADMIN-5's own unrelated `ADR-0066` (`TestLevel`, row above) while this branch was in flight, found via `git fetch` immediately before merging main into this branch | Accepted |
| [0068](0068-mcp-6-per-entity-mcp-tools.md) | **Partially supersedes [ADR-0065](0065-mcp-5-full-crud-all-entities.md)** (its Decision §1 tool *shape* only — the registry, the plain-arg `list` refactor and the `_actor_membership_exists` fix all stay) **and retires [ADR-0033](0033-mcp-server-architecture.md)'s 2 MCP-1 hand-wired tools.** MCP-6 — the MCP surface becomes **one tool per entity per supported action**, `tn_<entity>_<action>` (`tn_organization_get`, `tn_project_create`, `tn_requirement_list`, `tn_test_case_update`), **146 tools** generated at import time by a new `app/mcp/tools/entity_tools.py` walking `TOOL_REGISTRY` — no `resource` argument on any tool (the entity is the tool's identity), each signature carrying only that action's real params. ADR-0065's 6 reflective `*_entity`/`*_entities` tools and MCP-1's `create_test_case`/`list_test_cases` are **deleted, not aliased** (`tools/generic_crud.py` + `tools/test_cases.py` both removed); `test_case`'s two capabilities fold in as `tn_test_case_create` (gains REQ-3's rigor path, which the hand-wired tool never had) and `tn_test_case_list` (a new bespoke registry row wrapping the nested `GET /requirements/{id}/test-cases` — the generic factory registers no flat `list` for `TestCase`). Actions per entity come from `full_methods` (ADR-0055), **not** `methods`, plus declared `BESPOKE_EXTRA_ACTIONS` — so MCP-5's "never more than REST" AC is now enforced by *omission from `tools/list`* rather than a `405` at dispatch. `TOOL_REGISTRY` re-keyed plural-hyphenated → singular snake_case `resource` (the plural slug is untouched wherever REST uses it). New diff-based completeness test (`tests/unit/test_mcp_tool_naming.py`, 26 assertions against `ALL_ENTITY_CONFIGS`/`full_methods`, no hardcoded name list). **TC-MCP-017 and TC-MCP-022 genuinely corrected, not renamed** — see ADR Consequences. No REST/database/schema/RBAC/frontend surface change; backend unit 530/530, integration 328 passed against a live isolated stack (2 pre-existing bare-`/health`-via-nginx topology failures only). Numbered `0068`/`11.48`/`§56`/`NFR-68` — a first-choice `0067`/`11.47`/`§55`/`NFR-67` was drafted against `testlevel-seed`/`testtype-seed`'s in-flight state, but `testtype-seed` (renumbered `0066`→`0067` for its own ADMIN-5 collision) merged those exact numbers to `main` before this branch's own numbering-collision recheck ran — caught by that recheck, not at merge time, renumbered here | Accepted |
| [0069](0069-req-5-standalone-test-case-with-optional-requirement-link.md) | REQ-5 — `TestCase` gets a new nullable `project_id` column, giving it the same direct-project-scope shape `TestSuite` already has; `_TEST_CASE_CONFIG` is unrestricted for exactly this shape (generic factory `list`+`create` enabled, `scope_field="project_id"`, no atomic link-table write needed since a standalone case has no link to write) — the pre-existing project-mode sidebar "Test cases" nav item (ADR-0051), non-functional until now, starts working with zero frontend route change. `resolve_test_case_org_id` gains a 4th branch (`project_id`, checked after the two link-table branches, before the `TestSuiteTestCase` fallback). New retrofit route, `POST /test-cases/{id}/link-requirement`, lets a standalone case gain a `RequirementTestCaseLink` after the fact (`404` cross-org, `422` cross-project, `409 already_linked_to_requirement` if the case already has any Requirement traceability) — no unlink, no retrofit-to-`TestCondition`, both explicitly out of scope. No RBAC migration (`test_case.create`/`.read` already granted). Implementation landed, coverage re-verified, live-manual-test follow-ups (description field/type, fk-select dropdowns) landed same branch. **Renumbered from an initial `0068` draft (2026-09-15)** — `origin/main` had independently merged MCP-6's own unrelated `ADR-0068` (row above) while this branch was in flight, found via `git fetch` immediately before merging main into this branch | Accepted |
| [0070](0070-search-fields-audit-and-numeric-cast.md) | SEARCH-1 — the generic factory's `?q=` free-text search gets both halves of what ADR-0022's opt-in design always implied. **(a) Mechanism:** the `?q=` clause-builder is extracted into `_search_clause(model, column, term)`, and a numeric column (`isinstance(column.type, (Integer, Numeric))` — covers `BigInteger`/`SmallInteger`/`Float` by subclassing) is now `cast(column, String).ilike(...)` instead of a bare `ILIKE`. Not cosmetic: Postgres has **no `integer ~~* unknown` operator**, so an un-cast `ILIKE` on an int column is a hard `ProgrammingError` at query time — numeric columns were *structurally unusable* in `search_fields`, which is why every pre-existing tuple in the repo happens to be string-only. Semantics are substring-on-rendered-digits (`?q=1` matches sequence 1, 10, 21); exact-match-on-numeric was rejected (one param cannot carry two match semantics, and `filter_fields` already gives exact match). String columns are byte-for-byte unchanged. **(b) Per-entity audit:** `search_fields` goes from **4 to 19** of 27 configs — `test_plan` gains all 5 of its free-text columns (the originally reported bug: searching a Test Plan silently returned the unfiltered list), `test_step` gains `action`/`expected_result`/**`sequence`** (the repo's first numeric search field), plus 12 more new/extended tuples. Eight entities keep none, each with a written reason: `test_log`/`org_membership`/the 4 link tables have **zero free-text columns** (all FK/UUID/enum/timestamp; `TestLog`'s only payload is JSONB), and `organization`/`role_assignment` have **no generic `list` route** at all, so a tuple would be dead config. Two deliberate column-level exclusions inside searchable entities: `Attachment.size_bytes` (a measurement, not an identifier — matching its digits answers nothing) and `Defect.status`/`Approval.role` (plain `String` but closed vocabularies, already exact-matchable via `filter_fields`). Full-text `tsvector`/GIN rejected — much larger change, no ranking requirement, `ILIKE` adequate at current volumes. No schema/migration/route/RBAC/frontend change. Corrects TC-ADMIN-013 in place, whose negative half named `TestSuite` — an entity that now *has* `search_fields` | Accepted |
| [0071](0071-entity-table-column-preferences.md) | COLPREF-1 — per-entity column visibility + ordering on the generic admin entity list, persisted client-side. New "Columns" button in `EntityTable`'s own `.card-header .card-tools` opens a new `ColumnPreferencesModal` organism listing every field the served schema (`GET /entities/{resource}/schema`, [ADR-0055](0055-admin-3-backend-driven-entity-schema.md)) exposes as a table column (the `showInTable !== false` set — the same source the columns themselves come from, never a second list): a checkbox per field (show/hide), Up/Down buttons per field (reorder — **not** drag-and-drop, which would be a new frontend dependency this story explicitly declines), and "Reset to defaults" (clears the stored preference outright). Persisted in `localStorage`, keyed per entity as `testnexa.column-prefs.<config.resource>` so `requirement` and `test_case` can never collide, stored as `{"v":1,"order":[...],"hidden":[...]}`, every read/write `try`/`catch`-wrapped so a private window or blocked site data degrades to config defaults rather than throwing (NFR-71). **`EntityTable` owns this state itself**, unlike `page`/`pageSize`/`sort`/`filters`/`search`, which `EntityListPage` owns — those four each map to a backend query parameter and so belong to the component owning the list query, whereas column visibility/order maps to nothing server-side and affects only what this component paints, so every current and future `EntityTable` caller gets it with zero prop plumbing (in-repo precedent: `AppHeader`'s own `localStorage`-persisted color mode, [ADR-0054](0054-tabler-shell-migration-phase-2.md)); **`EntityListPage.tsx` needs no change at all**. Two locked (always-visible) classes, a deliberate judgment call: a config's `detailLinkField` when `detailPath` is set ([ADR-0060](0060-projects-page-retired-generic-surface.md) — `Project.name` today; it carries the only navigation into the detail workspace) and whichever field is last-remaining-visible (no actions-only table) — both render as a disabled, checked checkbox, and the apply layer force-restores a locked field even if a stale stored preference marks it hidden. Merge rules: an unknown stored name is ignored, a schema field absent from the stored `order` is appended in config order (so an entity that gains a column later shows it), a corrupt value falls back to defaults wholesale. Pure logic split into `frontend/src/lib/columnPreferences.ts` (`loadColumnPreferences`/`saveColumnPreferences`/`clearColumnPreferences`/`applyColumnPreferences`/`lockedFieldNames`), unit-testable with no DOM. No backend change of any kind — no route, no schema, no migration, no RBAC; no server-side/cross-device persistence (named as a trade-off, not an omission); no column resizing. **Numbered `0071`, not `0070`** — `0070`/WBS `11.50`/`§58`/`NFR-70`/`TC-ADMIN-044..046` are claimed by the sibling `search-fields-audit` branch, unmerged and in flight when this docs pass was written, so these are the next-free values *above* that branch's claims rather than above `origin/main` alone | Accepted |

| [0072](0072-entity-table-filter-modal.md) | ENTITY-FILTER-1 — the generic admin list's fourth list-query capability (after pagination/`?q=` search, ADR-0022, and sort, ADR-0056) finally gets a UI, and its backend allow-list stops being a per-entity opt-in. **Two independent gaps closed in one pass.** (a) `EntityTable`'s docstring still claimed it rendered "a filter row for `filterFields`" and its props still declared `filters`/`onFilterChange` — an earlier refactor had dropped the row and left the contract behind, neither prop destructured nor rendered anywhere, while `EntityListPage` continued the whole time to own real `filters` state, key its list query on it, and pass it to `listEntities`: the state-to-query pipeline was live and correct end-to-end, only the control that writes to it was missing. (b) `filter_fields` was an opt-in only **7 of 27** registered `CrudEntityConfig`s ever took, so a modal over today's tuples would be useful on 7 entities and empty on 20. Fix: `FieldMeta` gains `filterable` (default `true`), served as `"filterable"` on every `GET /entities/{resource}/schema` field entry alongside `sortable`, and the list route derives its filterable set from that same `derive_entity_schema` at router-build time — **the identical mechanism, for the identical stated reason, [ADR-0053](0053-tabler-install-phase-1-cdn.md)/[ADR-0056](0056-admin-4-generic-admin-crud-column-sort.md) already chose for sort**. The rule: every served field is filterable **except those whose derived type is `text`**; `CrudEntityConfig.filter_fields` survives only as an optional *narrowing* override and the 7 existing declarations are deleted (each a strict subset of the derivation, so keeping them would leave exactly those 7 entities *less* filterable than the other 20). Deliberately a derivation where **[ADR-0070](0070-search-fields-audit-and-numeric-cast.md) chose a hand audit** for `search_fields`: equality is type-safe on every scalar column where an un-cast `ILIKE` against an `Integer` is a hard `ProgrammingError` (the constraint that forced ADR-0070 to reason column-by-column), and a closed vocabulary is exactly what exact-match filtering is *for*. New: a malformed filter **value** coerces per column type (UUID/bool/int/date/enum) and raises `422` with `field_errors` — byte-for-byte `apply_sort`'s existing envelope, not a new error shape — on a path that was previously reachable but untested; `apply_filters_and_search` stays pure. **Widening exposes nothing new**: the derived fields are exactly the `summary_schema` fields the list route already serializes in full into every response body, composed onto an already-org-gated query (NFR-1 untouched; ADR-0070 §5's secret-column reasoning carries over — those models have no `CrudEntityConfig` at all). UI: one icon-only "Filter" button in `.card-tools` beside [ADR-0071](0071-entity-table-column-preferences.md)'s sibling "Columns" button, opening a `FilterModal` organism composing the existing `Modal`/`Button`/`Select`/`TextInput`/`FkSelect`/`FkAutocomplete` primitives (no new raw markup). **AND-only** — the backend supports exact equality and nothing else and `AND`s its `.where()` calls unconditionally, so no operator control is rendered at all rather than a single-option select implying a choice nobody has — and **at most one condition per field** (a second compiles to `field = a AND field = b`, always empty, read by a user as "no results" not "contradictory query"; the picker omits any field another row claims, making the state unrepresentable). Value controls reuse `EntityForm`'s own per-type branch verbatim. **Filters are deliberately NOT persisted — an explicit divergence from ADR-0071's `localStorage`**: a hidden column announces itself, a silently-filtered list just shows *fewer rows*, which reads as missing data or someone else's deletions; state stays in `EntityListPage` with the other four list-query params (all five already "component state only", TC-DS-018), mitigated by an active-condition count badge + `btn-outline-primary` on the button. Filter state deliberately stays in `EntityListPage`, **the opposite of ADR-0071's in-`EntityTable` choice**, on ADR-0071's own stated rule (a parameter that maps to a backend query belongs to whoever owns the list query); the dead `onFilterChange(field, value)` becomes `onFiltersChange(filters)` because Apply commits the whole set at once and a removed condition must actually disappear rather than survive a per-key merge. No schema change, no migration, no new route, no new query parameter (`?<field>=<value>` is ADR-0022's, unchanged), no RBAC change, response body untouched | Accepted |

| [0073](0073-generic-entity-detail-page.md) | **Extends the ADR-0025/ADR-0027 generic admin CRUD surface from two page components to three.** A new read-only `EntityDetailPage` (`container/entity-crud/EntityDetailPage/`) on two new routes, `/orgs/:orgId/admin/:entity/:id` and `/projects/:projectId/admin/:entity/:id`, added inside ADR-0057's own `entityCrudRoutes()` call site so both scopes get them from the same two existing `App.tsx` calls. It renders `config.fields` **unfiltered** — the single absent `.filter((f) => f.showInTable !== false)` is the whole feature, and it makes readable, for the first time anywhere in the app, the 5 `showInTable: false` fields on `TestPlan` (`scope`/`approach`/`staffing_and_training`/`schedule`/`created_by_actor_id`) plus the same class on `TestCase`/`TestExecution`/`Defect`/`RiskItem` — previously visible only by opening the *edit* form, i.e. entering a mutation surface to perform a read. Entry point: `EntityTable`'s new optional `onRowClick` makes each `<tr>` a keyboard-reachable (`tabIndex`, Enter/Space) affordance, with the Actions cell stopping propagation so Edit/Delete stay independent; omitting the prop reproduces the prior `<tr>` byte-for-byte. `EntityConfig.detailPath` (ADR-0060) takes precedence over the generic route, so `Project`'s row click still opens its bespoke `ProjectDetail` workspace — a branch on config data, not on an entity name. `EntityTable`'s cell renderer and batched fk-label effect are extracted to `components/molecules/entity-field-value/` + `pages/admin/useFkLabels` and shared by both pages (a reuse extraction, **not** a decision of its own — no rendered-output change; all 548 pre-existing Vitest tests passed unmodified across the move). Read-only by design: Back + permission-gated Edit, nothing else. No backend/API/database/RBAC change of any kind. Frontend `tsc` clean + 565/565 Vitest, 5/5 new e2e across 3 structurally different entity types. **Its "related-record panels ... deliberately out of scope" Consequences clause is partially superseded by [0074](0074-entity-detail-relationship-tabs.md); every other part stands** | Partially superseded by 0074 |
| [0074](0074-entity-detail-relationship-tabs.md) | **Partially supersedes [0073](0073-generic-entity-detail-page.md)'s "related-record panels are out of scope" clause.** `EntityDetailPage` gains tabs: an **Info** tab (ADR-0073's all-fields view, unchanged) plus one tab per *inbound* relationship. The relationship graph is **derived on the backend**, not hand-authored: new `crud_factory.derive_entity_relations(config, all_configs)` walks `ALL_ENTITY_CONFIGS` and is served as a tenth key, `relations`, on `derive_entity_schema` — so it rides the schema request every admin page already makes, needs no extra round trip, and reaches the MCP `describe` tool for free (both share that function). **One-to-many** = another entity's `FieldMeta.ref_entity` pointing here; **many-to-many** = an ADR-0005 link table with one FK here, classified **structurally** (exactly 2 FKs, no `create`/`update`) rather than by `_link` name — correctly excluding the `TestExecution`/`RoleAssignment`/`RiskItem` two-FK near-misses. **Many-to-one gets no tab** (already a labelled value on Info). A relation is emitted only when `GET /{entity}?{scopeField}=` can actually serve it (the FK must be the child's own `scope_field`, since `extract_scope_value` 422s otherwise) — **12 inbound FKs are therefore excluded and flagged, not worked around**, each needing its own backend list-capability decision; a diff-based unit test asserts the excluded set is the exact complement of the served one. Many-to-many tabs list the link rows but are labelled/navigated by the far entity (`targetField`), with a `" (linked)"` suffix that disambiguates `Requirement`'s two genuine `TestCondition` tabs. The scoping column is hidden (same value on every row) via a non-mutating config copy. Active tab in `?tab=`, validated, Info as fallback. New `Tabs` molecule — the reuse check found no existing tab primitive — stock Bootstrap 5 markup, **no `data-bs-toggle`** (Tabler's loaded JS would fight React for panel ownership), no count badge (would need N list requests, and `badge bg-secondary` is the known-invisible class). `make_crud_router` switches to a new `derive_sortable_fields` because the full schema now needs the registry at a point where the registry is still importing. 22 relationships across 9 entities live. Backend unit **577/577**, frontend `tsc --noEmit` clean + Vitest **86 files / 584 tests**, new e2e `admin7-entity-relation-tabs.spec.ts` | Accepted |

| [0075](0075-junction-table-registry-completeness.md) | **Completes [0074](0074-entity-detail-relationship-tabs.md)'s derivation and corrects its Decision §4 exhaustiveness claim.** ADR-0074 argued a *computed* relationship set cannot silently omit an entity — true, but it never examined **the set being enumerated**: `entity_registry._ALL_CONFIGS` is a hand-typed tuple, the one hand-authored list in the chain. `TestSuite`'s detail page therefore rendered **no tab strip at all** (`"relations": []`, confirmed live) despite two real, production-populated many-to-many relationships — `test_suite_test_case` (REQ-4/0030) and `test_plan_test_suite` (PLAN-1/0031) had bespoke membership routes but no `CrudEntityConfig`. ADR-0074's own completeness test could not catch it: it partitions the FKs of `ALL_ENTITY_CONFIGS`, the same set the derivation walks, so an unregistered model contributes nothing to either side and the partition held **vacuously**. Fix is two-part. (1) Both junctions get a read-only config (`methods={"list","get"}`, `create_schema=None`, `update_schema=NoSchema`), shaped verbatim on `trace.py`'s four link tables and living in the bespoke module that already owns each junction's HTTP surface; scoped on the **parent** side (`test_suite_id`/`test_plan_id`) so the reverse directions join 0074 §4's enumerated exclusions rather than widening `scope_field` for two of six link tables. (2) The durable half — completeness is now asserted at the **model layer**: `test_adr75_registry_completeness.py` walks SQLAlchemy `Base.metadata` (which a model joins by *declaration*, so it cannot be forgotten the way a registry row can) and fails on any FK-bearing table that is neither registered nor in a reasoned `EXPECTED_UNREGISTERED_CHILDREN` entry; mutation-tested. Full audit of all 8 junction tables: **4 flagged not fixed** — `test_case_test_design_technique` (relationship has *no implementation anywhere*; a config would ship a permanently-empty tab), `role_permission` (RBAC grant graph — security decision, and system roles are `org_id IS NULL`), `approval`/`invite` (bespoke, `invite` carries a `token_hash`). **The `ref_entity` mirror case was audited against live `pg_constraint` FKs and is clean** — all 7 gaps deliberate and documented. RBAC: 2 new read-only resources (29→31, catalog 100→102), granted to `org_admin`/`auditor` by definition and to `test_manager`/`tester` explicitly; migration `7d2c91af4e68` is the **first RBAC extension here that inserts `Permission` rows**, not just grants. MCP tools 147→153 (resources unchanged at 31 — both junctions were *already* config-less registry rows, part of why the gap hid). Backend unit **593/593**, integration **4/4** new, frontend `tsc` clean + Vitest **594/594**, e2e **6/6**. **[Amendment 1](0075-junction-table-registry-completeness.md#amendment-1-2026-09-15--every-junction-is-scoped-and-therefore-tabbed-from-both-ends) (2026-09-15, same branch, pre-merge) — every junction is scoped, and therefore tabbed, from BOTH ends.** A correction to this ADR's own **Decision §3**, which did not reject the change on its merits but explicitly parked it ("widening all six later remains open") on a coherence objection that doing all six at once spends. The symptom §3 left alive: `GET /entities/test-cases/schema` carried no `test_suite`/`test_plan` relation at all and `GET /entities/defects/schema` returned `"relations": []` — `Defect`'s page rendered no tab strip whatsoever, the very symptom this ADR fixed for `TestSuite`, surviving in four more places. All **6** junctions' `scope_field` widened to the branching 2-tuple of both FK columns, pre-amendment arm **first** (so `scope_validation_error` keys its `422` on the same field name and every item route's tenant walk stays byte-identical). `derive_entity_relations` **unchanged — zero lines**: `_scope_candidates` already explodes a tuple, the mechanism behind `RiskItem`'s two parents, so a capability widening across six entities cost nothing in the derivation, schema route, MCP `describe` or frontend. The one new symbol, `crud_factory.branching_resolver`, is **mandatory rather than stylistic** — `_resolve_scope_for_write` hands `resolve_org_id` a `SimpleNamespace` carrying only the supplied arm, so a single-arm walk would type-check, derive a valid relation, render a real tab, and `404` every request it fires. Relations **22 across 9 entities → 30 across 10** (`TestCase` +3 to six tabs, `TestSuite`/`TestCondition` +1 each, `Defect` from zero tabs to one); ADR-0074 §4's exclusions **14 → 8**, losing its entire link-table row. **No new permission code, no migration** (permissions are per-resource, not per-direction). Accepted contract change on those six list routes only: unscoped now `422`s with `"exactly one of X or Y must be set"` (same status/code/keyed field), and supplying **both** arms is newly reachable and `422`s with `"...not both"`. Two pre-existing tests **inverted, not deleted**, both TC-ADMIN-076, since each pinned the superseded §3. New unit suite **47/47** (incl. an in-suite mutation check proving the single-arm resolver genuinely fails the same inputs) + integration **5/5** (all create-via-real-bespoke-route then read back, all 6 junctions both directions, plus an NFR-1 cross-tenant `404` on the new arm). NFR-77, TC-ADMIN-078/079, WBS 11.56, Test-Design §64 | Accepted |
| [0076](0076-relationship-tab-write-actions.md) | **Extends [0074](0074-entity-detail-relationship-tabs.md) and [0075](0075-junction-table-registry-completeness.md) + its Amendment 1, superseding neither** — the derivation, the exclusion rules, the `?tab=` posture and the hidden scoping column all stand unchanged, and `derive_entity_relations` is not touched. ADR-0074/0075 left **all 30 relationship tabs read-only**, which for four of [ADR-0005](0005-traceability-link-dedicated-join-tables.md)'s own traceability tables meant a link row could only ever be written as a *side effect* of authoring one of its two ends (REQ-2's/REQ-3's create routes, EXEC-3's raise-defect): "link a test case that already exists" had no route anywhere, so the traceability matrix could be **grown but never assembled**. Two facts no derivation can produce — *which route writes a link row* (the URL shapes are arbitrary and bespoke) and *which permission it gates on* (REQ-4/PLAN-1 gate on the **parent's** `test_suite.update`/`test_plan.update`, not on anything named after the junction) — are now **declared by the route itself**: a new optional `CrudEntityConfig.link_create` (`LinkCreateAction{path_template, permission}`), set on link entities only, served as an **eleventh, additive key `linkCreate`** on `GET /entities/{resource}/schema` (`null` for the 23 non-link entities) beside ADR-0074's `relations`, so it rides the schema request the tab already makes and costs no extra round trip. Generic rather than a hand-authored map in disguise — the exact thing ADR-0074 Decision §1 forbids: placeholders are named after the link row's **own FK columns** (`{requirement_id}`/`{test_case_id}`), so a single declaration serves a tab mounted at *either* end of a junction (which matters precisely because Amendment 1 made all six bidirectional), and each declaration sits in the module that defines the route, so a route change and its declaration are one diff. **Four new bespoke routes**, all in `trace.py` (`POST /requirements/{id}/test-case-links/{case_id}`, `POST /requirements/{id}/test-condition-links/{cond_id}`, `POST /test-conditions/{id}/test-case-links/{case_id}`, `POST /test-cases/{id}/defect-links/{defect_id}`), each the byte-identical boundary sequence [ADR-0030](0030-req4-test-suite-membership-bespoke-routes.md) fixed — parent unresolvable/no membership → `404`; permission missing → `403`; far row in another org or absent → `404`, never `403` and never `422` (NFR-1/[ADR-0007](0007-org-scoped-multi-tenancy.md)); same org but different **project** → `422 validation_error`; duplicate pair → `409` — sharing `_gate_parent`/`_load_same_org`/`_insert_link` rather than a fifth independently-reasoned copy, and gating membership with the `AIAgent`-aware `_actor_membership_exists` per `backend/CLAUDE.md`'s rule for any *new* resource-gating route. **No generic-surface change**: `methods` stays `{"list","get"}`, `create_schema` stays `None`, a link row still cannot be `PATCH`ed, `DELETE`d or factory-created — links remain immutable (ADR-0005). **RBAC:** a fourth resource grouping, `LINK_CREATE_RESOURCES`, adding one `.create` code per traceability link (**catalog 102 → 106**), modelled as its own tuple rather than by promoting the four into `CRUD_RESOURCES`, which would mint `.update`/`.delete` codes for routes that do not exist and never will. Bundles: `org_admin` all 4 by definition; `test_manager` all 4 **plus the 3 `.read` codes it lacked** (read and write together — granting the write alone ships a button on a tab whose own list request `403`s before it can render); `tester` **only** `test_case_defect_link.create` (it already holds that link's `.read` and full `defect` CRUD, so "this failure is a defect that already exists" is a workflow it performs; the three requirement-level links are `test_manager`'s activity, the same restraint [ADR-0033](0033-plan3-test-cycle-creation-and-execution-scope-check.md)/[ADR-0044](0044-exec-3-raise-defect-from-execution.md) took); `auditor`/`ai_agent_scoped` nothing. REQ-4's and PLAN-1's shipped routes are deliberately **not re-gated** — `LinkCreateAction` declares its permission rather than deriving it precisely so both shapes coexist. Migration `3e6b08c5da71` backfills (the second RBAC extension here that inserts `Permission` rows, after ADR-0075's `7d2c91af4e68`), with a deliberately **asymmetric `downgrade()`**: it removes the 4 new codes and every grant of them, but for the 3 *pre-existing* `.read` codes it revokes only `test_manager`'s grants, never the catalog rows that predate it. Idempotency proven by invoking `upgrade()` **twice through a real `Operations` context**, never a second `alembic upgrade head` — which `backend/CLAUDE.md` documents as a bookkeeping-level no-op that never re-enters the function body. **UI: exactly one action per tab, chosen by `relation.kind`, never both** — one-to-many → **"New"**, opening the same `EntityForm` create modal `EntityListPage` already hosts, with `relation.scopeField` passed as `lockedValues` (disabled display field, out of the Zod schema, merged into the payload by `EntityForm` itself) so the row lands under this parent and cannot be retargeted; many-to-many → **"Link existing &lt;far entity&gt;"**, a modal wrapping the searchable `FkAutocomplete` picker REQ-5/[ADR-0069](0069-req-5-standalone-test-case-with-optional-requirement-link.md) already built, POSTing to the served `linkCreate.pathTemplate` interpolated from `{scopeField: parentId, targetField: pickedId}`. **No new form component and no new picker component.** Still no per-row Edit or Delete: every listed record is fully editable one click away and a link row has nothing to edit. Both actions gated twice, answering different questions — *can the API do this at all* (`config.methods`/`config.linkCreate`) and *may this actor* (`usePermissions`, fail-closed while loading) — with a missing permission making the button **absent, not disabled** (UI Design Document §5's hide-don't-disable posture, identical to `EntityListPage`'s own `canCreate`). Picker scoping derived in three ordered cases, no per-entity branch: no `scopeField` → search unscoped; `scopeField === "project_id"` and the route carries `:projectId` → supply it (9 of the 12 live link directions); otherwise render the far entity's own `scopeSelector` through the shared `ScopeSelector` molecule inside the modal. **MCP tools 153 → 157** (each new route gets its registry executor, `BESPOKE_EXTRA_ACTIONS` row and generated-description entry in the same change). **Unlinking is explicitly out of scope** — two of six junctions have a `DELETE` today, four have none, and "what does removing a traceability link mean for an already-exported RTM?" is a real decision belonging to its own story; the tabs therefore grow monotonically. **Shipped 2026-09-16 by [0077](0077-relationship-tab-unlink-action.md).** **Accepted, not reconciled:** `POST /test-cases/{id}/link-requirement` (REQ-5) and `POST /requirements/{id}/test-case-links/{case_id}` now both write `RequirementTestCaseLink` under different rules — REQ-5's is a one-shot retrofit that `409`s if the case has **any** requirement link, this one is the table's real many-to-many contract and `409`s only on the *pair*; changing REQ-5's shipped contract is a decision a future story should take deliberately rather than have this ADR make as a side effect. **Found and fixed: standalone `TestCase`s could not be added to a `TestSuite` at all.** `test_suite_membership.py` carried a private three-branch copy of the `TestCase`-project walk, written for REQ-4 before `TestCase` had a `project_id` column; REQ-5/ADR-0069 added that column and a fourth branch to the *org* resolver, and nothing pointed at the project-side copy — so it returned `None` for every standalone case and `add_test_case_to_suite` rejected **all** of them with `422 "This test case belongs to a different project."` even when suite and case sat in the same project. That copy's own docstring had predicted exactly this failure and argued the duplication was safe because it never gates tenancy; it was safe in that narrow sense and still wrong, since the duplication is what made the REQ-5 change *look* complete. Promoted to `crud_factory.resolve_test_case_project_id` beside its org-side sibling, mirroring its branch order, with `test_suite_membership.py` delegating; the regression test asserts against **REQ-4's own route** rather than any new one, since that is the route that was broken. Recorded here rather than in its own ADR — a resolver-branch completeness fix inside this ADR's necessary surface, found *before* shipping, which is the shape the ADR-0039/ADR-0040 "same-day fix still gets its own ADR" convention does **not** cover (`docs/CLAUDE.md`). **Verification, all executed against the isolated stack** (2026-09-15): backend unit **693/693** (44 new, including in-suite mutation checks on both directions of the completeness partition), backend integration **16/16** for this story against a database migrated to `3e6b08c5da71`, frontend `tsc` clean + Vitest **614/614** (20 new), new e2e `admin8-relation-tab-write-actions.spec.ts` **3/3** (a 1-n create, a link through a pre-existing membership route, a link through a brand-new traceability route). Seven pre-existing tests **corrected in place, none deleted** — each had pinned a claim this ADR makes false (the schema envelope's ten-key list, the "`requirement_test_case_link.create` is never advertised" MCP rows, the tool-count anchors 153 → **157**, and the RBAC catalog-size/read-only-action assertions, narrowed rather than loosened). FR-ADMIN-8, NFR-78, TC-ADMIN-080..095, WBS 11.57, Test-Design §65 | Accepted |
| [0077](0077-relationship-tab-unlink-action.md) | **Closes [0076](0076-relationship-tab-write-actions.md)'s own deferred item, superseding nothing** — that ADR's Consequences said "Unlinking is not shipped... belongs to its own story" and its Alternatives listed per-row Unlink as **deferred, not rejected**, on the specific ground that shipping it for two of six junctions would be the incoherence [0075](0075-junction-table-registry-completeness.md) §3 declined to create. All six move together here. The state this fixes: ADR-0076 made every relationship tab writable and **append-only**, so a traceability matrix could be assembled and never corrected — and the role accountable for its accuracy (`test_manager`, the only bundle with `requirement.export_rtm`) had no way to remove a mislink. Meanwhile **two junctions already had working `DELETE` routes** (REQ-4's `/test-suites/{id}/test-cases/{case_id}` and PLAN-1's `/test-plans/{id}/test-suites/{suite_id}`, [0030](0030-req4-test-suite-membership-bespoke-routes.md)/[0031](0031-plan1-test-plan-membership-and-status-transition-routes.md)) that nothing on the generic surface could reach, because the schema knew how to declare a link *create* and nothing else. **The RTM question ADR-0076 posed is answered, not dodged:** an RTM export is a point-in-time artifact holding no reference back to the link rows, so removing one changes what the *next* export says exactly as adding one does — symmetric with the additive half ADR-0076 already accepted. What the question does surface is that an unlink leaves **no audit trail** — and neither does a link, so this ADR makes an existing gap symmetric rather than introducing one; a dedicated audit log over traceability mutations is named as its own future decision, not invented under a button. **`CrudEntityConfig.link_delete` (`LinkDeleteAction{path_template, permission}`), `link_create`'s exact mirror**, served as a **twelfth, additive key `linkDelete`** (`null` for the 23 non-link entities). Three choices with cheaper-looking alternatives, each rejected on a stated ground: it is a **second key, not a verb flag on `LinkCreateAction`** (the two templates are byte-identical for all six today — a coincidence of design, not a contract, and the completeness test deliberately checks placeholders against `fk_fields_of` rather than against the create template); it is **read independently of `link_create`** on both sides of the wire (a junction that can be linked and not unlinked is exactly what four of six *were* between the two ADRs); and it is **not** expressed by adding `"delete"` to `methods`/`full_methods`, which means "the factory serves `DELETE /{resource}/{id}`" — false for a link row, which is addressed by its *pair*, and flipping it would make `EntityTable` offer a row delete that `405`s on the entity's own list page too. **Four new bespoke routes** in `trace.py`, at the **same URL** as each `POST`, each reusing ADR-0076's `_gate_parent` (the same function, not a copy) so the NFR-1 boundary is literally shared between an entity's link and unlink: parent unresolvable/no membership → `404`; permission missing → `403`; pair not linked → `404`; else `204`. Two deliberate differences from the `POST`: **`404`, not an idempotent `204`**, for a pair that is not linked (ADR-0030's own asymmetry with `POST`'s `409`, inherited — and the more useful answer when two people work the same tab), and **no cross-project `422` and no far-side org walk**, because an existing link row already passed both at insert time so re-deriving them could only reject rows the system itself created, leaving a past bug permanently uncorrectable. `_delete_link` shared by all four, same reasoning as `_insert_link`. **No generic-surface change**; links remain immutable (no `PATCH`) — ADR-0005's delete-and-recreate is what these routes make *possible*. **RBAC:** a fifth grouping `LINK_DELETE_RESOURCES`, one `.delete` code per traceability link (**catalog 106 → 110**), kept a separate tuple from `LINK_CREATE_RESOURCES` despite equal contents — they are equal by coincidence of scope, not by rule, and one `LINK_WRITE_RESOURCES` would make an append-only (or delete-only) junction unrepresentable. Bundles: `org_admin` all 4 by definition; `test_manager` all 4, symmetric with ADR-0076's creates and on the specific ground that it owns `requirement.export_rtm` (no `.read` backfill needed — `3e6b08c5da71` already granted those); `tester` **only** `test_case_defect_link.delete`, exactly mirroring its single `.create`, since the unlink is the literal undo of the one link it may make; `auditor`/`ai_agent_scoped` nothing. REQ-4's/PLAN-1's shipped routes are **not re-gated** — they keep `test_suite.update`/`test_plan.update`, which is why `LinkDeleteAction` declares its permission rather than deriving it. **`tester` gaining a `.delete` is a deliberate departure** from this repo's repeated restraint ([0018](0018-admin-shell-sidebar-layout.md)'s `release`, [0033](0033-plan3-test-cycle-creation-and-execution-scope-check.md)'s `test_execution`, [0044](0044-exec-3-raise-defect-from-execution.md)'s `defect`): those withhold deletes of records carrying **content and history**, whereas a link row is two FKs and a timestamp (ADR-0005), so removing one retracts an assertion rather than destroying work product. Stated so it reads as a judgment made, not a precedent overlooked. Migration `8c1d5a7b93e2`, with a **symmetric `downgrade()`** (unlike its ADR-0076 sibling's) because it grants nothing it does not also create — a property asserted, so a future edit adding a backfill fails a test rather than silently leaving the downgrade wrong. Idempotency proven by invoking `upgrade()` twice through a real `Operations` context, **and the idempotency harness is itself mutation-checked** against a deliberately non-idempotent insert, so "no rows moved" and "the harness cannot see rows move" are distinguishable results. **UI: a per-row "Remove" on many-to-many tabs only** (trash glyph, icon-only with `aria-label`, the shape `EntityTable`'s own row actions already use). Both ids are already on screen — `relation.scopeField` is the record being viewed, `relation.targetField` is read off the row — so it needs no picker, no form and no extra fetch. 1-n tabs are unchanged: their rows are *records*, and removing one would mean deleting the child, which its own screen already offers. Gated twice and **hidden, not disabled**, with the two permissions checked **independently of the create side's** (different codes for four of six, so "may link" is never evidence of "may unlink"). **Confirms first**, in the same `Modal` shape `EntityListPage`'s row delete uses and deliberately not a native `confirm()` (nothing here uses one, and it cannot render an `ApiError`'s message); the body says what survives, because "Remove" alone reads as a delete. **A failed unlink keeps the confirm open** — the opposite of ADR-0076 Amendment 1's compound create, and for a stated reason: nothing was written, so re-confirming is a *retry*, and the two realistic failures say different actionable things (`404` someone else already removed it, `403` you may not). `EntityTable` gains one narrow prop, `onUnlink`, rather than reusing `onDelete` — which is gated on `config.methods.includes("delete")`, false for every link entity and required to stay so. **Deliberately NOT extended to MCP**, and this is a decision rather than an omission: the generated-tool layer's `_make_delete_tool` produces `async def _delete(id: UUID)` dispatching a **single** id, which cannot express a pair-addressed link row — an MCP unlink is a new *action kind* with its own name, argument shape and generated description, i.e. real new MCP surface. REQ-4's and PLAN-1's `DELETE`s have been MCP-unreachable since they shipped for the identical reason, so all six junctions stay *consistent* (link yes, unlink no) rather than newly inconsistent. Flagged as an open gap. **Found, not fixed:** the seeded permission catalog carries two rows no catalog function declares — `ai_agent.create`/`ai_agent.update` exist in every migrated database (migration `d33d66f4b3c3`, MCP-4/[0063](0063-mcp-integration-screen.md)) but `ai_agent` is in neither `CRUD_RESOURCES` nor `READ_ONLY_RESOURCES`, so `build_permission_catalog()` returns 110 where a live database holds 112. Nothing is currently broken (every count test compares like with like), but a fresh database and a migrated one genuinely differ — exactly the failure class the backfill-migration discipline exists to prevent. Surfaced while verifying this ADR's migration live; left as its own item per root `CLAUDE.md`'s rule against drive-by fixes. **Verification, all executed against the isolated stack** (2026-09-16): backend unit **808/808** (46 new, incl. in-suite mutation checks on both directions of the `link_delete` partition), backend integration **16/16** for this story against a database migrated to `8c1d5a7b93e2`, frontend `tsc` clean + Vitest **753/753** (15 new), e2e `admin8-relation-tab-write-actions.spec.ts` extended with a live link-then-unlink flow. Four pre-existing tests **corrected in place, none deleted** (the schema envelope's eleven-key list, and the RBAC catalog-size/read-only-action/`org_admin`-bundle assertions — each narrowed to the new exact set rather than loosened to a subset check). FR-ADMIN-9, NFR-80, TC-ADMIN-100..115, WBS 11.59, Test-Design §66 | Accepted |
| [0078](0078-compound-create-through-bespoke-routes.md) | **Extends [0076](0076-relationship-tab-write-actions.md) — specifically its Amendment 1 — superseding nothing**, and adds no route, no permission code and no migration: the four link-create routes, the four `DELETE`s, `linkCreate`/`linkDelete`, the catalog (**110**) and the picker-scoping rule all stand. What it closes is a clause ADR-0076 Amendment 1 wrote as if it were a boundary rather than a gap: *"3 of the 12 live link directions point at an entity authored only through bespoke routes, so the action correctly never appears for them."* Three of twelve is **25% of the surface the requirement is about**, and the three are `Requirement`/`TestCase` → "Test conditions (linked)" and `TestCase` → "Defects (linked)" — i.e. exactly the two entities whose authoring is *least* discoverable elsewhere (`TestCondition` has no generic `create` because [0028](0028-req3-test-condition-rigor-path-bespoke-routes.md) removed it — `requirement_id` is `NOT NULL` and the link row must be written in the **same transaction**, which `create_item`'s one-row insert structurally cannot do; `Defect.test_execution_id` is `NOT NULL` and [0044](0044-exec-3-raise-defect-from-execution.md)'s route additionally `422`s a non-failed execution). The absence was **silent** — no error, no `403`, just a button that never renders. **One new fact no derivation produces, beyond ADR-0076 §1's URL-and-permission pair: whether the bespoke create route already writes the junction's link row itself.** REQ-3's does (deliberately — a condition can never exist unlinked), EXEC-3's does; that is a fact about another module's *transaction body*, absent from the URL, the response and every schema, and it decides whether the client makes **one request or two** — guess it wrong in the "already linked" direction and the follow-up `linkCreate` `409`s on the pair the first call just wrote, turning a success into a user-visible error. So: **`CrudEntityConfig.compound_creates`, a tuple of `CompoundCreateAction{far_field, path_template, permission, links_automatically, parent_entity?, parent_label?, parent_label_field?, parent_filters}`**, set on link entities only and served as a **thirteenth, additive key `compoundCreates`** beside `linkCreate`/`linkDelete`, riding the request the tab already makes. Two deliberate differences from `LinkCreateAction`: it is a **list, `[]` rather than `null`** when empty (a presence flag answers "can this junction be linked at all"; this is searched *by direction*, `find(a => a.farField === relation.targetField)`, so "declares none" and "declares some, but not yours" already collapse — 3 of the 6 link entities serve `[]` for the second reason), and **`far_field` makes it directional**, because ADR-0075 Amendment 1 made all six junctions list from both ends and the two ends genuinely disagree: the *same* atomic route serves `Requirement` → test-conditions with `links_automatically=True` and no picker, and `TestCase` → test-conditions with `links_automatically=False` and a Requirement picker. One flag per junction could not express that. **Whether a parent picker is needed is derived, not declared** — the template's single placeholder compared against the tab's own `relation.scopeField`: equal → the route's parent *is* the record being viewed (one modal, no picker, one request); unequal → the user picks the parent first, in the same `FkAutocomplete` "Link existing" uses, above the form and gating it. Derived because a declaration could contradict the template with nothing to say which is wrong; the unit test asserts the partition in **both** directions. The `Defect` direction is the one needing most care: `POST /executions/{id}/defects` writes `TestCaseDefectLink(test_case_id=execution.test_case_id, …)`, so it lands on *this* tab's case **precisely when the chosen execution belongs to it** — an unnarrowed picker would let a user file a defect against a record they are not looking at, which never appears in the tab they created it from. Hence a picker scoped to this test case's own executions and filtered to the failed ones (`parent_filters={"result":"fail"}` — EXEC-3 `422`s otherwise, so offering a passed run is offering a guaranteed rejection); inside that scope `links_automatically=True` is **exact, not approximate**, and the whole action is one request in one transaction — the compound path is not a degraded fallback but the **better** one wherever it applies, since ADR-0076 Amendment 1's "created but not linked" partial state is unreachable on it by construction. **`TestExecution.scope_field` widens to the branching `("test_cycle_id","test_case_id")`** because that scoping needs `GET /test-executions?test_case_id=` to be a legal list request and it was not — same shape `RiskItem` has always had and ADR-0075 Amendment 1 gave all six junctions, with a `branching_resolver` whose **cycle arm is declared first** so every item route's tenant walk stays byte-identical, plus a matching second `scope_selector` option (or the admin list page could scope by the cycle arm only while the route served both). Both columns are `NOT NULL` on every row, so only *list* scoping changes; no caller in the repo passes both arms (audited across `backend/tests/`, `e2e/tests/`, `frontend/src/`). Three consequences, all checked before the change rather than discovered after: the new arm becomes legal; **`TestCase` gains a one-to-many "Test executions" tab** — a test case's own execution history, which nothing else in the app surfaces; and **ADR-0076's own documented dead end retires** — `TestCase` → "Defects (linked)" had *neither* action working (its "Link existing" was blind behind `ScopeSelector`'s cascading-picker gap, `Defect`'s selector searching a `TestExecution` scoped by `test_cycle_id` alone) and now has both. **Gating moves with its reason:** the button requires the action's **own declared** `permission` (not `${farResource}.create` by convention — they coincide for all three today, which the unit test pins, and the client still reads the declared value for the same reason `LinkCreateAction` declares its own); ADR-0076 Amendment 1's *unconditional* `linkCreate.permission` requirement existed to foreclose a `201`-then-`403` stranding a real row, **and that reason does not apply when `links_automatically` is true** — there is no second call to be refused, so requiring it there would hide a button for a request that is never made. `createLinkNeedsLinkCall` is what both turn on. This is the **most surprising cell of the matrix** — "holds only the create permission" *renders* the button for the `Requirement`/`Defect` directions and *hides* it for the `TestCase` → test-conditions one — and is called out at the Vitest call site as well as in the ADR. Which roles this reaches is deliberately not uniform and no bundle is extended: `test_manager` holds full `test_condition` CRUD so both `TestCondition` directions work for it and only `defect.read` so the `Defect` one correctly does not appear; `tester` holds `defect.create` + `test_case_defect_link.create` and is exactly the persona whose workflow that is — an eighth ad hoc extension would make the `test_manager`-bundle audit ADR-0076 already flagged as overdue worse. **Completeness is asserted over the derived set, not the declarations:** `directions_without_a_create_path(ALL_ENTITY_CONFIGS) == set()` computed over the directions `derive_entity_relations` actually emits, so a future junction cannot ship a create-less tab silently; per ADR-0075's own lesson the checker takes its collection as a parameter and is **mutation-tested in-suite**, including against the subtlest vacuous-pass shape available here — a declaration pointed at the junction's *other* direction, which a "does this entity declare anything" check would have accepted. **Relationship-tab totals move: 30 relations → 31**, and ADR-0074 §4's exclusion table **8 rows → 7** (`("test-executions","test_case_id")` stopped being an exclusion the moment the scope widened). **`scopeField` is array-valued for a third entity, and one frontend comparison was quietly wrong about it** — `pickerScopeParams` did `scopeField === "project_id"`, which silently answers *no* for an array; normalized through a new `scopeArmsOf` helper rather than fixed at the one call site, changing no behaviour today (no far entity in a link direction has an array scope), which is exactly when that kind of fix is cheap. **One test's premise was superseded and inverted in place, not deleted:** `test_filter_field_only_fk_is_excluded` demonstrated ADR-0074 §2's *an FK must be a scope arm, not merely a filter field* rule using `TestExecution.test_case_id` — the rule is untouched, that column simply stopped being an example, so it is inverted (ADR-0075 Amendment 1's own precedent) with `test_filterable_alone_is_still_not_enough` added beside it asserting the original claim against `TestCase.test_level_id`, because a rule with no live example is a rule nothing defends. **Flagged again rather than absorbed:** the blank-optional-enum `422` ADR-0076 Amendment 1 found and deliberately did not fix is reachable from these modals too (`TestCondition.priority`/`Defect.severity` are required by their bespoke request schemas but derive as `required: false`, since those configs carry `create_schema=None` and the derivation has no create schema to read required-ness from) — same reason for not fixing it here, the fix is a change to the shared form affecting every entity and every create path; the tests select both fields explicitly and say why at the call site. Pre-existing tests corrected in place, none deleted: `test_adr74_entity_relations.py`'s `EXPECTED_EXCLUSIONS` row, `test_adr75_amendment1_bidirectional_junctions.py`'s 30→31 and 8→7, `test_entity_schema_derivation.py`'s twelve→thirteen keys, and `e2e/tests/admin7-entity-relation-tabs.spec.ts`'s positional tab strip (a new tab appeared on a screen this story is not about — updated in the same change, the only reason it was caught before a browser found it). FR-ADMIN-10, NFR-81, TC-ADMIN-116..128, WBS 11.60, Test-Design §67 | Accepted |
| [0079](0079-child-compound-create-for-one-to-many-tabs.md) | **Extends [0078](0078-compound-create-through-bespoke-routes.md), superseding nothing — the identical gap one relationship kind over.** The CTO's own follow-up click-through on the same `Requirement` screen ADR-0078 had just closed (`?tab=requirement-test-condition-links`) found the **direct** one-to-many tab (`?tab=test-conditions`) still had no "New" button, for the exact same reason (`TestCondition` has no generic `create`) — ADR-0078's mechanism never looked at one-to-many tabs, since it reads `compound_creates` off a *link* entity's config specifically. A live audit found **seven** such tabs; three have a real bespoke route whose own parent **is** the tab's own scope and are closed here, two need a genuinely new picker mechanism (a picked value filling a **body** field, not the path — every existing picker in this codebase fills a path placeholder) and are named as an explicit open gap rather than silently passed over, ~~two are exceptions no route could close without inventing a new capability (`TestLog` append-only, `OrgMembership`'s Invite+Accept flow)~~ — **corrected same day, pre-merge, this ADR's own `### Amendment 1`: `TestLog` was not actually an exception (a real route already existed), closed in [ADR-0080](0080-standalone-list-page-compound-create.md) instead; only `OrgMembership` remains a genuine exception.** **A new field, `CrudEntityConfig.child_compound_creates`, not a widened `compound_creates`** — the two disagree about what `far_field` means: `compound_creates`'s own completeness suite derives "the tab's own known field" as *"the other of this entity's exactly two FK columns,"* true for every link entity by definition; a one-to-many child is not reliably two-FK (`TestCondition` has exactly one relevant FK), so reusing the field would leave a test suite built entirely around "a junction has two FKs" quietly false for one class of declarer. Same `CompoundCreateAction` shape, matched by `far_field === relation.scopeField` (never `relation.targetField`, always `null` on a one-to-many relation). **No parent-picker UI is built for the one-to-many case in this pass** — all three closed declarations have the template's placeholder equal to `far_field` by construction, so the existing "New" modal's `lockedValues={{ [relation.scopeField]: parentId }}` already produces the right request with **zero new JSX**, since `config === farConfig` on a one-to-many tab; the mutation is a one-line branch (`activeChildCompoundCreate` present → `createViaCompoundRoute`, absent → the existing `createEntity`). **No "created, not linked" failure mode exists here at all** — a one-to-many row's presence in the list *is* the relationship, so there is never a second call to fail, a genuine simplification over ADR-0078's own n-n case. Served as a **fourteenth, additive schema key `childCompoundCreates`**, `[]` for every entity declaring none. Three declarations, all `links_automatically=True`, all reusing routes that already shipped (ADR-0028/ADR-0044/ADR-0033): `TestCondition` → `POST /requirements/{requirement_id}/test-conditions`; `Defect` → `POST /executions/{test_execution_id}/defects`; `TestCycle` → `POST /test-plans/{test_plan_id}/test-cycles`. No new route, no new permission code, no migration — the catalog stays **110**. Completeness mirrors ADR-0075's own lesson: `derive_entity_relations` is the oracle for which one-to-many directions exist, never a hand-list; a `CLASSIFICATION` table (closed/open/exception) is cross-checked against it so the split cannot silently drift, and both halves are mutation-tested (a flipped `far_field`, a removed declaration). FR-ADMIN-11, NFR-82, TC-ADMIN-129..133, WBS 11.61, Test-Design §68 | Accepted |
| [0080](0080-standalone-list-page-compound-create.md) | **A second UI host for [0079](0079-child-compound-create-for-one-to-many-tabs.md)'s `child_compound_creates`, superseding nothing, plus a same-day correction to 0079's own audit.** Found in one CTO message thread: the **standalone** `/admin/test-conditions` list page (not the relation tab 0079 closed) had no "New" button, then six sibling URLs asked about at once — `entry-exit-criteria`, `test-cycles`, `test-executions`, `test-logs`, `defects`, `risk-items`. A live audit (real HTTP calls, a minted token, no credential write) sorted these into three states: `entry-exit-criteria`/`risk-items` **already fully working** (generic `create` behind their own `scope_selector`, confirmed with real `201`s, zero code change); `test-conditions`/`test-cycles`/`defects` **broken** — `EntityListPage.tsx`'s `canCreate` gate never once looked at `child_compound_creates`, even though 0079 had already declared all three, because 0079 wired the mechanism into exactly one host (`EntityRelationTab`); `test-executions` **correctly open** (0079's own already-named gap, unchanged); `test-logs` **incorrectly closed off** — see the Amendment below. **The fix needs no new picker UI**: every entity declaring `child_compound_creates` also declares a `scope_selector` for the identical field (the generic `list` route requires it too), so `useEntityScope`'s already-resolved `scope.field`/`scope.value` are exactly what the declaration's `far_field` needs by the time any row renders — the same "no picker needed, the value is already known" reasoning 0079 established for a tab, reading a different already-known value. `canCreateViaCompound = !canCreate && config.childCompoundCreates?.find(a => a.farField === scope.field)`, gated on the declared permission; `createMutation` branches to `createViaCompoundRoute` when present, the existing `createEntity` otherwise. **`### Amendment 1` to ADR-0079, same commit:** `TestExecution` → "Test logs" was misclassified an exception ("append-only, no route could exist") — `POST /executions/{id}/comments` (EXEC-2) already exists, gated `test_execution.update`, structurally identical to the other three closed directions, missed because its name doesn't read like a generic entity create. Closing it needed `_TEST_LOG_CONFIG` to gain a real writable schema (`create_schema=AddTestLogCommentRequest`, **without** adding `"create"` to `methods` — the factory only ever registers a create route when both are true, so `TestLog`'s actual REST surface, `POST /test-logs` still `405`ing, is unchanged) plus an explicit `search_fields=()` override so the two newly-writable plain-string fields don't silently widen `?q=` as a side effect. `TestExecution` → "Test logs" moves from "exception" to "closed"; only `Organization` → "Org memberships" remains a genuine exception. **No new route, no new permission code, no migration** — the catalog stays **110**. Two pre-existing test literals (`test-logs`' `filterFields`, unit + integration copies) corrected in place for the two new filterable fields. FR-ADMIN-12, NFR-83, TC-ADMIN-134..137, WBS 11.62, Test-Design §69 | Accepted |
| [0081](0081-scope-selector-cascading-via.md) | **Closes a one-level-lower gap [0080](0080-standalone-list-page-compound-create.md) exposed but didn't cause: `ScopeSelector`'s own `FkAutocomplete` search could never find anything for `test-executions`/`defects`/`test-logs`, so the "New" button 0080 added was correct and unreachable.** Not a new bug — `scope-selector.tsx`'s own docstring already documented a "two-hop" limitation, undated: a `refEntity` whose own list route needs a scope param the page's route context doesn't supply (`TestCycle` needs `test_plan_id`; `TestExecution` needs `test_case_id`/`test_cycle_id`). New `ScopeSelectorOption.via: ScopeSelectorOption | None`, recursive by shape though every real declaration is one level deep, serialized by the same helper recursing into itself. `ScopeSelector` renders `via` as a preceding picker step; its resolved value threads into the outer option's own `extraParams` as `{[via.paramName]: pickedId}`, never reported through `onResolved`. Three declarations, all reusing an already-one-hop (`project_id`-scoped) entity: `test-executions`' "By test cycle" arm → `via=(test-plan, test_plan_id)`; `defects`/`test-logs` → `via=(test-case, test_case_id)` — `test-case` chosen over a `test-cycle` chain specifically because it's the fewest-hops close, not a three-level chain. `test-executions`' "By test case" arm and `test-cycles`/`entry-exit-criteria`/`risk-items`'s own selectors declare none — confirmed live, before any code change, that they already worked (the one-hop `project_id`-threading fix from years earlier already covered them). No third level is built; every `via` target is itself one-hop, asserted directly rather than assumed. A "Change `<via>`" affordance returns to the via step; switching arms in a multi-option selector clears a stale `via` pick too. **Found, flagged, not fixed in this pass**: `ScopeSelector` never passes `labelField` to its own `FkAutocomplete`, so every picker (including the three untouched here) shows a raw UUID rather than the friendly label already declared elsewhere on the same FK's `field_meta` — cosmetic, not a CRUD blocker, a distinct contained follow-up. **No new route, no new permission code, no migration.** FR-ADMIN-13, NFR-84, WBS 11.63 | Accepted |
| [0082](0082-role-assignment-generic-admin-list.md) | **Drafted as `0073`, renumbered same day** — collided with `origin/main`'s PR #69 squash-merge (this table's own `0073`-`0081` rows above, "generic entity detail page + relationship tabs"), caught via a cross-session coordination message (this file's own numbering-collision recipe, applied to a peer live session rather than a `git fetch`). RoleAssignment's `_ROLE_ASSIGNMENT_CONFIG` gains a real generic `list` (`GET /role-assignments?org_id=...`, flat, factory-registered — separate route from RBAC-3's bespoke nested `GET /orgs/{org_id}/role-assignments`, which `RoleAssignmentsPanel.tsx` keeps using unchanged). Closes two independent gaps in one pass: the generic admin surface's "Role assignments" list page (previously "served schema does not include the list method"), and MCP's `tn_role_assignment_list` (previously absent — no way for an `AIAgent` caller to discover its own/another actor's `RoleAssignment`s, blocking RBAC bootstrap over MCP). The original `_ROLE_ASSIGNMENT_CONFIG` exclusion reasoning (bespoke route returns a bare array) was stale — [ADR-0041](0041-ds-2-table-container-shared-pagination.md) already fixed that envelope shape weeks earlier; nobody revisited `methods` when it shipped. No RBAC migration (`role_assignment.read` already gates both routes), no schema change. MCP tool count moves with ADR-0073..0081's own count (147 → 157 across those nine ADRs) by one more, to **158** — `test_generated_tool_count_is_the_number_adr_0067_states` updated in the same merge to include both deltas; [ADR-0068](0068-mcp-6-per-entity-mcp-tools.md) gains a forward-pointer rather than an in-place edit | Accepted |
| [0083](0083-aiagent-permission-inheritance.md) | `has_permission`/`has_permission_in_any_org` (`app/core/rbac.py`) now take the actor object and, for an `AIAgent`, **fall back** to its `acting_on_behalf_of_user_id`'s `RoleAssignment` grants — but only when the agent holds NO `RoleAssignment` of its own at all (existence check, not a per-check union) — closing a real bootstrap gap this session hit directly (an MCP key with zero grants of its own had no way to act, even though the human it was minted for held everything needed). `has_permission` additionally requires the behalf-user to hold an *active* `OrgMembership` in the org being checked; `has_permission_in_any_org` does not (no single org to check against, and its `User` path was never gated on one either). **Same-day amendment:** the first draft unconditionally UNION'd the behalf-user's grants into every check, which broke a pre-existing test (TC-RBAC-011) proving an agent with a narrower grant of its own stays isolated from its behalf-user's broader one — fixed by gating the fallback on the agent having zero grants *at all* in scope, not on whether this specific check happens to miss. All 13 call sites updated to pass the actor object instead of `str(actor.actor_id)` — mechanical, no permission code or scope-resolution logic touched. No schema/migration/RBAC-catalog change. Backend unit 906/906 unchanged; 3 new integration tests plus TC-RBAC-011 all green against a live isolated stack | Accepted |
| [0084](0084-test-cycle-project-scope.md) | `TestCycle` gains a real, denormalized `project_id` (NOT NULL, backfilled from `test_plan.project_id` in the same migration) and a branching `scope_field` (`test_plan_id` \| `project_id`, ADR-0075 Amendment 1's `branching_resolver` mechanism reused verbatim) — closes the "can only be shown through a TestPlan filter" gap by giving it the same direct-project-scoped generic-admin list every other project-scoped entity already has, with no frontend change (`derive_entity_relations` gives `Project` a sixth tab, "Test cycles," for free). Denormalization chosen over widening `crud_factory` to support a join-scoped `scope_field` (CTO's explicit pick from a plan-with-open-questions pass) — smaller, more localized, accepts a second-source-of-truth tradeoff nothing in this codebase's own create paths can actually cause to drift. 14 direct-ORM `TestCycle(...)` construction sites across `backend/tests/`/`e2e/tests/` needed a one-line fix for the new NOT NULL column; 3 pre-existing relation-completeness tests updated for the new counts (31→32 relations, `CLASSIFICATION` gains an `open`-bucket row). No RBAC/permission-code change. Backend unit 906/906, full integration suite green against a live migrated isolated stack (backfill + create-then-list-both-ways + the new 422 boundary all verified directly), frontend `tsc` clean + Vitest 98 files/788 tests green. **Found broken live the same day** — see [ADR-0085](0085-entity-scope-array-fast-path-fix.md), `useEntityScope.ts`'s fast path never recognized `project_id` as an arm of a branching `scopeField` array | Accepted |
| [0085](0085-entity-scope-array-fast-path-fix.md) | `useEntityScope.ts`'s ADR-0060 "shape B" fast path checked `config.scopeField === "project_id"`, a strict string comparison — always false for [ADR-0084](0084-test-cycle-project-scope.md)'s new branching-array `scopeField`, so `/projects/:projectId/admin/test-cycles` fell through to a manual "By test plan / By project" picker instead of resolving immediately, found live in the CTO's own click-through minutes after that ADR merged. Fix: reuse `scopeArmsOf` (`EntityRelationTab.tsx`, already built and exported for this exact array-vs-string gap, ADR-0078) instead of a second hand-rolled comparison. Verified against the real running `main` stack (locally-minted token via the `/auth/refresh`-mock technique, no credential write) — screenshot before/after. 2 new Vitest cases pin the regression; full suite 98 files/790 tests green, `tsc` clean. No backend/schema change | Accepted |
| [0086](0086-child-compound-create-parent-picker.md) | The project-scoped Test Cycles admin list (ADR-0084's `project_id` arm) had no "New" button — `child_compound_creates`' bespoke route still only understood `test_plan_id`, and `EntityListPage` had never supported a picker for the case where the far field differs from the current scope field (`EntityRelationTab.tsx`'s own docstring already named this "not yet supported" for the standalone-list case). Adds a second `CompoundCreateAction` (`far_field="project_id"`, same bespoke route, `parent_entity="test-plan"`), a real parent picker in `EntityListPage`'s create modal (reusing `compoundParentField`/`pickerScopeParams`), and gives `_TEST_CYCLE_CONFIG` a `create_schema` (`CreateTestCycleRequest`) purely so `release_id`/`environment_id`/`name` derive writable+required — `"create" not in methods` is what actually keeps the generic route unregistered, unchanged. **Two live-only defects found and fixed the same pass**: the first draft's `lockedValues` left `project_id` an unlocked, empty picker in the form (fixed by locking both the picked parent and the list's own scope field); `EntityForm`'s `fk` fields never threaded `routeParams`/`extraParams` to `FkAutocomplete`/`FkSelect` at all — invisible until this ADR gave `TestCycle` its first-ever editable `fk` fields needing route/query scoping (`release_id`/`environment_id`), fixed by adding `fkRouteParams`/`fkExtraParams` to `EntityForm`. Verified end-to-end against the real running `main` stack: picked a Test Plan → Release → Environment → Name → submitted → row appeared, zero errors. No new route, no permission code, no migration. Backend unit 915/915, frontend `tsc` clean + Vitest 793/793. **Amendment (2026-09-17)**: the parent picker itself is `FkSelect` (a dropdown), not `FkAutocomplete` — live-manual-test feedback right after merge; see [ADR-0087](0087-fk-select-for-bounded-catalogs.md) for the generalized mechanism this became | Accepted |
| [0087](0087-fk-select-for-bounded-catalogs.md) | Generalizes ADR-0086's Amendment (`FkSelect` for the Test Plan parent picker) into a declared per-option flag rather than a hardcoded component swap, after "apply this to all type to search boxes" turned out to be unsafe applied literally — `FkSelect` fetches at most 100 rows with no search, correct only for a genuinely small, bounded catalog. Adds `ScopeSelectorOption.select`/`CompoundCreateAction.parent_select` (both default `False`), set `True` on 9 scope-selector declarations + 1 compound-create parent whose ref entity is `test-plan`/`test-suite`/`test-cycle` (bounded); left `False` on 9 others whose ref entity is `requirement`/`test-case`/`defect`/`test-execution`/`test-condition`/`project` (unbounded) — stated explicitly, not silently skipped. `TestPlanDetail`'s own bespoke Release/Environment pickers (no per-declaration config to hang a flag off) are hardcoded `FkSelect` directly, same call as `EntityForm`'s own release_id/environment_id fields. No route/permission/schema change. Backend unit 915/915, frontend `tsc` clean + Vitest 793/793, verified live (served schema's `scopeSelector[0].select: true`; `TestPlanDetail`'s Create Cycle modal renders both fields as native `<select>`s) | Accepted |
| [0088](0088-picker-display-name-never-raw-id.md) | Fixes the exact "select with id, don't select with name" complaint the ADR-0087 dropdown rollout surfaced: `Defect`/`TestExecution` pickers had no always-present, distinguishing label column — `Defect.external_ref` is optional (falls back to the raw UUID when unset), `TestExecution.result` is a 4-value enum (every failed run of one `TestCase` reads identically as "fail"). Adds a Pydantic `@computed_field` `display_name` to both `DefectSummary`/`TestExecutionSummary` (`external_ref` when set, else `"{severity} defect ({created_at:%Y-%m-%d})"`; `"{result} — {executed_at:%Y-%m-%d %H:%M}"`), and repoints every `field_meta.label_field` at `defect_id`/`test_execution_id` to it. A computed field isn't a `model_fields` entry, so `_to_summary`'s generic getattr loop needs no change; `labelFor()` already reads whatever key `label_field` names off the plain JSON response. Verified server-side (zero live rows to click through): both fallback and preferred label shapes compute correctly, served schema now carries `"labelField": "display_name"`. Backend unit 915/915, frontend `tsc` clean + Vitest 793/793, both unchanged | Accepted |
| [0089](0089-scope-selector-label-field.md) | The actual fix the CTO's live test needed, ADR-0088 alone wasn't it: `ScopeSelectorOption` never had a `label_field` at all — `ScopeSelector.tsx` never passed *any* `labelField` to its own `FkAutocomplete`/`FkSelect`, so every scope-selector picker in the app (not just `test-conditions`) rendered the raw id regardless of how correct the referenced entity's own display column was. Flagged and left unfixed in ADR-0081's own Consequences ("cosmetic... a distinct contained follow-up"); ADR-0087's dropdown rollout is what made it impossible to miss. Adds `label_field`/`labelField` to the dataclass, its serialization, and the TS type; threads it to both the outer option's and its `via`'s own picker; sets it on all 26 existing declarations, matching each `ref_entity`'s already-established `label_field` elsewhere. Verified live: `test-conditions`' picker now shows the requirement's real description text, not its id. Backend unit 915/915, frontend `tsc` clean + Vitest 795/795 (2 new regression tests) | Accepted |
| [0090](0090-agent-org-self-discovery.md) | An `AIAgent` bearer session had no way to discover which org(s) it may act within — every MCP list tool requires `org_id`/`project_id` up front (NFR-1), and `GET /auth/me/orgs` (ADR-0036) explicitly 403s any `AIAgent` caller. New `GET /agents/me/orgs` — the `AIAgent`-only mirror of that route, same response shape and `_active_orgs_for_user` query, gates inverted — deliberately outside `/auth/*` so ADR-0065 Decision §2's exclusion stands unchanged. New MCP pseudo-resource `agent_org: {"list": ...}` generates `tn_agent_org_list`, no arguments. Registry 31→32 resources, 158→159 tools. No schema/migration/permission change. Backend unit 915/915 unchanged, new integration file 2/2 (agent 200 active-only, human 403 no-leak, unauthenticated 401, zero-active 200 empty, MCP round-trip byte-identical to REST), `test_shell6_me_orgs.py` 3/3 unchanged, full suite 1328/1338 (10 pre-existing failures confirmed structurally unrelated) | Accepted |
| [0091](0091-actor-membership-gate-fix-bespoke-routes.md) | Found immediately after using ADR-0090's own new tool: `tn_test_suite_test_case_create` `404`'d as an `AIAgent` against a real, `GET`-able suite. Root cause: seven bespoke routes across 5 modules (`test_suite_membership.py`, `test_plan_membership.py`, `projects.py::create_project`, `org_memberships.py::invite_member`, `role_assignments.py::create_role_assignment`, `releases.py::create_release`, `test_cycle_creation.py::create_test_cycle_for_plan`, plus REST-only `roles.py`/`rbac_routes.py`) never adopted `_actor_membership_exists` — each still gated on the plain, `AIAgent`-blind `_org_membership_exists(db, org_id, actor.actor_id)`, 404-ing every agent caller regardless of real membership. All swapped to the canonical helper; 4 modules' own now-dead local copy deleted outright. `create_project` was completely unusable by any `AIAgent` until this fix. No schema/migration/permission change. New integration test 3/3 (project create, suite membership, plan-suite membership, each proven previously-404, now `201`, for a real agent). Backend unit 915/915, full suite 1331/1341 (10 pre-existing failures, identical set, re-confirmed unrelated) | Accepted |
| [0092](0092-relation-tab-sort-filter-columns.md) | CTO report: a relationship tab's table (e.g. `test-suites/{id}?tab=test-suite-test-cases`) should have the same CRUD-entity capability a standalone admin list page has — sort, Filter, Columns. `EntityRelationTab` already rendered the real `EntityTable`, not a hand-rolled one; the gap was `EntityTable`'s `bare` prop returning early with no `Card.Header` at all (where Columns/Filter/search live), plus `EntityRelationTab` never wiring `onSortChange`/`filters` despite the click-to-sort logic already running inside `bare` mode. Fix: extracted the Columns/Filter/search controls + both modals into values shared by both the `bare` and non-`bare` render branches (still no `title`/`Card` in `bare` mode); added local `sort`/`filters` state to `EntityRelationTab` (reset when `relation.entity` changes, since the component isn't remounted on tab switch), threaded into the list query and `EntityTable`'s props. No backend/schema change — `sortable`/`filterable`/`filterFields` were already served. **Explicit exclusion, named not silently deferred**: operates on the currently-rendered (link-entity, for many-to-many) fields, not the far entity's richer field set — merging those is a separate, larger change. New Vitest file, 3/3. `tsc` clean, full suite 798/798 (795 + 3 new) | Accepted |

**Deciders on all ADRs below:** xuanbinh91@gmail.com (CTO), unless noted otherwise.
**Date:** 2026-09-03 (0001–0017, 0021), 2026-09-04 (0018–0020), 2026-09-05 (0022–0027, 0029), 2026-09-06 (0028, 0030, 0031, 0032, 0033), 2026-09-07 (0034, 0035, 0036, 0037, 0038, 0039, 0040, 0041), 2026-09-08 (0042, 0043, 0044, 0045, 0046 — drafted as `0044`, renumbered 2026-09-09), 2026-09-09 (0047, 0048, 0049, 0050 — drafted as `0048`, then `0049`, renumbered twice same day — 0051, 0052), 2026-09-10 (0053, 0054), 2026-09-09 docs/2026-09-11 renumbered (0055 — drafted as `0053`), 2026-09-11 (0056, 0057), 2026-09-12 (0058, 0059, 0060, 0061, 0062), 2026-09-13 (0063), 2026-09-14 (0064 — drafted as `0063`, renumbered same day, MCP-4 collision; 0065), 2026-09-13 (0066), 2026-09-14 (0067 — drafted as `0066`, renumbered same day, ADMIN-5 collision; 0068 — drafted as `0067`, renumbered same day, TestType-seed collision), 2026-09-14→15 (0069 — drafted as `0068`, renumbered 2026-09-15, MCP-6 collision), 2026-09-14 (0070), 2026-09-15 (0071, 0072, 0073, 0074, 0075, 0076 — the last four drafted as `0070`–`0073`, renumbered 2026-09-15, PR #70 collision), 2026-09-16 (0077, 0078, 0079, 0080, 0081 — per PR #69's own row content above; 0082 — drafted as `0073`, renumbered same day, collided with PR #69/ADR-0073..0081), 2026-09-17 (0083, 0084, 0085, 0086, 0087), 2026-09-18 (0088, 0089, 0090, 0091, 0092)
