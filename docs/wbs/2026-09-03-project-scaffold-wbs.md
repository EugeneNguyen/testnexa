# Work Breakdown Structure — Project Scaffold

**Date:** 2026-09-03
**Owner:** xuanbinh91@gmail.com (CTO)
**Sources:** [Requirements Document](../requirements/2026-09-03-project-scaffold-requirements.md), [Scaffold design spec](../superpowers/specs/2026-09-03-project-scaffold-design.md), [Pre-implementation plan](../superpowers/plans/2026-09-03-project-scaffold-plan.md)

Sizing: **S** ≤ 0.5 day, **M** ≈ 1–2 days, **L** ≈ 3–5 days, for one engineer working with an AI pair — indicative for sequencing, not a committed estimate.

---

## 1. Backend foundation

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 1.1 | DB models — all 28 business entities (36 physical tables incl. `RefreshToken`/`LoginAttempt` and junction tables — see Database Document §1) across 11 cluster modules (`tenancy`, `auth`, `rbac`, `actor`, `project`, `assets`, `planning`, `execution`, `trace`, `taxonomy`, `governance`) | — | L | [Database Document](../database/2026-09-03-database-design.md) |
| 1.2 | Alembic initial migration (all 36 physical tables, UUIDv7 PKs) | 1.1 | M | ADR-0008 |
| 1.3 | Alembic seed migration — partial unique index on `role.name WHERE org_id IS NULL`; ~100-row `Permission` catalog (29 resources); 5 system `Role` rows + bundles (`org_admin` = full catalog); existence-checked inserts for idempotent re-run; taxonomy lookups — excludes `ai_agent.create`/`.update`, seeded separately by 2.3c ahead of this task | 1.2 | M | FR-RBAC-4, NFR-20, FR-ADMIN-1, [RBAC-4 plan](../superpowers/plans/2026-09-03-rbac-4-seeded-system-roles-plan.md) |
| 1.4 | Core infra: DB session management, settings/config, Actor-resolution helper | 1.1 | S | ADR-0002 |

## 2. Auth & RBAC

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 2.1 | `security.py` — JWT issue/verify, argon2 password hashing | 1.4 | S | FR-AUTH-1 |
| 2.2 | Local login route, org auto-select/picker/403-zero-org resolution | 2.1, 1.2 | M | FR-AUTH-1 |
| 2.2b | `POST /auth/refresh` (rotate-on-use, org re-check), `GET /auth/me`, minimal `get_current_actor` (bearer JWT verify only, no permission codes) | 2.2 | M | FR-AUTH-2, NFR-12, NFR-13, [ADR-0013](../adr/0013-refresh-token-rotation-policy.md) |
| 2.2c | `POST /auth/logout` — authenticated, idempotent revoke-current-session (CAS scoped to `user_id`, `revoked_reason="logout"`), `204`, clears refresh cookie | 2.2b | S | FR-AUTH-3, NFR-14, [ADR-0014](../adr/0014-logout-session-revocation-policy.md) |
| 2.3a | `security.py` — `generate_api_key`/`hash_api_key`/`verify_api_key` (`tnx_agent_<prefix>_<secret>` format, argon2) | 2.1 | S | FR-AUTH-4, [ADR-0015](../adr/0015-ai-agent-credential-mechanics.md) |
| 2.3b | `get_current_actor` agent-key branch (prefix-narrowed lookup + argon2 verify, `AIAgent.last_used_at` update) + `AIAgent.last_used_at` migration | 2.3a | M | FR-AUTH-4, NFR-18 |
| 2.3c | `rbac.py` — minimal generic `has_permission`/`require_permission` (org-wide `RoleAssignment` resolution only; pulled forward from 2.4, project-scoped branch left for RBAC-3) + `ai_agent.create`/`ai_agent.update` permission-seed data migration | 1.3, 2.3b | M | FR-AUTH-4 AC2, [ADR-0015](../adr/0015-ai-agent-credential-mechanics.md) |
| 2.3d | `POST /orgs/{org_id}/agents` + `.../revoke` routes (human-only gate, org_admin-permission-gated, 404-vs-403 org-membership boundary) | 2.3c | M | FR-AUTH-4, NFR-17, NFR-19 |
| 2.4a | `GET /auth/me/orgs` — human-only, active-`OrgMembership`-only, reuses `OrgSummary` verbatim, no new schema | 2.2b | XS | FR-AUTH-5, NFR-48, [ADR-0036](../adr/0036-shell-6-organization-switcher-header-dropdown.md) |
| 2.4 | `rbac.py` — `require_permission(code)` project-scoped branch + permission-code registry generated from model registry (extends 2.3c's org-wide-only implementation) | 1.3, 2.3c | M | FR-RBAC-3, NFR-10 |
| 2.5a | `has_permission_in_any_org(actor_id, code)` — `core/rbac.py` sibling to `has_permission`, no `org_id` filter (no target org exists yet pre-creation); gates 2.5c only | 2.3c | S | FR-RBAC-1, [ADR-0016](../adr/0016-organization-bootstrap-creation-flow.md) |
| 2.5b | `POST /auth/signup` — public, bootstrap-only (closes once ≥1 `Organization` exists, `pg_advisory_xact_lock`-serialized against concurrent first signups); creates User+Organization+OrgMembership(active)+org_admin RoleAssignment (RBAC-4's seeded Role, not a per-org copy), issues tokens via 2.2's existing cookie/token code | 2.2, 1.3 | M | FR-RBAC-1, NFR-21, [ADR-0016](../adr/0016-organization-bootstrap-creation-flow.md) |
| 2.5c | `POST /orgs` — authenticated, `has_permission_in_any_org("organization.create")` gate (no 404-vs-403 boundary — no target org to hide); creates a second Organization + creator's own OrgMembership/org_admin RoleAssignment in it | 2.5a, 1.3 | S | FR-RBAC-1, [ADR-0016](../adr/0016-organization-bootstrap-creation-flow.md) |
| 2.5d | OrgMembership routes — invite/suspend/reactivate members | 2.5c | M | FR-RBAC-2 |
| 2.6a | `app/schemas/rbac.py` + `app/api/routes/role_assignments.py` — `POST`/`GET /orgs/{org_id}/role-assignments` (bespoke, org-path-scoped, `require_permission`/404-vs-403 boundary reused; cross-org `role_id`/`project_id` → 422; `User` actor requires an existing `OrgMembership`, any status → 422 if absent) | 2.4, 3.0a | S | FR-RBAC-3, NFR-29, NFR-30, [ADR-0021](../adr/0021-role-assignment-creation-flow.md) |
| 2.6b | Fix `app/api/routes/projects.py` — `get_project`/`update_project` pass `project_id` into `has_permission`, so a project-scoped grant satisfies `project.read`/`.update` on its own project (closes the gap AUTH-4/PROJ-1 left open) | 3.0b | S | FR-RBAC-3, NFR-30, [ADR-0021](../adr/0021-role-assignment-creation-flow.md) |
| 2.7 | Human-only Approval defense-in-depth check | 2.4 | S | FR-RBAC-5 |
| 2.8 | Login throttle: `LoginAttempt` table + per-(IP, email) failed-attempt counter, 429 above threshold | 2.2 | S | NFR-11, [ADR-0011](../adr/0011-login-rate-limiting.md) |
| 2.9 | RBAC seed data migration — extend `test_manager` bundle with `release.create`/`.read`/`.update` (existence-checked insert, same idempotent pattern as 2.3c's) | 1.3 | S | FR-PROJ-2, [ADR-0019](../adr/0019-release-creation-flow.md) |
| 2.10 | RBAC seed data migration — extend `test_manager` bundle with `test_condition.create`/`.update`/`.delete` and `test_case.create`/`.update`/`.delete` (`.read` on both already held; same idempotent pattern as 2.9's) | 1.3 | S | FR-REQ-3, [ADR-0028](../adr/0028-req3-test-condition-rigor-path-bespoke-routes.md) |

## 3. Generic CRUD API

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 3.0a | `app/schemas/projects.py` + `app/api/routes/projects.py` — `POST /orgs/{org_id}/projects` (bespoke, org-path-scoped, `require_permission`/404-vs-403 boundary reused as-is; `standards_profile` inherits `Organization.default_standards_profile` when omitted; creator auto-assigned project-scoped `test_manager` RoleAssignment) | 2.4, 1.2 | M | FR-PROJ-1, NFR-22, NFR-23, [ADR-0017](../adr/0017-project-creation-flow.md) |
| 3.0b | `GET`/`PATCH /projects/{id}` — org resolved from the fetched row (no `org_id` path segment), anticipating the generic factory's eventual item-route shape | 3.0a | S | FR-PROJ-1, [ADR-0017](../adr/0017-project-creation-flow.md) |
| 3.0c | `app/schemas/releases.py` + `app/api/routes/releases.py` — `POST`/`GET /projects/{project_id}/releases` (bespoke, project-path-scoped, `has_permission`-called-directly + 404-vs-403 boundary, same posture as 3.0b); list sorted by `target_date`, `NULLS LAST` pinned both directions | 3.0b, 2.9 | M | FR-PROJ-2, NFR-25, [ADR-0019](../adr/0019-release-creation-flow.md) |
| 3.0d | `GET /releases/{id}` + `GET /releases/{id}/test-cycles` — row-resolved, no path `project_id`; the cycles query nests each cycle's `TestExecution`s, gated on `release.read` AND `test_cycle.read` AND `test_execution.read` | 3.0c | M | FR-PROJ-2, NFR-26, [ADR-0019](../adr/0019-release-creation-flow.md) |
| 3.1 | Pydantic v2 schemas — 1:1 mirror of `app/models/`, one file per model cluster (`assets`, `planning`, `taxonomy`, `governance`, `rbac`) | 1.1 | M | [API Document](../api/2026-09-03-api-design.md) |
| 3.2 | `app/api/crud_factory.py` — `CrudEntityConfig` dataclass + `chain_resolver()` (per-entity `org_id`-resolution, direct/one-hop/branching/multi-hop) + `make_crud_router()` (list/get/create/update/delete, pagination, exact-match `filter_fields` + opt-in `?q=` `search_fields`, item routes gated via row-resolved `has_permission` not `require_permission`, global-catalog routes via `has_permission_in_any_org`, `DELETE`'s `IntegrityError` → `409`) | 2.4, 2.5a, 3.1 | L | FR-ADMIN-2, NFR-6, NFR-29, NFR-30, [ADR-0022](../adr/0022-generic-crud-router-factory.md) |
| 3.3 | Apply factory to all 20 entities the API Document §3 lists, across 6 route modules (`assets`, `planning`, `taxonomy`, `governance`, `execution` (Defect only), `rbac_routes`) + extend `organizations.py`/`projects.py`/`org_memberships.py` with the factory-served methods each is still missing (`Project`: `DELETE` only; `Organization`: `GET`/`PATCH`/`DELETE`; `OrgMembership`: full 5) | 3.2 | L | FR-ADMIN-2, [ADR-0022](../adr/0022-generic-crud-router-factory.md), [ADMIN-2 plan](../superpowers/plans/2026-09-05-admin-2-generic-crud-factory-plan.md) |
| 3.4 | Wire the 3 entities the factory never registered a route for: `TestExecution` (full CRUD, `execution.py`, resolver via `TestCycle`), `TestLog` (list/get only, same file, resolver delegates one further hop through `TestExecution`'s), new `app/api/routes/trace.py` + `schemas/trace.py` for the 4 link tables (list/get only, resolvers compose existing `chain_resolver`/`resolve_via_test_case` helpers, no new resolver logic) | 3.3 | M | FR-ADMIN-2, [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md) |
| 3.5 | `GET /orgs/{org_id}/permissions/mine` — bulk-resolves the caller's own permission codes in `org_id` (`RoleAssignment`→`Role`→`RolePermission`→`Permission.code`, one query, reusing `has_permission`'s join shape) | 2.5a | S | FR-ADMIN-2, NFR-37, [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md) |

## 4. Bespoke API routes

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 4.0 | `Requirement.title` migration + schema/factory-config update (`search_fields`) — the one gap between FR-REQ-1 and the schema, per [ADR-0025](../adr/0025-requirement-title-field.md); `Requirement`'s create/list/update/delete/search were already delivered by 3.3 (ADMIN-2) | 3.3 | S | FR-REQ-1, [ADR-0025](../adr/0025-requirement-title-field.md) |
| 4.1a | Bespoke `POST`/`GET /requirements/{id}/test-cases` — direct-link path, atomic `TestCase` + `RequirementTestCaseLink` create + requirement-scoped list, plus a resolver gap-fill ([ADR-0029](../adr/0029-testcase-resolver-direct-link-fallback.md)) | 3.2, 4.0 | M | FR-REQ-2 |
| 4.1b | Bespoke `POST /requirements/{id}/test-conditions` + `POST /test-conditions/{id}/test-cases` — atomic `TestCondition`/`TestCase` + link-table creates; restrict `_TEST_CONDITION_CONFIG`'s generic `create` (ADR-0022) now redundant and link-less; `test_manager` RBAC seed migration extending `test_condition.*`/`test_case.create`/`.update`/`.delete` | 3.2, 4.0 | M | FR-REQ-3, [ADR-0028](../adr/0028-req3-test-condition-rigor-path-bespoke-routes.md) |
*4.1a/4.1b correction:* the original single 4.1 row also named `TestStep` as needing new routes here — stale by the time either sub-item was scoped: `TestStep`'s full create/list/update/delete already shipped generically via 3.3 (ADMIN-2's factory, `CreateTestStepRequest` registered from the start), no bespoke work needed for it under FR-REQ-2..4. Both 4.1a and 4.1b are now delivered, coexisting per-TestCase within a project (ADR-0006).

| 4.1c | `app/api/routes/test_suite_membership.py` — `POST`/`DELETE /test-suites/{id}/test-cases/{case_id}` (join row add/remove) + new `GET /test-suites/{id}/test-cases` (live membership list, not in the original API Document); cross-project add → `422`, duplicate add → `409`; no RBAC bundle change needed (`test_manager`/`tester` already hold `test_suite.*`/`.read`) | 3.2, 4.0 | S | FR-REQ-4, NFR-38, NFR-39, [ADR-0030](../adr/0030-req4-test-suite-membership-bespoke-routes.md) |

| 4.2 | `app/api/routes/test_cycle_creation.py` — `POST /test-plans/{id}/test-cycles` (cross-project `422` on `release_id`/`environment_id`) + `app/api/routes/execution_authoring.py` — `POST /test-cycles/{id}/executions` (execution-scope-check: execution only against a TestCase in a suite included in the plan, reuses 4.2a's coverage-query join); restrict `_TEST_EXECUTION_CONFIG`'s generic `create` (now redundant and scope-check-less); `test_manager` RBAC seed migration extending `environment.*`/`test_execution.create`+`.read` | 3.2, 4.2a | M | FR-PLAN-3, FR-EXEC-1, [ADR-0033](../adr/0033-plan3-test-cycle-creation-and-execution-scope-check.md) |
*4.2 correction (PLAN-1 documentation pass, 2026-09-06):* the original row named `TestPlan`/`EntryExitCriteria`/`TestCycle`/`Environment` routes here — stale by the time PLAN-1 was scoped, same class of correction 4.1a/4.1b made to the original 4.1 row above. All four already ship full (or, for `TestCycle`, `create`-excluded) generic CRUD via 3.3 (ADMIN-2's factory, `planning.py`) — no bespoke work needed for any of the four's own entity CRUD. What remains under this row is FR-PLAN-3's own execution-scope-check (below, 4.2 retained for that) — FR-PLAN-1's own gap is now 4.2a, FR-PLAN-2's `EntryExitCriteria` needed no bespoke *route* work at all (plain generic CRUD, already covered by 3.3); its own remaining gap is visibility-surface only, now 4.2b below.
*4.2 further correction (PLAN-3 documentation pass, 2026-09-06):* the row above ("execution-scope-check") is now delivered, plus `TestCycle`'s own `create` gap (also stale-scoped to 4.2 originally, never broken out into its own letter) — both are now the same row's content, since `POST /test-cycles/{id}/executions` and `TestCycle`'s own create are one story's (FR-PLAN-3) two bespoke routes, [ADR-0033](../adr/0033-plan3-test-cycle-creation-and-execution-scope-check.md).

| 4.2a | `app/api/routes/test_plan_membership.py` — `POST`/`DELETE /test-plans/{id}/test-suites/{suite_id}` (join row add/remove) + `GET /test-plans/{id}/test-suites` (live-list) + new `GET /test-plans/{id}/test-cases` (coverage query, two-hop `TestPlan`→`TestSuite`→`TestCase`, deduplicated) + `status`-transition legality guard on the generic `PATCH /test-plans/{id}` (`draft→approved`/`approved→superseded` only, else `409`); cross-project include → `422`, duplicate include → `409`; no RBAC bundle change needed (`test_manager` already holds `test_plan.*`/`test_suite.*`) | 3.2, 4.0 | S | FR-PLAN-1, NFR-40, NFR-41, [ADR-0031](../adr/0031-plan1-test-plan-membership-and-status-transition-routes.md) |
| 4.2b | `TestCycleSummary` (`app/schemas/releases.py`) gains a nested `exit_criteria` field; `GET /releases/{id}/test-cycles` (`app/api/routes/releases.py`) batch-queries `EntryExitCriteria` by the result set's distinct `test_plan_id`s (`type = exit` only), attaches per cycle; permission gate extends from triple to quadruple (`entry_exit_criteria.read` added); no migration, no RBAC bundle change needed | 3.3 | S | FR-PLAN-2, NFR-42, [ADR-0032](../adr/0032-plan2-entry-exit-criteria-visibility.md) |
| 4.3 | TestExecution + append-only TestLog routes | 4.2 | M | FR-EXEC-1..2 |
*4.3 correction (EXEC-2 documentation pass, 2026-09-07):* this row's `TestExecution` half (FR-EXEC-1) was already delivered by 4.2/ADR-0033 (the create route) + EXEC-1's own frontend pass (ADR-0034) — same stale-scope-split class 4.1a/4.1b/4.2's own corrections already established. What this row's *append-only TestLog* half (FR-EXEC-2) actually needed — nothing wrote to `TestLog` before this pass, despite the table/read-routes existing since ADR-0027 — is now delivered: a new generic `post_update_hook` on `CrudEntityConfig` (status-change log on `PATCH`), a log-on-create side effect on `POST /test-cycles/{id}/executions`, and two new bespoke routes (`POST /executions/{id}/comments`, `GET /executions/{id}/logs`), [ADR-0038](../adr/0038-exec-2-append-only-test-log.md).
| 4.4 | Defect routes, raise-from-execution | 4.3 | S | FR-EXEC-3 |
| 4.5 | Approval route (human-only, GOV-1) | 2.7 | S | FR-GOV-1 |
| 4.6 | RiskItem, Attachment routes (incl. server-side size/type limits, storage backend switch) | 3.2 | M | FR-GOV-2..3, NFR-4, NFR-7 |
| 4.7 | RTM traversal route + CSV export | 4.1a, 4.1b, 4.3, 4.4 | M | FR-TRACE-1..2 |

## 5. MCP server stub

| # | Deliverable | Depends on | Size | Status | Maps to |
|---|---|---|---|---|---|
| 5.1 | MCP server scaffolding, thin client over the same service layer as REST (no separate weaker code path) — streamable-HTTP transport, ASGI sub-app mount at `/mcp`, FastMCP(`name="TestNexa-MCP"`, `stateless_http=True`, `json_response=True`); per-tool `Authorization` resolution reusing `app/core/rbac._resolve_agent_actor`; FastAPI lifespan bridge for the MCP SDK's `session_manager` | 2.3 | M | Done | FR-MCP-1..3, NFR-43, [ADR-0033](../adr/0033-mcp-server-architecture.md) |
| 5.2 | MCP-1 tools: `create_test_case`, `list_test_cases` (REQ-2's direct-link path) — direct-call dispatch into the existing `assets.py` route handlers; `_actor_membership_exists` helper on the route's NFR-1 boundary so AIAgent callers resolve via `acting_on_behalf_of_user_id` (otherwise every MCP-originated request 404s); integration tests in `tests/integration/test_mcp_test_cases.py` (TC-MCP-001/002/003 + cross-project 403 negative + no-auth 401 negative) | 5.1, 4.1a, 4.1b | S | Done | FR-MCP-1, NFR-43, [ADR-0033](../adr/0033-mcp-server-architecture.md) |
| 5.2a | MCP-1 `create_test_case` extended to also dispatch the REQ-3 rigor path (`POST /test-conditions/{id}/test-cases`) — same `test_case.create` permission, same `created_by_actor_id` stamping, parameter disambiguated by a `test_condition_id` field | 5.2 | XS | Backlog | FR-MCP-1 AC2 |
| 5.3 | MCP-2 tools: `update_test_case` (`PATCH /test-cases/{id}`); MCP-3 tools: `create_test_execution`, `read_requirement` (read-only — no Requirement write/approval/membership tools in this scaffold, matches 26's MVP-scoped MCP surface) | 5.1, 4.1a, 4.1b, 4.3 | S | Backlog | FR-MCP-2, FR-MCP-3, NFR-43, [ADR-0033](../adr/0033-mcp-server-architecture.md) |

## 6. Frontend foundation

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 6.1 | Vite + React Router + TanStack Query + Tailwind scaffold | — | S | ADR-0009 |
| 6.2 | Typed API client + TanStack Query hooks (`useEntityList`, `useEntity`, `useCreateEntity`, `useUpdateEntity`, `useDeleteEntity`) | 6.1, 3.2 | M | [API Document](../api/2026-09-03-api-design.md) |
| 6.3a | Token store module (`apiFetch` reads/attaches `Authorization`, 401→refresh→retry-once with concurrent-refresh dedup) | 6.2, 2.2b | M | FR-AUTH-2 |
| 6.3b | `AuthContext` boot-time silent refresh (`isInitializing`), `ProtectedRoute` guard | 6.3a | S | FR-AUTH-2 |
| 6.3c | Logout action (calls `/auth/logout`, unconditionally clears token store + org state and redirects to `/login` regardless of the call's success/failure) | 6.3a, 2.2c | S | FR-AUTH-3, NFR-14 |
| 6.3d | ~~`AppHeader` navbar (CoreUI `CHeader`, brand + "Log out" button wired to 6.3c) mounted inside `ProtectedRoute`, replacing the bare unwrapped `children` render~~ **Revised 2026-09-08 (6.12/[ADR-0042](../adr/0042-adminlte-design-system.md)):** raw `nav.app-header.navbar.navbar-expand` markup replaces `CHeader` (raw HTML since ADR-0037, reclassed onto AdminLTE per ADR-0042); brand + "Log out" wiring, mount point, and `ProtectedRoute` integration unchanged | 6.3c, 6.3b | S | FR-AUTH-3, ADR-0012, [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 6.4 | `AppSidebar` (CoreUI `CSidebar`/`CSidebarNav`, single nav-item array — org-home + org-members, `NavLink`-driven active state) + `AppShell` (sidebar + header + content, owns toggle-visibility state per CoreUI's own template pattern) wraps `ProtectedRoute`'s children, replacing 6.3d's bare `<AppHeader/>` mount; `AppHeader` gains a `CHeaderToggler` wired to that state. **Revised 2026-09-08 (6.12/[ADR-0042](../adr/0042-adminlte-design-system.md)):** raw AdminLTE markup — `aside.app-sidebar` + `ul.nav.sidebar-menu.flex-column` + `nav.app-header`, all direct children of `.app-wrapper`'s CSS grid; the ~~"per CoreUI's own template pattern, no custom breakpoint logic"~~ clause is **deliberately reversed** (NFR-24 revised) — collapse state is `sidebar-collapse`/`sidebar-open` on `document.body` with `matchMedia("(max-width: 991.98px)")`, and the `.sidebar-overlay` scrim is rendered by us. Nav-item array, `NavLink` active state, and the toggler's `data-testid` are unchanged. | 6.3d | M | FR-SHELL-1 (revised), NFR-24 (revised), [ADR-0018](../adr/0018-admin-shell-sidebar-layout.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 6.5 | `AppBreadcrumb` (route-derived `CBreadcrumb`) + `AppFooter` (`CFooter`) added to `AppShell`, completing the free-template shell shape | 6.4 | S | FR-SHELL-2, [ADR-0020](../adr/0020-admin-shell-full-template-parity.md) |
| 6.6 | ~~`OrgHome` dashboard stat widgets (`CWidgetStatsA`/`B` × 2 — Project count, active Org Member count)~~, read via existing `GET /projects`/`GET /org-memberships` list `total`, no new API/query hook beyond 6.2's existing list hooks. **Revised 2026-09-08:** rendering moved `CWidgetStatsA`/`B` → `WidgetStatsTile` under 6.12/[ADR-0042](../adr/0042-adminlte-design-system.md) (uncorrected in this row until now) → `InfoBox` under 6.13/[ADR-0045](../adr/0045-ds-3-infobox-widget-consolidation.md). Count-sourcing (`GET /projects`/`GET /org-memberships` `total`) unchanged throughout. | 6.4, 6.2 | S | FR-SHELL-3, NFR-27 (revised), [ADR-0020](../adr/0020-admin-shell-full-template-parity.md), [ADR-0045](../adr/0045-ds-3-infobox-widget-consolidation.md) |
| 6.7 | Dark/light color-mode toggle — header dropdown, `localStorage`-persisted. **Revised 2026-09-08 (6.12/[ADR-0042](../adr/0042-adminlte-design-system.md)):** ~~CoreUI `useColorModes` hook~~ → plain React `useState`/`useEffect` writing `data-bs-theme` on `<html>`, key `coreui-react-color-scheme` → `lte-theme` (**one-time orphaning of every saved user preference**). The 3-choice dropdown and its `data-testid`s are unchanged. | 6.3d | S | FR-SHELL-4 (revised), NFR-28, [ADR-0020](../adr/0020-admin-shell-full-template-parity.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 6.8 | UI-element reference pages (Colors, Typography, Icons) + "UI Elements" sidebar nav group — template-parity scaffolding, no FR/story backing (flagged explicitly, not to be mistaken for product scope) | 6.4 | S | [ADR-0020](../adr/0020-admin-shell-full-template-parity.md) |
| 6.9 | `FormField` shared component (~~`components/shared/`, CoreUI `CFormLabel`+`CFormInput`+`CFormFeedback`~~ **revised 2026-09-08: `components/molecules/form-field/` per [ADR-0043](../adr/0043-atomic-design-tiering-for-frontend-components.md); raw Bootstrap label/input/invalid-feedback markup per [ADR-0042](../adr/0042-adminlte-design-system.md)**, RHF `register()`-bound); `Login.tsx`'s 2 and `Signup.tsx`'s 5 hand-authored label+input blocks migrated onto it, both screens brought onto React Hook Form + Zod for the first time in the process | 6.1 | M | FR-DS-1, NFR-34, [ADR-0023](../adr/0023-frontend-shared-component-location.md), [ADR-0043](../adr/0043-atomic-design-tiering-for-frontend-components.md) |
| 6.10 | ~~`AppSidebar`'s `<CSidebar>` gains `colorScheme="dark"` — matches CoreUI free-template demo look, static (not wired to 6.7's toggle)~~ **Superseded 2026-09-08 by 6.12/[ADR-0042](../adr/0042-adminlte-design-system.md): the `sidebar-dark-*` skin family does not exist in AdminLTE v4 (zero occurrences in the shipped CSS), so this row's deliverable no longer has a mechanism — the sidebar renders `bg-body-secondary`, a real visual change. Row kept for history; the "static, not wired to 6.7's toggle" property survives.** | 6.4 | XS | FR-SHELL-5, NFR-36 (both revised), [ADR-0026](../adr/0026-sidebar-dark-color-scheme.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 6.11 | `AppHeader` org-switcher `CDropdown` (`cilBuilding` icon, lazy-fetches `GET /auth/me/orgs` on open, no caching) — lists caller's orgs, current org indicated via `useParams().orgId`, click → `navigate('/orgs/{id}')` (always org root); no create-org action in the menu | 6.3d, 2.4a | M | FR-SHELL-6, NFR-48, [ADR-0036](../adr/0036-shell-6-organization-switcher-header-dropdown.md) |
| 6.12 | **CoreUI → AdminLTE v4 design-system migration, big-bang** — swap deps (`@coreui/react`+`@coreui/coreui`+`@coreui/icons`+`@coreui/icons-react` out; `admin-lte@4.9.1` + `bootstrap@^5.3.8` + `@fortawesome/fontawesome-free` in), reorder `main.tsx`'s CSS imports, rewrite **every** `@coreui/react` call site as raw Bootstrap 5/AdminLTE markup (zero `@coreui/*` imports left in `frontend/src` or `frontend/tests`), rebuild the shell onto `.app-wrapper`'s CSS grid, reimplement sidebar/treeview/color-mode/dropdown/modal/toast behavior in React against AdminLTE's class contract (**no jQuery, no vendored plugin JS**), retarget the `Icon` atom from unloaded Bootstrap Icons to Font Awesome (`variant` prop added), convert `WidgetStatsTile`'s `icon` prop from a CoreUI name string to an FA class string. Every `data-testid` and accessible role preserved verbatim; test changes limited to design-system contract changes (class names, `data-bs-theme`). | 6.4–6.11, 7.x, 8.x | XL | FR-SHELL-1/2/4/5, FR-DS-1, NFR-24, NFR-36 (all revised), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 6.13 | New `InfoBox` molecule (`components/molecules/info-box/` per [ADR-0043](../adr/0043-atomic-design-tiering-for-frontend-components.md)'s own tiering convention, AdminLTE Info Box markup: `.info-box`/`.info-box-icon`/`.info-box-content`) — retires `WidgetStatsTile` (`OrgHome`'s Project-count/active-Org-Member-count widgets, 6.6) and `TestCycleDetail`'s local `StatTile` (4 execution-dashboard tiles, 8.4), both deleted outright once migrated. Repo-wide scope per direct CTO instruction. `ActiveMemberCountWidget` gains an icon it didn't have before; every other data-testid and count-sourcing/aggregation call unchanged. | 6.6, 8.4 | S | FR-SHELL-3, NFR-27 (revised), FR-EXEC-1, NFR-46 (revised), [ADR-0045](../adr/0045-ds-3-infobox-widget-consolidation.md) |
| 6.14 | Tabler v1.5.1 installed via CDN in `frontend/index.html` (`<link>`+`<script>`, pinned version, jsDelivr) — Phase 1 (install only) of a planned future AdminLTE→Tabler swap; CDN chosen over the npm package per explicit CTO instruction; Tabler's `<link>` placed before AdminLTE/Bootstrap/Font Awesome's own imports (injected via `main.tsx`) so AdminLTE continues to win every conflicting selector — zero visible change to any existing screen. No component migrated, `admin-lte`/`bootstrap`/`@fortawesome/fontawesome-free` untouched. Code committed on branch `tabler-install`, not yet merged into `main` as of this docs pass. | — | XS | FR-DS-3, NFR-56, [ADR-0053](../adr/0053-tabler-install-phase-1-cdn.md) |

## 7. Generic CRUD UI

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 7.1 | `<EntityTable>`, `<EntityForm>` (react-hook-form + zod), `<FkAutocomplete>` — generic ~~`components/crud/`~~ components (**revised 2026-09-08: `components/organisms/`/`components/molecules/` per [ADR-0043](../adr/0043-atomic-design-tiering-for-frontend-components.md)**, superseding [ADR-0023](../adr/0023-frontend-shared-component-location.md)'s location) | 6.2 | M | FR-ADMIN-2, [ADR-0043](../adr/0043-atomic-design-tiering-for-frontend-components.md) |
| 7.2 | `entityConfigs/` — one field-config object per entity (28 — every ERD entity except `Approval`/`User`/`AIAgent`/`AuthIdentity`, structurally excluded per [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md)) | 7.1 | L | FR-ADMIN-2, [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md) |
| 7.3 | Admin pages routed from an entity registry (`pages/admin/`), permission-gated action buttons via a new `usePermissions` hook | 7.2, 6.3b, 3.5 | M | FR-ADMIN-2, NFR-10, NFR-37 |
| 7.4 | Scope-selector step (`FkAutocomplete` against the ref entity before the list query fires) for `RiskItem`/`Attachment`'s branching/deep-chain scope; nav wiring — org/global entities in `AppSidebar`'s new "Admin" `CNavGroup`, project-scoped entities linked from `ProjectDetail.tsx` | 7.3 | M | FR-ADMIN-2, [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md) |

## 8. Bespoke workflow screens

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 8.1 | Login, OrgSwitcher/ProjectSwitcher. **The OrgSwitcher half of this reservation was never built as its own screen** — `Login.tsx`'s own post-login `orgContext`/`orgs` redirect (to `/orgs/{id}` or `/orgs/pick`) covers only the immediately-after-login case; a persistent, always-available switcher is delivered separately under SHELL-6 (6.11 below), not here. `ProjectSwitcher` remains undelivered/unreserved — no story has asked for one | 6.3b | M | FR-AUTH-1, FR-RBAC-1 |
| 8.1b | `OrgHome.tsx` project list + "New Project" ~~CoreUI~~ modal (name, optional standards_profile), inline standards_profile edit. **Revised 2026-09-08 (6.12/[ADR-0042](../adr/0042-adminlte-design-system.md)):** the modal is now the raw-Bootstrap `Modal` helper pattern (`OrgHome.tsx`'s own local component), not `CModal` | 8.1, 3.0b | S | FR-PROJ-1, [ADR-0017](../adr/0017-project-creation-flow.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
*8.1b bug fix (2026-09-07):* the list was local `useState` only — any unmount (navigating into a project and back, not just a page reload) silently emptied it even though the rows still existed server-side. Fixed by fetching `GET /projects?org_id=` (the generic-CRUD factory route already shipped by 3.3/ADR-0022, already reused by 6.6's dashboard widget) via `useQuery` on every mount; `createProject`/`updateProject` still write straight into the query cache for an instant UI update. No new backend route, no new ADR — a wiring fix reusing an already-shipped route, not a new architecture decision. TC-PROJ-018.
| 8.1c | `ProjectDetail.tsx` (route `/projects/:projectId`) — Release list (sortable `target_date` column) + "New Release" ~~CoreUI~~ modal; `OrgHome.tsx` project list items link here; per-release expand renders `GET .../test-cycles` result (cycles + nested executions, read-only audit view). **Revised 2026-09-08 (6.12/[ADR-0042](../adr/0042-adminlte-design-system.md)):** every modal on this screen (Release, plus REQ/REQ-2/REQ-3/REQ-4's own additions below) is now the raw-Bootstrap `ProjectModal` helper, not `CModal` | 8.1b, 3.0d | M | FR-PROJ-2, [ADR-0019](../adr/0019-release-creation-flow.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 8.2 | RequirementDetail (Requirement → optional TestCondition → TestCase, both paths). **Both REQ-2's direct-link path and REQ-3's TestCondition-mediated path landed as extensions of `ProjectDetail.tsx` instead** (2026-09-05/06, expand-in-place Requirement rows → TestCase list/TestCondition list, same pattern the Release/TestCycle audit view already uses) — a dedicated `RequirementDetail` route was never built and is no longer reserved (Sitemap's stale 2026-09-05 reservation removed) | 7.1, 4.1a, 4.1b | M | FR-REQ-1..3 |
| 8.3 | TestSuiteBuilder. **Delivered as an extension of `ProjectDetail.tsx` instead** (2026-09-06, same "Both...landed as extensions of `ProjectDetail.tsx` instead" pattern as 8.2 immediately above) — a new "Test Suites" section (list + create modal) plus a per-TestCondition TestCase sub-list with an "Add to suite" action, rather than a dedicated `TestSuiteBuilder` route; the Sitemap's reserved path is removed, not resurrected | 7.1, 4.1c | S | FR-REQ-4, [ADR-0030](../adr/0030-req4-test-suite-membership-bespoke-routes.md) |
| 8.4 | TestExecutionRunner (pass/fail/blocked + notes, raises Defect). **FR-EXEC-1's own slice delivered as `TestCycleDetail`** (new route `/projects/:projectId/test-plans/:testPlanId/test-cycles/:testCycleId`, nested under `TestPlanDetail` same as PLAN-1's own dedicated-page precedent) — dashboard (4 stat tiles reusing the existing generic-list `total`, no new route), flat execution-history list, "Record Result" modal submitting `POST /test-cycles/{id}/executions` (the create route ADR-0033 already built ahead of this story). **FR-EXEC-2's own slice delivered too** (EXEC-2 pass, 2026-09-07): a "History" button per execution row opens a modal on this same screen — ordered `TestLog` timeline + comment/attachment form, no new route, see [ADR-0038](../adr/0038-exec-2-append-only-test-log.md). FR-EXEC-3 (raise Defect) remains undelivered — no stub on this screen, see [ADR-0034](../adr/0034-exec-1-test-execution-recording-dashboard.md) | 7.1, 4.3, 4.4 | M | FR-EXEC-1..3 |
| 8.5 | TraceabilityMatrix (view + CSV export) | 7.1, 4.7 | M | FR-TRACE-1..2 |
| 8.6 | **Revised 2026-09-07 (DASH-1):** `LandingPage` is deleted (superseded, [ADR-0035](../adr/0035-dash-1-root-redirect-and-dashboard-placeholder.md)). `/` becomes a pure guard in `App.tsx`: spinner while `isInitializing`, else `<Navigate to="/login">` (no `accessToken`) or `<Navigate to="/dashboard">` (`accessToken` present) — checks `accessToken` only, never `orgContext`/`orgs`. New empty `Dashboard` page (~~CoreUI~~ shell placeholder, no data fetch — **revised 2026-09-08, [ADR-0042](../adr/0042-adminlte-design-system.md): raw AdminLTE `.app-content` markup, still no data fetch**) at new route `/dashboard`, wrapped in the existing `ProtectedRoute`. `Login.tsx`'s own separate post-login `orgContext`/`orgs` redirect is untouched. | 6.3b | S | FR-DASH-1, NFR-35 (revised), NFR-47, [ADR-0035](../adr/0035-dash-1-root-redirect-and-dashboard-placeholder.md), [ADR-0042](../adr/0042-adminlte-design-system.md) |
| 8.7 | `TestPlanDetail` (new route `/projects/:projectId/test-plans/:testPlanId`, **not** a `ProjectDetail` extension — first bespoke screen this scaffold routes separately per-entity rather than folding into `ProjectDetail`, CTO direction 2026-09-06) — plan header (identifier/scope/approach/staffing/schedule/status), "Test Suites" section (live included-suites list + add/remove, `test_plan_membership.py`), read-only "Covered Test Cases" section (`GET /test-plans/{id}/test-cases`); `ProjectDetail`'s existing generic-admin "Test Plans" list link routes here instead of the generic `EntityFormPage` edit route | 7.1, 4.2a | M | FR-PLAN-1, [ADR-0031](../adr/0031-plan1-test-plan-membership-and-status-transition-routes.md), [PLAN-1 UI Design Document](../ui-design/2026-09-06-plan-1-test-plan-membership-ui-design.md) |
| 8.7a | `TestPlanDetail` gains a new "Entry/Exit Criteria" section — full CRUD (list/add/edit/delete, all 4 types), reusing `entityCrud.ts` + `entityConfigs/entry-exit-criteria.ts` filtered by `?test_plan_id=`, no new API-lib file; `ProjectDetail`'s existing Release→TestCycle expand-in-place row gains a second flat `<ul>` (`cycle.exit_criteria`, `type=exit` only) alongside the existing executions `<ul>`, same expanded cell | 8.7, 4.2b | S | FR-PLAN-2, NFR-42, [ADR-0032](../adr/0032-plan2-entry-exit-criteria-visibility.md), [PLAN-2 UI Design Document](../ui-design/2026-09-06-plan-2-entry-exit-criteria-visibility-ui-design.md) |
| 8.7b | `TestPlanDetail` gains a new "Test Cycles" section — live list (`GET /test-cycles?test_plan_id=`) + "Create Cycle" modal (`release_id`/`environment_id` `FkAutocomplete`s, inline "+ New Environment" sequential-create toggle, `name`/`start_date`/`end_date`), submits the new `POST /test-plans/{id}/test-cycles`; `entityConfigs/test-cycle.ts` stays without `"create"` (bespoke-only, same posture `TestCase`/`TestCondition` already have) — no admin-surface fallback | 8.7, 4.2 | M | FR-PLAN-3, [ADR-0033](../adr/0033-plan3-test-cycle-creation-and-execution-scope-check.md), [PLAN-3 UI Design Document](../ui-design/2026-09-06-plan-3-test-cycle-creation-ui-design.md) |

## 9. Infra

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 9.1 | `docker-compose.yml` — postgres, backend, frontend, nginx, dev+prod profiles | 1.1, 6.1 | M | ADR-0010 |
| 9.2 | `nginx.conf` — `/api/*` → backend, `/*` → frontend | 9.1 | S | ADR-0010 |
| 9.3 | `postgres-test` service for integration tests | 9.1 | S | NFR-9 |

## 10. Testing

| # | Deliverable | Depends on | Size | Maps to |
|---|---|---|---|---|
| 10.1 | Backend unit tests (isolated model/service logic) | per-task | ongoing | [Test Plan](../test-plan/2026-09-03-master-test-plan.md) |
| 10.2 | Backend integration tests — RBAC allow/deny matrix at HTTP layer, real Postgres; includes login throttle (429) and zero-active-org (403) cases | 9.3, all §2–5 | L | NFR-9, NFR-11, [Test Design](../test-design/2026-09-03-test-design.md) |
| 10.3 | Frontend unit tests (Vitest + RTL) — generic CRUD components + bespoke screens | per-task | ongoing | [Test Plan](../test-plan/2026-09-03-master-test-plan.md) |
| 10.4 | E2E tests (Playwright) — login→requirement→test case→execution; RBAC-denial flow | 9.1, all §6–8 | L | [Test Case doc](../test-cases/2026-09-03-test-cases.md) |
| 10.4b | E2E test — members→org-home round trip via the sidebar's nav link (`shell-nav.spec.ts`), asserted by URL after a real click, not browser back | 6.4, 9.1 | S | FR-SHELL-1 AC3, TC-SHELL-003 |
| 10.4c | E2E test — dark/light toggle flips theme + persists across reload; frontend/integration tests for stat widget counts (seeded fixture, not mocked) and breadcrumb path-derivation | 6.5, 6.6, 6.7, 9.1 | S | FR-SHELL-2..4, TC-SHELL-007..013 |
| 10.4d | Frontend test — sidebar keeps dark `colorScheme` across all three app-wide mode states (light/dark/auto), proving 6.10 is not wired to 6.7's toggle | 6.10, 6.7 | XS | FR-SHELL-5, TC-SHELL-015 |

## 11. Documentation (this batch)

| # | Deliverable | Status |
|---|---|---|
| 11.1 | Requirement Document | Done |
| 11.2 | WBS (this document) | Done |
| 11.3 | ADRs (0001–0023 + index) | Done |
| 11.4 | Database Document | Done |
| 11.5 | API Document | Done |
| 11.6 | Master Test Plan | Done |
| 11.7 | Test Design | Done |
| 11.8 | Test Cases | Done |
| 11.9 | ADR-0022 (generic CRUD router factory) + propagation across Requirements/WBS/Database/API/Test Plan/Test Design/Test Cases (this revision, 2026-09-05) | Done |
| 11.10 | ADR-0024 (public landing page) + LANDING-1 user story + propagation across Requirements/WBS/Database/API/Test Plan/Test Design/Test Cases (this revision, 2026-09-05) | Done |
| 11.11 | ADR-0025 (`Requirement.title` schema gap-fill) + propagation across Requirements/WBS/Database/API/Test Cases (REQ-1 documentation pass, 2026-09-05) — Test Plan/Test Design needed no new content (existing ADMIN-2/generic-CRUD sections already cover `Requirement` generically; no new technique or risk this ADR introduces) | Done |
| 11.12 | ADR-0027 (generic admin CRUD UI + execution/traceability backend completion) + new UI Design Document + new Sitemap Document + propagation across Requirements/WBS/Database/API/Test Plan/Test Design/Test Cases (this revision, 2026-09-05) | Done |
| 11.13 | ADR-0029 (`TestCase` resolver direct-link fallback, REQ-2 gap-fill; renumbered from an initial 0028 draft after a numbering collision with REQ-3's own, separately-landed ADR-0028) + correction of ADR-0022's own resolver prose (not silently edited — ADR-0029 records why) + propagation across Requirements/WBS/Database/API/Sitemap/Test Plan/Test Design (REQ-2 documentation pass, 2026-09-05) — UI Design Document needed only a one-line scope note (REQ-2 is a bespoke `ProjectDetail.tsx` extension, out of that document's stated generic-admin-surface scope, same posture it already declares for every other bespoke workflow screen); Test Cases needed no new row (TC-REQ-003/004 already existed and already assert the behavior this ADR fixes, just a source-link addition) | Done |
| 11.14 | ADR-0030 (REQ-4 TestSuite membership bespoke routes: add/remove/live-list, cross-project rejection, duplicate-add conflict) + new REQ-4 UI Design Document + Sitemap correction (removes the stale `TestSuiteBuilder` reservation) + propagation across Requirements (NFR-38/39)/WBS/Database/API/Test Plan/Test Design/Test Cases (REQ-4 documentation pass, 2026-09-06) | Done |
| 11.15 | ADR-0031 (PLAN-1 TestPlan↔TestSuite membership bespoke routes: add/remove/live-list, coverage query, status-transition legality guard) + new PLAN-1 UI Design Document (new `TestPlanDetail` route, not a `ProjectDetail` extension) + Sitemap addition + propagation across Requirements (NFR-40/41)/WBS (4.2 correction + 4.2a/8.7)/Database/API/Test Plan/Test Design/Test Cases (PLAN-1 documentation pass, 2026-09-06) | Done |
| 11.16 | ADR-0032 (PLAN-2 `EntryExitCriteria` visibility: full-CRUD `TestPlanDetail` section — zero new backend routes, reuses ADMIN-2's factory verbatim — + nested `exit_criteria` field on `GET /releases/{id}/test-cycles`, quadruple permission gate) + new PLAN-2 UI Design Document + propagation across Requirements (NFR-42)/WBS (4.2 further-corrected + 4.2b/8.7a)/Database/API/Sitemap/Test Plan/Test Design/Test Cases (PLAN-2 documentation pass, 2026-09-06) | Done |
| 11.17 | ADR-0033 (first-party MCP server: streamable-HTTP transport, ASGI sub-app mount on existing FastAPI under `/mcp`, direct-call tool dispatch bypassing FastAPI's `Depends` defaults, per-tool bearer-key resolution reusing `_resolve_agent_actor`) + new `app/mcp/` module + MCP-1 tools `create_test_case` + `list_test_cases` + `_actor_membership_exists` route-side helper for the AIAgent NFR-1 boundary (closes a silent 404-for-every-MCP-call gap surfaced when the MCP tool hit `_org_membership_exists(actor.actor_id)` against an `OrgMembership` table FK'd to `user.actor_id`) + `mcp[cli]>=1.28,<2` dep + nginx `/mcp/` passthrough + propagation across Requirements (NFR-43)/WBS (5.1+5.2 split into done/5.2a+5.3 backlog)/API Doc §6/Test Plan (§6 release-blocking + §14 risk row)/Test Design (§11 expanded)/Test Cases (existing TC-MCP-001/002/003 source-linked + new TC-MCP-008/009 for the two MCP-1 AC1 negative cases) — Database/UI Design/Sitemap explicitly not touched (no schema change, no human-facing UI for MCP, no new Sitemap route) (MCP-1 documentation pass, 2026-09-06) | Done |
| 11.17 | ADR-0033 (PLAN-3 `TestCycle` creation `POST /test-plans/{id}/test-cycles` + `POST /test-cycles/{id}/executions` execution scope-check reusing PLAN-1's coverage-query join, also FR-EXEC-1's own create route; generic `TestExecution` create removed; `test_manager` RBAC bundle extension for `environment.*`/`test_execution.create`+`.read`) + new PLAN-3 UI Design Document (`TestPlanDetail`'s 5th section) + Sitemap update + propagation across Requirements (NFR-43/44/45)/WBS (4.2 further-corrected + 8.7b)/Database/API/Test Plan/Test Design/Test Cases (PLAN-3 documentation pass, 2026-09-06) | Done |
| 11.18 | ADR-0034 (EXEC-1 `TestCycleDetail` recording screen — first frontend caller of ADR-0033's `POST /test-cycles/{id}/executions`; live dashboard reusing the existing generic-list `total` across 4 `result`-filtered calls, no new backend route/schema change; test-case picker scoped to PLAN-1's coverage query; no RBAC change) + new EXEC-1 UI Design Document + Sitemap update (8.4/`TestExecutionRunner` partially resolved) + propagation across Requirements (NFR-46)/WBS (8.4 corrected)/Database/API/Test Plan/Test Design/Test Cases (EXEC-1 documentation pass, 2026-09-07) | Done |
| 11.19 | ADR-0035 (DASH-1 root route becomes a pure `accessToken`-only redirect — spinner/`/login`/`/dashboard` — deletes the LANDING-1 public landing page outright, adds new empty `Dashboard` placeholder screen, fixes the AUTH-2 reload-while-logged-in gap by dropping the `orgContext` dependency; supersedes ADR-0024) + new DASH-1 user story (supersedes LANDING-1 story) + new DASH-1 UI Design Document + Sitemap update (`/` guard, new `/dashboard` route) + propagation across Requirements (FR-DASH-1, NFR-35 revised, NFR-47)/WBS (8.6 revised)/Database/API/Test Plan/Test Design/Test Cases (DASH-1 documentation pass, 2026-09-07) | Done |
| 11.20 | ADR-0036 (SHELL-6 organization switcher header dropdown — icon-triggered `CDropdown` in `AppHeader`, lazy-fetches on open, always shown even for a single-org account, switch always navigates to the target org's root, no create-org action — + new `GET /auth/me/orgs` endpoint, reuses `OrgSummary` verbatim, active-memberships-only, no schema change) + new SHELL-6 user story + new SHELL-6 UI Design Document + propagation across Requirements (FR-SHELL-6, FR-AUTH-5, NFR-48)/WBS (8.1 corrected + 2.4a/6.11 added)/Database (SHELL-2/3/4's pre-existing ADR-0019→ADR-0020 link typo corrected in the same pass)/API/Sitemap (new "Header controls" section)/Test Plan (new risk row)/Test Design (§31)/Test Cases (TC-SHELL-016..020, TC-AUTH-035) (SHELL-6 documentation pass, 2026-09-07) — no code written this pass, per explicit instruction | Done |
| 11.21 | ADR-0038 (EXEC-2 append-only `TestLog` — generic `post_update_hook` on `CrudEntityConfig`, log-on-create + log-on-`result`-change, two new bespoke routes `POST /executions/{id}/comments`/`GET /executions/{id}/logs`, `agent_action` overrides the human event type for an `AIAgent` caller, `_actor_membership_exists` bug-fix promoted to `crud_factory.py`, `TestCycleDetail` "History" modal; no schema/RBAC change) + new EXEC-2 UI Design Document + Sitemap update (8.4/`TestExecutionRunner` FR-EXEC-2 slice resolved) + propagation across Requirements/WBS (4.3, 8.4 corrected)/Database/API/Test Plan/Test Design/Test Cases (EXEC-2 documentation pass, 2026-09-07) — renumbered from a since-superseded ADR-0036/WBS-11.20/Test-Design-§31 draft after a real collision with SHELL-6 was found merging into `main` | Done |
| 11.22 | ADR-0039 (DASH-2 relabels `OrgHome` "Dashboard" — heading/sidebar+icon/breadcrumb only, route unchanged, accepted unresolved naming overlap with ADR-0035's separate `/dashboard` placeholder — + Project table gains ID column, Edit modal (name+standards_profile together, replaces inline click-to-edit), Delete button+confirm modal (first frontend caller of ADR-0022's already-shipped `DELETE /projects/{id}`), client-side search/sort/pagination over the already-fully-fetched list; no backend/schema/RBAC change) + new DASH-2 UI Design Document + Sitemap update (`/orgs/:orgId` row + route tree label, naming-overlap note) + propagation across Requirements (FR-DASH-2, FR-PROJ-3, NFR-49, NFR-50)/Database/API/Test Plan (new risk row)/Test Design (§33)/Test Cases (TC-SHELL-021, TC-PROJ-019..024) (DASH-2 documentation pass, 2026-09-07) | Done |
| 11.23 | ADR-0040 (`role_assignment.project_id` FK `RESTRICT` → `CASCADE`, migration `6a11a6a1d803` — fixes the defect ADR-0039's own isolated-env verification found: every Project created through the app's UI permanently 409'd on `DELETE /projects/{id}` because its auto-granted creator `RoleAssignment` blocked its own deletion; scoped to this one FK only, every other `project_id`-FK'd entity keeps `RESTRICT`) + `test_delete_project_created_via_api_cascades_the_creator_role_assignment` (backend integration, replaces the prior characterization test proving the old, now-fixed `409`) + propagation across Database/API/Test Plan (risk row updated)/Test Cases (TC-PROJ-021 row/coverage-note updated) (2026-09-07, same-day follow-up to the DASH-2 pass) | Done |
| 11.24 | ADR-0041 (DS-2 shared `Table` container — new `frontend/src/container/Table.tsx`, new `src/container/` location convention amending ADR-0023, table + pagination + page-size selector 10/25/50/100, server and client pagination modes, reused across `EntityTable`/`OrgHome`'s Project table (replaces ADR-0039's own bespoke pagination chrome, keeps its client-side search/sort)/`RoleAssignmentsPanel`/`OrgMembers`/`ProjectDetail`'s outer tables/`TestPlanDetail`; backend `page_size` max 25 → 100 hardcoded literal, default unchanged; `GET /orgs/{org_id}/role-assignments` gains pagination, breaking response-shape change, no external caller) + new DS-2 user story (added to the existing design-system-component-stories.md file) + new DS-2 UI Design Document + propagation across Requirements (FR-DS-2, NFR-6 revised, NFR-51)/Database (reviewed, no schema impact)/API (§1 convention, `role-assignments` route+prose+footnote)/Sitemap (note only, no route change)/Test Plan (new risk row)/Test Design (§34)/Test Cases (TC-DS-009..018) (DS-2 documentation pass, 2026-09-07) — no code written this pass, per explicit instruction | Done |

| 11.25 | ADR-0042 (**AdminLTE v4 + Bootstrap 5 + Font Awesome Free replaces CoreUI as the project's design system** — `admin-lte@4.9.1` + `bootstrap@^5.3.8` + `@fortawesome/fontawesome-free`, CSS entry reordered in `main.tsx`, every `@coreui/*` package removed; **raw HTML/JSX against the CSS repo-wide**, generalizing ADR-0037's 5-file pattern with no component-library wrapper anywhere; **AdminLTE's own JS plugins and jQuery deliberately NOT vendored**, all behavior reimplemented as React state against AdminLTE's literal class contract; **big-bang scope explicitly reversing ADR-0037's incremental stance**, because two Bootstrap-family stylesheets cannot coexist — ADR-0012's own Tailwind-vs-CoreUI cascade argument turned on itself; resolves PR #42's dangling `Icon`-atom open question (blank `bi bi-*` glyphs against a never-loaded font) by retargeting the atom to Font Awesome rather than adding a second icon library) + supersession of ADR-0012 and ADR-0037, partial supersession of ADR-0018/ADR-0020/ADR-0026 + propagation across root `CLAUDE.md` ("Design system" section rewritten) / `frontend/CLAUDE.md` (3 notes marked actively-wrong-under-AdminLTE, 4 annotated as surviving) / Requirements (FR-SHELL-1/2/4/5, FR-DS-1, NFR-24, NFR-36 revised in place with strikethrough; new traceability row) / WBS (6.4, 6.7, 6.10 revised; 6.12 added) / ADR index (AdminLTE-2026-09-08 documentation pass) | Done |
| 11.26 | ADR-0043 (atomic-design tiering replaces `components/shared/`/`components/crud/`, supersedes ADR-0023's directory-location decisions — `FormField`/`FeaturedCard`/`WidgetStatsTile` → `molecules/`, `FkAutocomplete`/`ScopeSelector` → `molecules/`, `EntityTable`/`EntityForm` → `organisms/`, `AppBreadcrumb`/`AppFooter`/`AppHeader`/`AppSidebar` → `organisms/`, `AppShell` → `templates/`; pure location move, zero export/prop/behavior change, `container/`'s ADR-0041 axis untouched) + 14 `src/` + 12 `tests/` import-path updates + test-file relocation to mirror the new `src/` shape (including the 5 pre-existing `App*` tests, previously flat at `tests/` root, moved under `tests/components/organisms/`/`tests/components/templates/` in the same pass — closing a `frontend/CLAUDE.md`-documented "tests mirror `src`" inconsistency that predated this ADR) + stale doc-comment corrections in every moved file (several explicitly asserted "this repo does NOT use atoms/molecules/organisms tiers," true when written, DS-1-era, now corrected) + propagation across WBS (6.9, 7.1 revised with strikethrough) / ADR index / ADR-0023 (forward-pointing status addendum) | Done |
| 11.29 | ADR-0046 (SHELL-7 sidebar-mini layout — `sidebar-mini` added to `AppShell`'s constant body-class set, AdminLTE's own icon-only-collapsed-with-hover-expand modifier, no new state machine — + org-scoped CRUD nav restructure: `AppSidebar`'s flat 8-entity `Admin` group splits into 3 named, individually-iconed groups, Access Control/Catalogs/Organization, presentation-layer only, `orgScopedEntities` registry/routes unchanged; `UI Elements` untouched) + new SHELL-7 user story + new SHELL-7 UI Design Document + propagation across Requirements (FR-SHELL-7)/Database (reviewed, no schema impact)/API (reviewed, no API impact)/Sitemap (Admin-group description updated to the 3 new groups)/Test Plan (new risk row)/Test Design (§37)/Test Cases (TC-SHELL-022..028) (SHELL-7 documentation pass, 2026-09-08) — no code written this pass, per explicit instruction. **Addendum (2026-09-09):** implementation + all 7 TCs automated (Vitest + Playwright) landed same branch, commit `eb0dd91` — see that commit and the isolated-stack verification pass for the automated-coverage record. | Done |

| 11.27 | ADR-0044 (EXEC-3 raise-a-`Defect` bespoke create route `POST /executions/{id}/defects` — atomic `Defect`+`TestCaseDefectLink` insert, `422` if the execution's `result != fail` — + new `GET /test-cases/{id}/defects` ordered read, most-recent-first, not in the original API Document scope + `test_manager` RBAC bundle extension for `defect.create`, `test_manager`+`tester` extension for `test_case_defect_link.read`, fifth ad hoc extension for `test_manager`) + new EXEC-3 UI Design Document (`TestCycleDetail`'s "Raise Defect" button on `fail` rows + `EntityFormPage`'s new `test-case` Defects section) + Sitemap update (FR-EXEC-3's remaining "not yet built" reservation resolved) + propagation across Requirements (NFR-52)/WBS (this row)/Database/API/Test Plan (new risk row)/Test Design (§35)/Test Cases (TC-EXEC-007/008/009 sharpened, TC-EXEC-011/012 added) (EXEC-3 documentation pass, 2026-09-08) — renumbered from an initial ADR-0041/WBS-11.24/Test-Design-§34/NFR-51 draft after a real collision with DS-2 was found merging into `main`; docs-only, no code written this pass, per explicit instruction — implementation (backend routes/migration/RBAC, frontend UI, 3 test layers) landed 2026-09-08 in a follow-on pass, verified against an isolated stack and by the user manually | Done |

| 11.28 | ADR-0045 (DS-3 shared `InfoBox` component, `components/molecules/info-box/` — AdminLTE v4's own documented Info Box markup, `.info-box`/`.info-box-icon`/`.info-box-content`; retires `WidgetStatsTile` (`OrgHome`'s 2 count widgets) and `TestCycleDetail`'s local `StatTile` (4 execution-dashboard tiles), both deleted once migrated; repo-wide scope per direct CTO instruction, widened from the original 2-widget ask once a second independent stat-tile duplication was found; `ActiveMemberCountWidget` gains an icon it didn't have before; count-sourcing/aggregation/data-testids unchanged) + new DS-3 user story (added to the existing `design-system-component-stories.md` file) + new DS-3 UI Design Document + propagation across Requirements (NFR-27/NFR-46 revised in place, new traceability row)/WBS (6.6 corrected — a pre-existing, uncorrected gap from 6.12's own pass — + 6.13 added)/Database (reviewed, no schema impact)/API (reviewed, no route/schema change)/Sitemap (note only, no route change)/Test Plan (new risk row)/Test Design (§36)/Test Cases (TC-DS-019..022) (DS-3 documentation pass, 2026-09-08) — renumbered from an initial ADR-0043/WBS-11.26/Test-Design-§35 draft after a real collision with the atomic-design-tiering ADR (also 11.26/§35 above) was found merging into `main`; `InfoBox` relocated to `components/molecules/` to match that ADR's own new convention; no code written this pass, per explicit instruction | Done |

| 11.30 | ADR-0047 (PROJ-4 gives Project CRUD its own sidebar-reachable page, amending ADR-0039's placement on "Dashboard" — new `/orgs/:orgId/projects` route + `ProjectsPage.tsx`, the table/modals moved verbatim out of `OrgHome.tsx`; new flat "Projects" sidebar nav item, no icon, DASH-2/TC-SHELL-021's "Dashboard is the only icon item" invariant left unchanged; `OrgHome`/"Dashboard" trims to the 2 stat widgets + `RoleAssignmentsPanel`; Project-count widget becomes a link to the new page; no backend/schema/RBAC change) + new PROJ-4 user story (added to the existing `project-release-stories.md` file) + new PROJ-4 UI Design Document + propagation across Requirements (FR-PROJ-3 revised in place, new FR-PROJ-4, new traceability row)/Database (reviewed, no schema impact)/API (reviewed, no route/schema change)/Sitemap (`/orgs/:orgId` row revised, new `/orgs/:orgId/projects` row, route-tree updated)/Test Plan (new risk row)/Test Design (§38)/Test Cases (TC-PROJ-019/024 location clause corrected in place, new TC-PROJ-025/026, TC-SHELL-027's own nav-order row corrected for the "Projects" insertion — a real cross-story interaction found while writing this pass) — numbered `0047`/`11.30`/`§38`, not `0046`/`11.29`/`§37`, since those were already claimed by SHELL-7 (unmerged but in flight on its own branch when the number was picked; SHELL-7 has since merged, PR #48, confirming `0047` stayed next-free) — **docs-only, no code landed on `main` this pass, per explicit instruction.** **Addendum (2026-09-09):** the full implementation (`ProjectsPage.tsx`, `OrgHome.tsx` trimmed, `App.tsx` route, `AppSidebar.tsx`/`AppBreadcrumb.tsx` nav+breadcrumb entries, 3 e2e specs updated for the new navigation step) was already built and verified in an earlier pass on the still-unmerged `proj-3-sidebar-crud` worktree branch (commit `4610efc`) — Vitest 422/422, 5 Playwright specs green against a fresh isolated stack (`testnexa-proj4-test`), torn down after. That branch is not yet merged into `main`; this docs pass exists independently of it on `main` so the plan is reviewable before the code lands (root `CLAUDE.md`'s own "docs on `main`, then implement" flow) — merging the branch (or rebasing it onto this now-updated `main`, which now also carries SHELL-7 and this row's own TC-SHELL-027 fix that branch's own commit predates) is a separate, later step | Done |

| 11.31 | ADR-0048 (BRAND-1 logo/brand system — checkmark-in-shield mark + "**Test**Nexa" wordmark, one `currentColor` SVG pair, `logo-mark.svg`/`logo-full.svg`, no per-theme duplicate files, applied via `BrandLogo` atom (fixing its long-standing hardcoded "AdminLTE" text bug) on login/signup, `AppHeader`'s navbar-brand (small mark), `AppSidebar`'s `.sidebar-brand` (AdminLTE's own `.brand-image-xl`/`.brand-image-xs`+`.logo-xl`/`.logo-xs` cross-fade on `sidebar-mini` collapse, zero new JS, already active since SHELL-7); new SVG-only favicon with its own fixed non-`currentColor` palette; no new brand color, reuses stock Bootstrap `--bs-primary`; no backend/database/API change) + propagation across Requirements (new FR-BRAND-1, NFR-53, new traceability row)/Database (reviewed, no schema impact)/API (reviewed, no API impact)/Sitemap (chrome-only note, no route change)/Test Plan (new risk row)/Test Design (§39)/Test Cases (Design System / Shared Components extended, TC-DS-023..030) — numbered `0048`/`11.31`/`§39`, confirmed next-free against `origin/main` immediately before writing (0047/11.30/§38 already claimed by PROJ-4, merged same day) — **docs-only, no code written this pass, per explicit instruction.** No new user story file — folded into the existing `design-system-component-stories.md` feature area (FR-BRAND-1 sits in Requirements §2.12 alongside FR-DS-1/FR-DS-2) rather than opening a new stories file for a single-story area | Done |
| 11.32 | ADR-0049 (FRONTEND-1 co-locate Vitest unit tests — every `frontend/tests/**.test.{ts,tsx}` moves to live next to its source under `frontend/src/`, `frontend/tests/` renamed to `frontend/test-setup/` (contains only the jsdom-polyfill `setup.ts`), `vite.config.ts`'s `setupFiles` path updated to match, relative imports in every moved file shorten to `./<sibling>` or `./` via the existing `index.ts` barrel, `frontend/CLAUDE.md`'s pre-existing "tests live in `frontend/tests/`" paragraph rewritten in place, e2e test comments referencing the old path corrected, historical plan docs `docs/superpowers/plans/*.md` deliberately NOT rewritten — see ADR-0049 Consequences; no backend/database/API/UI surface change) + propagation across Requirements (new NFR-54, new traceability row)/Database (reviewed, no schema impact)/API (reviewed, no API impact)/UI Design (reviewed, no UI change)/Sitemap (reviewed, no route change)/Test Plan (reviewed, no new risk — same vitest-layer mechanics, different physical path)/Test Design (§40)/Test Cases (no new TC rows — see the FRONTEND-1 coverage-summary note; total stays at the BRAND-1 pass's 239/135) — numbered `0049`/`11.32`/`§40`/`NFR-54`, confirmed next-free against `origin/main` immediately before this docs pass (0048/11.31/§39/NFR-53 already claimed by BRAND-1, merged same day) | Done |

| 11.33 | ADR-0050 (SHELL-9 resolves org nav context on project-scoped routes — new `useResolvedOrgId()` hook, first file in a new `frontend/src/hooks/` directory, route param else `GET /projects/{id}` fetch + read `org_id`, generalizing `useAdminRouteContext`'s own pre-existing admin-only version of the same idea; `AppSidebar`'s full org nav — Dashboard/Projects/Members/3 admin groups — now renders on every project-scoped screen, PROJ-4's existing "Projects" item serving as the way back to the list rather than a new nav entry; `AppBreadcrumb` gains a resolved `Projects → {project name}` root on all 5 project-scoped route patterns, replacing a bare unlinked "Project" label; loading/error/404 fallback reuses the existing empty-nav/graceful-degradation states, no new UI invented; no backend/schema/RBAC change) + new SHELL-9 user story (added to the existing `admin-shell-sidebar-stories.md` file) + new SHELL-9 UI Design Document + propagation across Requirements (new FR-SHELL-8, new NFR-55, new traceability row)/Database (reviewed, no schema impact)/API (reviewed, no route/schema change)/Sitemap (5 project-scoped route rows' breadcrumb-trail note updated)/Test Plan (new risk row)/Test Design (§41)/Test Cases (new TC-SHELL-029..034 — no pre-existing row pinned the prior empty-sidebar/bare-breadcrumb behavior on project routes, so this is new coverage, not a correction) — numbered `0050`/`11.33`/`§41`/`NFR-55`, renumbered from an initial `0049`/`11.32`/`§40`/`NFR-54` draft after a real collision with FRONTEND-1's own independent claim on those exact same numbers, found merging into `main` — **docs-only, no code written this pass, per explicit instruction** (SHELL-9 documentation pass, 2026-09-09). **Addendum (2026-09-09):** implementation (`useResolvedOrgId.ts`, `AppSidebar.tsx`/`AppBreadcrumb.tsx`) landed same branch, commit `2113a96`, plus all 6 TCs automated. Verified against isolated stack `testnexa-shell9-test`: Vitest 442/442 (69 files, incl. 33 pre-existing tests fixed for a missing `QueryClientProvider` the new hook's `useQuery` call requires), Playwright green on the new/updated shell specs, backend integration 239/243 (4 fails independently confirmed unrelated — 3 documented known-harmless per `e2e/CLAUDE.md`, 1 flaky/order-dependent, re-run alone clean; `git diff main -- backend/` is empty, this story touches no backend code). DB-clone step confirmed via `alembic_version`/row-count match against main, not just "containers Up." | Done |

| 11.34 | ADR-0051 (SHELL-10 amends ADR-0050 Decision §2 — project-scoped routes stop rendering the org nav and instead get a distinct project-mode nav: new `PROJECT_ENTITY_GROUPS` over `projectScopedEntities`, 4 named/iconed groups — Test Design/Test Planning/Execution & Defects/Setup, 13 of 20 entities, 7 explicitly excluded via `PROJECT_EXCLUDED_ENTITY_KEYS`; 2 "back" links at the bottom, Project Overview + Back to Projects; `useResolvedOrgId()` gains a `mode: "org"\|"project"` field as the explicit route-kind signal `AppSidebar` needed; no backend/schema/RBAC change, every linked route already shipped under ADR-0025/ADR-0027) + new SHELL-10 user story (added to `admin-shell-sidebar-stories.md`) + new SHELL-10 UI Design Document + propagation across Requirements (new FR-SHELL-9, FR-SHELL-8's own sidebar-content clause struck through/superseded in place, new traceability row)/Database (reviewed, no schema impact)/API (reviewed, no route/schema change)/Sitemap (5 project-scoped route rows' sidebar note superseded, new SHELL-10 note)/Test Plan (2 new risk rows)/Test Design (§42)/Test Cases (TC-SHELL-029/030/031 revised in place — literal claim moved from "org nav" to "project-mode nav" — new TC-SHELL-035..038, total 237/135 → 241/138 on this branch's own pre-merge count; reconciled against BRAND-1/FRONTEND-1's own parallel total to 249/142 at merge time, see Test Cases' own merge-reconciliation note) — numbered `0051`/`11.34`/`§42`, renumbered from an initial `0050`/`11.33`/`§41` draft for the same reason as 11.33's own row above — implementation landed same branch, commit `3dadd0e` (picked up from a died-mid-task agent's uncommitted work, read and confirmed correct — no stray/temporary markers — before building on it; one real gap found and fixed, entity groups gated on `projectId` alone instead of `orgId` too, ADR-0051 Consequences), tests commit `ba17f01`, numbering-collision fix commit `8dc3eb1` (ADR-0048→0049/WBS 11.31→11.32/§39→§40/NFR-53→54, real collision against BRAND-1 merged same day on `origin/main`, PR #50), second renumbering pass this merge (0049→0050/0050→0051, 11.32→11.33/11.33→11.34, §40→41/§41→42, NFR-54→55 — a real collision against FRONTEND-1's own independent claim on the exact same first-renumbered values, found merging into `main`). Verified against isolated stack `testnexa-shell9-test`: `tsc --noEmit` clean, Vitest 451/451, new/rewritten Playwright specs green at `--workers=1` (a multi-worker run's failures independently confirmed host contention — `immich` at 260% CPU — not a regression), backend integration 240/243 (same 3 known-harmless fails, frontend-only story, `git diff main -- backend/` empty). **Known open item, now resolved this merge:** BRAND-1's own code (logo/favicon/header changes, PR #50) plus FRONTEND-1's own code (PR #52) are both merged into this branch as part of landing it on `main` | Done |
| | **Amendment, 2026-09-19, [ADR-0093](../adr/0093-retire-test-conditions-standalone-admin-page.md) (11.67):** this row's own `test-conditions`-in-the-Test-Design-group finding is historical only — that entity's standalone admin surface is retired outright, dropping the group from 4 children to 3 and the registry from 20 `projectScopedEntities` to 19. This row's own text describes what was correct on 2026-09-09 and is left unedited | |
| 11.35 | [ADR-0052](../adr/0052-remove-ui-elements-nav-group.md) (REMOVE-UI-1 — retire the org-scoped "UI Elements" nav group + Colors/Typography/Icons reference pages that ADR-0020 had added as template-parity scaffolding; frontend-only — `AppSidebar`'s `navGroups` array loses the `ui-elements` group, `App.tsx`'s 3 corresponding React Router routes are deleted, `frontend/src/pages/ui-elements/{Colors,Icons,Typography}.tsx` go with them; no backend/database/API change) + ADR-0020's `Status` row edits to "Partially superseded by 0042, 0052" + new removal UI Design Document (`ui-design/2026-09-09-remove-ui-elements-nav-group-ui-design.md`) + Sitemap correction (`/orgs/:orgId/ui-elements/{colors,typography,icons}` route row + tree branch removed) + propagation across Requirements ("Not an FR (explicitly, per ADR-0020)" paragraph struck + superseded by 0052) / Master Test Plan (scope-creep risk row retired, struck + superseded) / SHELL-7 UI Design Document (ASCII sketch line `[—]  UI Elements  ▸` removed) / Test Design (§43) / Test Cases (TC-SHELL-014 struck through + supersession note; TC-SHELL-027 corrected in place, 7-item → 6-item; new TC-SHELL-039 added, sidebar's `UI Elements` group absent asserted as an explicit P2 negative, not inferred from the TC-SHELL-027 correction alone) — numbered `0052`/`11.35`/`§43`/`TC-SHELL-039`, **renumbered from an initial `0049`/`11.32`/`§40`/`TC-SHELL-029` draft** (FRONTEND-1/SHELL-9/SHELL-10 each independently claimed the next-free values on `origin/main` while this branch was in flight; this branch's own initial claim collided with all three, retitled to next-free against post-merge main, **three real collisions same day** — confirmed `0052`/`11.35`/`§43`/`TC-SHELL-039` next-free against `origin/main` immediately before writing) — **docs-only, no code written this pass, per explicit instruction.** Implementation (sidebar drop, route drop, page-file deletion, `AppSidebar.test.tsx` fixture swap) follows in a subsequent commit on this same `remove-ui-elements` branch | Done |
| 11.36 | [ADR-0053](../adr/0053-tabler-install-phase-1-cdn.md) (Tabler v1.5.1 installed via CDN in `frontend/index.html` — Phase 1, install only, of a planned future AdminLTE→Tabler design-system swap; CDN chosen over the npm package per explicit CTO instruction; Tabler's `<link>` placed before AdminLTE/Bootstrap/Font Awesome's own imports so AdminLTE continues to win every conflicting selector, zero visible change to any existing screen; does not supersede ADR-0042; no backend/database/API change) + new Story DS-4 (added to the existing `design-system-component-stories.md` file) + propagation across Requirements (new FR-DS-3, new NFR-56, new traceability row)/WBS (new 6.14)/Database (reviewed, no schema impact)/API (reviewed, no API impact)/UI Design (reviewed, no UI change — zero visible delta by design)/Sitemap (reviewed, no route change)/Test Plan (2 new risk rows: dual-stylesheet cascade-order regression, CDN-vs-self-hosted-premise tension)/Test Design (§44)/Test Cases (new TC-DS-031/032, Design System / Shared Components 30/18 → 32/19) — numbered `0053`/`11.36`/`§44`/`NFR-56`/`TC-DS-031..032`, confirmed next-free against `origin/main` immediately before writing (0052/11.35/§43/TC-SHELL-039 already claimed by REMOVE-UI-1, merged). **The install itself (`frontend/index.html`'s 2 CDN tags) was already implemented and committed on branch `tabler-install` (commit `561bd93`) in a prior pass** — this row is the docs-propagation pass for that already-shipped, not-yet-merged code, per the CTO's own separate "write the plans and documents" instruction for this pass (no code written in *this* pass). | Done |
| 11.37 | [ADR-0054](../adr/0054-tabler-shell-migration-phase-2.md) (Tabler shell migration Phase 2, direct CTO instruction — "replace the sidemenu and header" with Tabler's own documented page-layout markup, confirmed scope/cascade-order via two `AskUserQuestion` rounds before implementation: (1) incremental direction commit, shell now/rest later, not a big-bang full-app swap; (2) cascade flips to Tabler-wins globally, CTO explicitly accepted the resulting repo-wide visual shift on unmigrated pages) — `AppSidebar`/`AppHeader`/`AppShell`/`AppFooter` rewritten to Tabler's `.page` flow layout + `navbar-vertical`/`navbar-nav`/Bootstrap-dropdown-group markup, replacing AdminLTE's `.app-wrapper` CSS grid + `sidebar-menu`/`nav-treeview` treeview; `frontend/index.html`'s Tabler `<link>` moved after `main.tsx`'s CSS imports; `sidebar-mini` icon-rail fold retired outright (no Tabler equivalent built this pass — tracked as a future-story candidate in ADR-0054's own Consequences); sidebar carries `data-bs-theme="dark"` directly (Tabler's per-navigation theme override) restoring FR-SHELL-5's "sidebar stays dark independent of the app-wide toggle" claim that AdminLTE v4's own skin-family removal had left unmet; `lte-theme` → `tabler-theme` localStorage key rename (second one-time reset, same trade-off ADR-0042 §4.3 already accepted once); **partially supersedes ADR-0042** (AdminLTE remains live for every other screen) + propagation across Requirements (new FR-DS-4, new NFR-57, FR-DS-3/NFR-56/FR-SHELL-1/2/4/5/7 revised in place, 2 traceability rows)/Database (reviewed, no schema impact)/API (reviewed, no API impact)/Sitemap (reviewed, no route change)/Test Plan (2 new risk rows: repo-wide visual-shift-mid-migration, sidebar-mini feature-reduction UX gap)/Test Design (§45)/Test Cases (TC-SHELL-004/015 revised in place — literal mechanism claim moved from AdminLTE body-classes/`bg-body-secondary` to Tabler's `#sidebar-menu` show-class/`data-bs-theme` attribute; TC-SHELL-022..024/028 struck through + supersession note, `sidebar-mini` feature retired; TC-DS-027/031 revised in place — literal claim inverted, AdminLTE→Tabler cascade winner; `shell7-sidebar-mini.spec.ts` deleted, feature it tested no longer exists) — numbered `0054`/`11.37`/`§45`, confirmed next-free against `origin/main` immediately before writing (0053/11.36/§44/NFR-56/TC-DS-031..032 already claimed by TABLER-1/DS-4, merged). Implementation + docs landed together on this same branch: `tsc --noEmit` clean, Vitest 70 files/475 tests green (worktree `npm ci` run fresh, no stale `node_modules`) | Done |
| 11.39 | [ADR-0056](../adr/0056-admin-4-generic-admin-crud-column-sort.md) (ADMIN-4 — column sort for the generic admin CRUD list, extends [ADR-0022](../adr/0022-generic-crud-router-factory.md)'s factory: `?sort=<field>`/`?sort=-<field>` on the generic `list` route via a new pure query-builder, `apply_sort`, 422 on an unknown/unsortable field name never reaching `getattr(model, ...)`; `FieldMeta.sortable` (default `true`) served as `sortable` on every field entry `GET /entities/{resource}/schema` already returns (ADR-0055) — the sort allow-list is computed from that same `derive_entity_schema` function at router-build time, not a second hand-kept list, closing the same class of drift ADR-0055 closed for field shape; frontend `EntityTable` renders a clickable `fa-sort`/`-up`/`-down` header when the caller passes `onSortChange`, `EntityListPage` owns the click-to-sort toggle (unsorted→asc→desc→unsorted, resets to page 1 on change) and turns it into the `sort` query param. `Release`'s 3 static fields marked `sortable: false` — its list route is 100% bespoke, never routes through `apply_sort`. Confirmed via grep before starting: the generic `list` route had zero `order_by`/`sort` reference anywhere) + propagation across Requirements (new FR-ADMIN-3, new NFR-60, new §2.8 paragraph, new traceability row) / ADR index (0056 row added) / Database (reviewed, no schema impact — query-param-driven `ORDER BY` over already-defined columns) / API (§1's Sorting convention split into its two non-interchangeable shapes — the pre-existing bespoke `Release` `?sort=&order=` and this new generic single-param `?sort=`/`?sort=-`; §3.1's schema example + derivation table gain `sortable`; §0 impact note) / UI Design (new "Column sort" subsection — 3-state icon table, click-cycle rule, `Release` exception) / Sitemap (reviewed, no route change) / Test Plan (3 new risk rows: router-build-time allow-list staleness across a dev-container restart, a `queryKey`-remount Vitest-test gotcha found writing the sort-toggle test, the pre-existing dual-sort-shape confusion risk this pass's own §1 split addresses) / Test Design (§47) / Test Cases (new TC-ADMIN-035..039, Taxonomy & Generic Admin CRUD 34/22 → 39/25, grand total 256/149 → **261/152** by direct column summation) — numbered `0056`/`11.39`/`§47`/`FR-ADMIN-3`/`NFR-60`/`TC-ADMIN-035..039`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind; 0055/11.38/§46/NFR-59/TC-ADMIN-034 the true current maxima). Implementation (`apply_sort`, `FieldMeta.sortable`, `EntityTable`/`EntityListPage` wiring, 7 backend-unit + 6 backend-integration + 5 frontend-Vitest tests) landed on this same `table-crud` branch across several commits before this docs pass; `tsc --noEmit` clean, Vitest 71 files/507 tests green, backend unit 467 passed, backend integration 312 passed/3 skipped/7 failed (all 7 independently reconfirmed non-regressions — 4 flaky under host CPU contention from concurrent sibling sessions' isolated stacks, reran clean alone; 3 the same already-documented topology quirks — bare `/health`, hardcoded LAN-IP MCP test) | Done |
| 11.38 | [ADR-0055](../adr/0055-admin-3-backend-driven-entity-schema.md) (ADMIN-3 — the backend becomes the single source of truth for admin-CRUD **field shape**, **partially superseding [ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md)**'s hand-written frontend `entityConfigs/<entity>.ts`: new per-entity route `GET /entities/{resource}/schema` serving `fields[]`+`methods`/`scopeField`/`scopeSelector`/`scopeResolution`/`searchFields`/`filterFields`; new backend `app/api/entity_registry.py` collecting every `CrudEntityConfig` instance — previously scattered uncollected across 7 route-cluster modules — keyed by the same plural `:entity` route slug the frontend already uses; hybrid derivation, not full auto-inference: type/required-ness/enum values auto-derived from Pydantic `model_fields`, FK ref-entity+label-field/enum badge colors/label overrides/`showInTable` declared explicitly in a new `CrudEntityConfig.field_meta` of `FieldMeta` dataclasses; frontend replaces its static import with a long-`staleTime` `useQuery`, all entities ported in one pass and their `entityConfigs/*.ts` deleted, no pilot subset, no fallback copy; navigation/routing/sidebar grouping deliberately stays frontend-static — this ADR narrows to field shape only, the actual drift surface. Triggering incident: `Requirement.title` shipped required on the backend under ADR-0025 on 2026-09-05 and the frontend form never learned about it, `422 title Field required` on every generic-admin create with no input to fix it from, found live 2026-09-09) + propagation across Requirements (§2.8 backend-driven-field-shape paragraph, new NFR-58/NFR-59, new traceability row) / ADR index (0055 row added, ADR-0027's own Status + index row marked `Partially superseded by 0055`) / Database (reviewed, **no schema impact** — a derivation over already-defined Pydantic schemas and existing `CrudEntityConfig` instances; no table, column, index or migration) / API (§0 impact note, §3 `GET /entities/{resource}/schema` subsection with request/response shape + derivation rules, §4 route-table row; **no asterisk-count footnote added** — see `docs/CLAUDE.md` on that convention's fragility at 13 existing markers) / UI Design (reviewed, no new screen — the same `EntityListPage`/`EntityFormPage`/`EntityTable`/`EntityForm` surfaces, sourcing their field shape differently, plus a new schema-fetch loading state) / Sitemap (reviewed, no route change — no frontend route is added, removed or renamed) / Test Plan (reviewed, no new risk row — the schema-fetch loading/latency and forgotten-`FieldMeta` risks are both already stated in ADR-0055's own Consequences and pinned by TC-ADMIN-029/034) / Test Design (§46) / Test Cases (TC-ADMIN-003/004's `entityConfigs`-mechanism clauses corrected in place, not struck — the behavior each asserts is still exactly true, only where the config lives changed; new TC-ADMIN-027..034; Taxonomy & Generic Admin CRUD 26/16 → 34/22, grand total 248/143 → **256/149** by direct column summation) — numbered `0055`/`11.38`/`§46`/`NFR-58`+`NFR-59`/`TC-ADMIN-027..034`, **renumbered from an initial `0053`/`11.36`/`§44`/`NFR-56`+`NFR-57` draft (2026-09-11)** — `main` had independently merged its own unrelated `ADR-0053`/`0054` (Tabler CDN install + shell migration, WBS `11.36`/`11.37`, `§44`/`§45`, `NFR-56`/`NFR-57`) while this branch was in flight, found merging into `main`; `TC-ADMIN-027..034` unaffected (no collision in that series, highest pre-existing was `TC-ADMIN-026`). The grand-total recompute above is against `main`'s own current 248/143 (post-Tabler-1/Tabler-2), not the stale 249/142 this row's own first draft assumed — the Taxonomy & Generic Admin CRUD row itself is untouched by either Tabler pass, so the same `+8`/`+6` delta applies, just against a different base. **Docs-only pass for renumbering** — the backend derivation mechanism (`crud_factory.derive_entity_schema`, `entity_registry.py`, `routes/entity_schema.py`), the `scopeSelector`/`scopeResolution` extension, the frontend port, and all three test layers landed on this same `table-crud` branch across several commits before this merge; `apply_sort`/column-sort (ADR-0053-independent, unrelated feature added same branch) not itself covered by this row | Done |

| 11.40 | [ADR-0058](../adr/0058-project-generic-admin-org-scoped.md) (`Project` added to `orgScopedEntities` — generic admin surface reachable at `/orgs/:orgId/admin/projects`, list/edit/delete via the pre-existing entity-agnostic `scope_field`/`useEntityScope` mechanism, no new backend/filtering concept; new `AppSidebar.tsx` `ORG_EXCLUDED_ENTITY_KEYS` keeps it out of the sidebar, mirroring the pre-existing `PROJECT_EXCLUDED_ENTITY_KEYS`, since ADR-0047's bespoke `ProjectsPage` stays the real nav path; no backend/schema/RBAC change) + propagation across Sitemap (org-scoped table gains a `projects` row, route-tree caption 8→9, entity-count note)/User Stories (SHELL-7's own "8 entities" ACs get a forward-pointing addendum, not a rewrite — accurate historical record of what that story delivered)/Test Design (§48)/Test Cases (TC-SHELL-025 revised in place — groups-plus-exclusions partition, not a flat 8-entity one, mirroring TC-SHELL-035's own discipline) — numbered `0058`/`11.40`/`§48`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind; 0057/11.39/§47 the true current maxima). `tsc --noEmit` clean, full Vitest 72/72 files, 510/510 tests (`AppSidebar.test.tsx` 27/27) | Done |
| 11.41 | [ADR-0059](../adr/0059-project-generic-admin-create.md) (generic admin surface's "New" button now works for `Project` — reuses the existing bespoke `POST /orgs/{org_id}/projects` (ADR-0017) unchanged via `entityConfigs/overrides.ts`'s `createPath` mechanism, `Release`'s own precedent; `create_schema` added to `_PROJECT_FACTORY_CONFIG` for schema-metadata purposes only, no competing route registered; a `post_create_hook` mirroring `post_update_hook` (EXEC-2/ADR-0038) was considered and rejected — the route's pre-insert 404/permission/inheritance logic doesn't fit a post-write hook shape; found and fixed a real gap where `useAdminRouteContext`'s `routeParams.orgId` was the raw, sometimes-`undefined` URL param rather than the resolved value the new `createPath` interpolation needs on the pre-existing project-scoped path; deliberately collapsed ADR-0017 Q3's `standards_profile` omitted-vs-explicit-null distinction into one case, since `EntityForm` cannot express "omit this key," only "send null") + propagation across API Design (§2's create-request description corrected in place)/Sitemap (`projects` row's create-column corrected)/ADR-0058 (in-place `### Amendment` — "create is absent by design" no longer holds)/Test Design (§49)/Test Cases (TC-PROJ-007 amended in place — asserts the opposite of its original claim, with its own docstring explaining why) — numbered `0059`/`11.41`/`§49`, confirmed next-free against `origin/main` immediately before writing. Verified against a live isolated stack (`testnexa-admincrud-test`, fresh DB clone from `main`): backend integration `test_projects.py` 18/18 (including the amended TC-PROJ-007 and the unaffected TC-PROJ-003/006/008), backend unit 467/467 (the 2 directly-touched files re-run alone 79/79); frontend `tsc --noEmit` clean, full Vitest 73/73 files/512/512 tests, including a new end-to-end test (`EntityListPage.projectsCreate.test.tsx`) proving the whole chain without mocking the pieces this ADR changed. Isolated stack torn down clean (`docker compose down -v`, confirmed via `docker ps`/`volume ls`/`network ls`) | Done |

| 11.42 | [ADR-0060](../adr/0060-projects-page-retired-generic-surface.md) (**supersedes [ADR-0047](../adr/0047-proj-4-projects-page-sidebar-entry.md)** — bespoke `ProjectsPage.tsx` [549 lines + its own 49-test Vitest suite] deleted outright; `/orgs/:orgId/projects` now renders the generic `EntityListPage`/`EntityFormPage` via a new `entityKeyOverride` prop [for a route with no `:entity` segment], same URL/nav item; backend `search_fields=("name",)` closes the search gap; new generic `EntityConfig.detailPath`/`detailLinkField` + `EntityTable` support restores the row-name-links-to-`ProjectDetail` navigation, reusably, not a one-off; new `/orgs/:orgId/projects/:id/edit` route + `AppBreadcrumb` pattern, `ProjectsPage`'s own edit was a modal with no route to need one. **Found and fixed a real, retroactive bug during live verification**: `useEntityScope`'s branch order checked a `scopeResolution` (present on `Project`'s config for its *other*, project-scoped admin route) before a directly-satisfiable plain `scopeField` — silently disabling scope resolution, and therefore the entire list query, on any route with `:orgId` but no `:projectId`, meaning ADR-0058's own org-scoped admin route had been broken since it shipped; reordered, with a new dedicated test using the real backend-shaped config no prior fixture matched) + propagation across Requirements (FR-PROJ-3/NFR-50 struck-and-superseded in place, FR-PROJ-4 unaffected-note added, new traceability row) / ADR index (0047 marked `Superseded by 0060`, its own Status line updated) / User Stories (PROJ-4's ACs get a forward-pointing addendum, not a rewrite) / Sitemap (`/orgs/:orgId/projects` row + new `:id/edit` row, `projects` admin-registry row's `ProjectsPage` reference corrected, route-tree branch) / Test Design (§50) / Test Cases (TC-PROJ-019/020/022/023/024 struck-and-superseded — coverage moved to the generic surface's own already-existing shared tests; TC-PROJ-021/025/026 unaffected; new TC-PROJ-027; Project & Release 25/12 → **21/11**, grand total by direct summation **257/151**) — numbered `0060`/`11.42`/`§50`/`TC-PROJ-027`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind; 0059/11.41/§49 the true current maxima). Verified against a live isolated stack (`testnexa-swap-test`, fresh clone from `main`): backend integration `test_projects.py` 18/18, a live `?q=`/schema/create/edit round trip via direct `curl`, and a full real-browser click-through (this is what surfaced the `useEntityScope` bug); frontend `tsc --noEmit` clean, full Vitest 73/73 files/496/496 tests; backend unit 467/467; the 2 edited Playwright specs (`project-create.spec.ts`, `ds2-table-container.spec.ts`) both pass clean individually (a same-batch run hit this repo's own already-documented host-CPU-contention flake on an unrelated, untouched test in the same file). Isolated stack torn down clean | Done |

| 11.43 | [ADR-0063](../adr/0063-mcp-integration-screen.md) (FR-MCP-4 — new "MCP Integration" screen at `/orgs/:orgId/mcp` and `/projects/:projectId/mcp`, one shared component [`McpIntegration`, mode resolved via `useResolvedOrgId()`, no `scope` prop]: client-connection docs [Claude Code, Codex CLI, Cursor, generic MCP-HTTP fallback — config syntax verified via live web search before writing, not guessed] + org-mode live `AIAgent` key issue/list/revoke; reuses ADR-0015's existing `create`/`revoke` routes unchanged, plus one new bespoke `GET /orgs/{org_id}/agents` list route [same human-only + 404-vs-403 + permission-check gate order, same "belongs to `org_id` via `acting_on_behalf_of_user_id`'s `OrgMembership`" resolution `revoke_agent` already established, `{items,total,page,page_size}` envelope per NFR-6]; permission code reused — `ai_agent.create`, not a new `.read` code — so no RBAC seed migration; sidebar nav item both modes, after "Members"/after "Overview") + propagation across Requirements (new FR-MCP-4, new NFR-61, new §2.10 paragraph, new traceability row) / ADR index (0063 row added) / User Stories (new Story MCP-4, human-persona counterpart to MCP-1..3's agent-persona stories) / UI Design Document (new, `2026-09-13-mcp-4-mcp-integration-screen-ui-design.md`) / Database (reviewed, no schema impact — `GET` route over the already-defined `ai_agent`/`actor` tables, no new column/migration) / API (§4 route-table row + a new §2 descriptive paragraph for `GET /orgs/{org_id}/agents`, `docs/CLAUDE.md`'s asterisk-footnote convention not disturbed) / Sitemap (2 new screen rows, org+project route tables, route-tree diagram, MCP-1's own "no Sitemap impact" note gets an explicit MCP-4 exception) / Test Plan (reviewed, no new risk row — same risk taxonomy MCP-1..3's own rows already cover, credential-issuance UX risk folded into ADR-0063's own Consequences instead) / Test Design (§51) / Test Cases (new TC-MCP-010..015, AI Agent / MCP 7/0 → **13/1**, grand total by direct summation **263/152**) — numbered `0063`/`11.43`/`§51`/`FR-MCP-4`/`NFR-61`/`TC-MCP-010..015`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind; 0062/11.42/§50/NFR-60/TC-MCP-009 the true current maxima). Implementation (backend list route + 4 integration tests, frontend `lib/api/agents.ts` + shared `McpIntegration` screen + key-management panel + 7 Vitest tests + 2 new routes + sidebar nav items in both modes + `AppSidebar.test.tsx` ordering assertions corrected in place, 1 new e2e spec) landed on this same `worktree-mcp-integration` branch before this docs pass. **Same-day CTO manual-verification pass found and fixed 3 real bugs, all same branch, unmerged** (ADR-0063's own `### Amendment`; light-mode code-block contrast and a form-row alignment bug, both CSS-only; the `acting_on_behalf_of_user_id` over-broad-picker + accidental-hard-block gap, TC-MCP-015) — all fixed and re-verified live, not deferred. Verified against a live isolated stack (`testnexa-mcpint-test`, fresh DB clone from `main`): backend integration 320 passed/3 skipped/3 known-harmless topology failures (bare `/health` ×2, hardcoded LAN-IP MCP test — all three re-confirmed via `-v`, not carried from memory), backend unit 467/467 (unaffected by the frontend-only fixes, not rerun a second time since `git diff -- backend/` after the amendment was empty); frontend `tsc --noEmit` clean, full Vitest 77/77 files/521/521 tests; the e2e spec (`mcp1-integration-page.spec.ts`) passes clean 3 times across the original + amendment passes plus 3 pre-existing sidebar-touching specs (`shell-nav.spec.ts` ×2, `auth-agents.spec.ts`, `shell9-project-nav-context.spec.ts` ×2) unaffected; two throwaway Playwright scripts (deleted after use, per `e2e/CLAUDE.md`'s own convention) confirmed the CSS fixes and the admin/non-admin picker split live, in a real browser, against two separately-seeded demo accounts (one with `org_membership.read`, one without); cleanup script confirmed leaving zero orphan rows via direct `psql` count. Isolated stack left running for a manual CTO handoff pass, not yet torn down | Done |
| 11.44 | [ADR-0064](../adr/0064-dash-3-dashboard-org-list-and-chooser.md) (DASH-3 — resolves ADR-0035's own deferred "org-scoped or global" question for `/dashboard`: stays global, fetches `GET /auth/me/orgs` fresh on every mount [no backend change, reuses SHELL-6/ADR-0036's existing route], branches on count — 0 orgs → empty state + "Create organization" CTA, exactly 1 org → immediate `navigate()` to `/orgs/{id}` with no intermediate render, 2+ orgs → "Select an organization" card list; `/orgs/pick`/`OrgPicker.tsx` retired outright, post-login/signup/accept-invite redirect always targets `/dashboard` now, which owns all org-count branching in one place instead of split across `Login.tsx`'s own effect and a separate screen. Does **not** resolve the separate FR-DASH-2/ADR-0039 "Dashboard" naming overlap [NFR-49 unchanged] — only incidentally softens it for the 2+-org case, whose own heading is "Select an organization," not the literal word "Dashboard") + propagation across Requirements (FR-DASH-1/NFR-47 partially superseded in place, new FR-DASH-3/FR-DASH-4/NFR-62/NFR-63, new traceability row) / ADR index (0064 row added) / Database (reviewed, no schema impact) / API (reviewed, no API impact — `GET /auth/me/orgs` reused unchanged, `Dashboard` gains a new caller of an already-documented route) / UI Design (new DASH-3 UI Design Document — 3 states: 0/1/2+ orgs) / Sitemap (`/dashboard` row description corrected, `/orgs/pick` row struck-and-superseded, route-tree caption updated) / Test Plan (3 new risk rows: retired-route test-fixture cross-references, single-org auto-redirect racing the boot spinner, 0-org create-CTA correctness) / Test Design (§52) / Test Cases (TC-DASH-005 struck-and-superseded; TC-DASH-003/006 corrected in place for the org-count-dependent settle target; TC-AUTH-003/004 corrected in place — mechanism note, outcome unchanged; TC-SHELL-001/005/008/009 corrected in place — `/orgs/pick` example replaced with `/dashboard`, same "no `:orgId`" precondition; new TC-DASH-007..013; **pre-existing drift found and corrected**: the Dashboard & Root Redirect row's own stated P1 count [3] disagreed with the actual 4 P1 rows among TC-DASH-001..006 — corrected to 6/4 before applying this pass's own delta; Dashboard & Root Redirect 6/4 → **12/8**, grand total by direct column summation **269/157** once merged alongside MCP-4's own AI Agent/MCP delta) — numbered `0064`/`11.44`/`§52`/`FR-DASH-3`/`FR-DASH-4`/`NFR-62`/`NFR-63`/`TC-DASH-007..013`, **renumbered from an initial `0063`/`11.43`/`§51`/`NFR-61`/`62` draft (2026-09-14)** — `main` had independently merged its own unrelated `ADR-0063`/WBS `11.43`/`§51`/`NFR-61` (MCP-4, row above) while this branch was in flight, found via `git fetch` merging into `main`. **Docs-only pass, no code written this pass, per explicit CTO instruction** — implementation is a follow-on step. | Done |
| 11.45 | [ADR-0065](../adr/0065-mcp-5-full-crud-all-entities.md) (MCP-5 — **partially supersedes [ADR-0033](../adr/0033-mcp-server-architecture.md)'s own MCP-2/MCP-3 hand-wired-tool backlog**: full list/get/create/update/delete across every entity the generic CRUD factory [ADR-0022] and the bespoke mutating routes [API Doc §4] serve, restricted to exactly the methods REST already grants each entity — 5 reflective tools [`list_entities`/`get_entity`/`create_entity`/`update_entity`/`delete_entity`] + `describe_entity` [reuses ADR-0055's `derive_entity_schema`], dispatched from one new registry [`app/mcp/tool_registry.py`] both the MCP and REST surfaces read [`ALL_ENTITY_CONFIGS` + ~13 declared bespoke-route rows], so a future route/method change needs one registry edit, not a parallel MCP-side one; `crud_factory.py`'s `list` closure refactored off raw `Request.query_params` into a plain-arg function shared by the REST route and `list_entities`. **Found and fixed a real, pre-existing defect blocking the story outright**: the generic factory's own `_fetch_and_gate`/`_resolve_scope_for_write` gates never adopted `_actor_membership_exists` [defined in the same module, unused] — every one of the 27 generic-factory entities had 404'd unconditionally for any `AIAgent` caller since ADR-0022 shipped, invisible until this story's own planning traced the two gates' actual call sites; fixed with no REST-visible behavior change for a `User` actor, regression-tested. New standing rule added to `backend/CLAUDE.md`: any new bespoke mutating route's registry row lands in the same commit as the route itself) + propagation across Requirements (new FR-MCP-5, new NFR-64/NFR-65, new §2.10 paragraph, corrective forward-note on NFR-43 — it described the bespoke routes only, never the generic factory, so it stays true as written rather than struck, new traceability row) / ADR index (0065 row added, ADR-0033's own Status line amended to point here — forward-pointing note, its Decision/Consequences text untouched) / User Stories (new Story MCP-5) / Database (reviewed, no schema impact — this story is a transport-layer/gate-correctness change over already-defined tables) / API (§6 MCP tool-surface table replaced with the 6 reflective tools + the registry's bespoke-row list, one new paragraph on the `_actor_membership_exists` fix) / UI Design (reviewed, no UI impact — backend/MCP-only, no screen touched) / Sitemap (reviewed, no impact — same "MCP is not a frontend route" reasoning MCP-1's own note already established, one line added noting MCP-5 as a second confirmed instance) / Test Plan (2 new risk rows: the `_actor_membership_exists` fix regressing human-caller behavior; the broad new attack surface of exposing every entity's CRUD via one registry needing per-entity method-gating coverage, not just a representative subset) / Test Design (§53) / Test Cases (new TC-MCP-016..023, AI Agent / MCP 13/1 → **21/3**, grand total by direct summation **277/159**) — numbered `0065`/`11.45`/`§53`/`FR-MCP-5`/`NFR-64`/`NFR-65`/`TC-MCP-016..023`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind after rebasing this worktree onto `origin/main`, which had independently merged DASH-3/MCP-4's own `0064`/`11.44`/`§52`/`NFR-62`-`63` in the interim; `0064`/`11.44`/`§52`/`NFR-63`/`TC-DASH-013` the true prior maxima). **Docs-only pass, no code written this pass, per explicit CTO instruction** — implementation is a follow-on step. **Addendum (2026-09-14, same branch, follow-on commit):** implementation landed — `_actor_membership_exists` fix, `app/mcp/tool_registry.py`, `app/mcp/tools/generic_crud.py`, `app/mcp/server.py` registration, all 8 new TCs' automated tests (backend unit + integration + one real-MCP-SDK-client e2e test), plus 2 pre-existing MCP-1 tests corrected in place (their own exact-tool-count assertions were a stale negative once MCP-5's 6 tools joined the registry deliberately, not a leak). Verified against the isolated stack this branch's own worktree stood up (`testnexa-mcp5-test`, ports `47215`/`47216`, DB cloned from `main`): backend unit 485/485, backend integration 326 passed/3 skipped/3 pre-existing known-harmless topology failures (2× bare `/health` via nginx, 1× hardcoded-wrong-LAN-IP MCP test — all pre-existing, unrelated, re-confirmed live not carried from memory). Frontend/e2e (Playwright) untouched — `git diff --stat -- frontend/ e2e/` empty, zero UI surface per this ADR's own Decision, so no frontend test layer applies. | Done |
| 11.46 | [ADR-0066](../adr/0066-admin-5-seed-test-level-catalog.md) (ADMIN-5 — seeds the `test_level` global-catalog table with the 5 ISTQB CTFL v4.0.1 test levels [Component Testing, Component Integration Testing, System Testing, System Integration Testing, Acceptance Testing] via a new idempotent Alembic data migration, existence-checked by `name` against the table's own pre-existing unique constraint, symmetric `downgrade()` deleting exactly those 5 rows by name — closes FR-ADMIN-1 AC1's own pre-existing "seeded via Alembic data migration" claim for `TestLevel` [root `CLAUDE.md` had flagged this as an open product decision since 2026-09-06]. `TestType`/`TestDesignTechnique` remain unseeded, deliberately out of scope — no canonical vocabulary decided for either yet. No schema/API/UI change — the pre-existing `GET /test-levels` route and the `TestCase` create-form dropdowns [`ProjectDetail.tsx`, both the direct and `TestCondition`-mediated create flows] simply stop being empty) + propagation across Requirements (annotation on FR-ADMIN-1's own paragraph, new NFR-66, new traceability row) / ADR index (0066 row added) / Database (new seed-data paragraph, §3.10) / API (reviewed, no shape impact — `GET /test-levels` unchanged, now returns 5 rows instead of 0) / UI Design (reviewed, no impact — no new screen, existing dropdowns unchanged) / Sitemap (reviewed, no impact — no new route) / Test Plan (reviewed, no new risk row — same idempotent-seed risk shape NFR-20/RBAC-4 already covers) / Test Design (§54) / Test Cases (new TC-ADMIN-040..042, Taxonomy & Generic Admin CRUD 39/25 → **42/25**, grand total by direct summation **280/159** — this pass also corrected the coverage-summary table's own stale printed **Total** row, which had drifted to 269/157 sometime after MCP-5's own addendum despite every row above it already reflecting MCP-5's 277/159, per `docs/CLAUDE.md`'s own "recompute by direct summation, don't trust the running number" note) — numbered `0066`/`11.46`/`§54`/`NFR-66`/`TC-ADMIN-040..042`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — 0 commits behind; `0065`/`11.45`/`§53`/`NFR-65`/`TC-MCP-023` the true prior maxima). **Docs-only pass, no code written this pass, per explicit CTO instruction** — implementation (the migration itself, all 3 TCs' automated tests) is a follow-on step. | Done |
| 11.47 | [ADR-0067](../adr/0067-testtype-catalog-default-seed.md) (`TestType` catalog gets a permanent default seed — 5 ISTQB CTFL v4.0.1-aligned rows [`Functional Testing`, `Non-functional Testing`, `Black-box Testing`, `White-box Testing`, `Confirmation Testing`] via one new idempotent Alembic data migration [`854917c76ac5`, existence-checked by `name`, same pattern as `34053c46f9fc_seed_rbac_system_roles.py`; `downgrade()` deletes only these 5 by name] — resolves the sibling half of the decision ADR-0066/ADMIN-5 [row above] resolved for `TestLevel`, drafted independently and reconciled at merge time, not a duplicate. No schema/API/UI/RBAC change — `TestType` already had full CRUD via the generic factory [ADR-0022]) + propagation across Database (§3.10 addendum) / ADR index (0067 row added, renumbered from an initial `0066` draft — ADMIN-5 collision) / Test Design (§55) / Test Cases (new TC-ADMIN-043, Taxonomy & Generic Admin CRUD 42/25 → **43/26**, grand total by direct summation **281/160**) / Test Plan (new risk row — seed-migration idempotency + custom-row survival, mirrors RBAC-4's own precedent row) — numbered `0067`/`11.47`/`§55`/`TC-ADMIN-043`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — merged ADMIN-5's own `0066`/`11.46`/`§54`/`TC-ADMIN-040..042` in first, the true post-merge maxima). Requirements gets a new `NFR-67` (`TestType` seed-migration idempotency, mirroring ADMIN-5's own `NFR-66`) + an addendum on FR-ADMIN-1's own text + a new traceability row — no other doc (API/UI-Design/Sitemap) applies, reviewed, none touched (no route/schema/screen change). **Alembic chain note**: this branch's own migration (`854917c76ac5`) and ADMIN-5's (`63f8478c1c12`) had both independently chained off the same prior head (`f19a7c3e5b62`), which would have produced two Alembic heads on merge — re-pointed this branch's migration's `down_revision` to `63f8478c1c12` (ADMIN-5's, now the real current head) before merging, keeping a single linear chain (`alembic heads` confirmed exactly one after the fix). **Verified against a fresh isolated stack** (`testnexa-testtype-test`, rebuilt post-merge, ports `47201`/`47202`/`47203`, DB cloned from `main` [already at ADMIN-5's own head with `TestLevel` seeded], confirmed reachable via both `localhost` and the host's LAN IP): both migrations apply cleanly in sequence, all 5 `TestType` rows present alongside the 5 `TestLevel` rows, no interference between the two catalogs' seeds. **Fixed a real vacuous-assertion bug in this branch's own idempotency test**, found via ADMIN-5's own retro (`backend/CLAUDE.md`'s "`alembic upgrade head` is a no-op once already at the target revision" write-up) — the original shelled out to the CLI a second time, which never re-enters the migration's own `upgrade()` body once already at head, so the assertion held regardless of whether the migration's insert-existence-check logic actually worked; rewritten to invoke `upgrade()` directly via a real `Operations` context (ADMIN-5's own documented fix pattern), twice, bypassing Alembic's revision-tracking short-circuit entirely — reconfirmed live. Backend unit 493/493 (full suite, includes ADMIN-5's own 4 + this branch's own 4). Backend integration re-run scoped to related files only, not the full suite, per this task's own "only run related testcases" instruction: `test_admin_testtype_seed.py` (3/3), `test_admin5_seed_test_level.py` (3/3), `test_rbac_seed.py` (7 passed/1 skipped — TC-RBAC-019's own documented FK-safety skip guard, this DB has real `RoleAssignment` rows) — 13 passed/1 skipped total. e2e (`testtype-seed-catalog.spec.ts` + `admin5-test-level-seed.spec.ts`, `--workers=1`): both failed on the first combined run (`page.goto`/dropdown-row timeouts) — `docker stats` showed 3 other sibling isolated stacks plus unrelated containers contending on the host at the time; both pass clean rerun alone with headroom (47.6s/25.0s respectively, well past the plain default but not stuck), confirming host contention per this repo's own already-documented class, not a regression. | Done |
| 11.48 | [ADR-0068](../adr/0068-mcp-6-per-entity-mcp-tools.md) (MCP-6 — **partially supersedes [ADR-0065](../adr/0065-mcp-5-full-crud-all-entities.md)'s Decision §1 tool *shape* only; its registry, plain-arg `list` refactor and `_actor_membership_exists` fix are untouched and are what this builds on — and **retires [ADR-0033](../adr/0033-mcp-server-architecture.md)'s 2 MCP-1 hand-wired tools**). The MCP surface becomes one tool per entity per supported action, `tn_<entity>_<action>` — **146 tools** generated at import time by a new `app/mcp/tools/entity_tools.py` walking `TOOL_REGISTRY`, no `resource` argument on any of them, each signature carrying only that action's real params. ADR-0065's 6 reflective tools and MCP-1's `create_test_case`/`list_test_cases` are **deleted, not aliased** (`tools/generic_crud.py` + `tools/test_cases.py` removed); `test_case` folds in as `tn_test_case_create` (gains REQ-3's rigor path) + `tn_test_case_list` (new bespoke registry row wrapping the nested `GET /requirements/{id}/test-cases`). Action set per entity = `full_methods` (ADR-0055), **not** `methods`, plus declared `BESPOKE_EXTRA_ACTIONS` — so "never more than REST" is enforced by omission from `tools/list`, not a `405` at dispatch. `TOOL_REGISTRY` re-keyed plural-hyphenated → singular snake_case `resource`. New diff-based completeness test (`tests/unit/test_mcp_tool_naming.py`). TC-MCP-017/TC-MCP-022 genuinely corrected (see ADR Consequences). No REST/database/schema/RBAC/frontend change. Backend unit 530/530, integration 328 passed against a live isolated stack. Numbered `11.48`/`0068`/`§56`/`NFR-68` — a first-choice `11.47`/`0067`/`§55`/`NFR-67` was drafted against `testlevel-seed`/`testtype-seed`'s in-flight state, but `testtype-seed` merged those exact numbers to `main` first (its own renumbered `0066`→`0067`); caught by this branch's own pre-merge numbering-collision recheck, renumbered here) | Done |
| 11.49 | [ADR-0069](../adr/0069-req-5-standalone-test-case-with-optional-requirement-link.md) (REQ-5 — `TestCase` gains a new nullable, direct `project_id` FK, the same scope shape `TestSuite` already has; `_TEST_CASE_CONFIG` unrestricted for exactly this shape [`list`+`create` enabled via the generic factory, `scope_field="project_id"`, new `CreateStandaloneTestCaseRequest` — no atomic link-table write needed, a standalone case has nothing to link]; `resolve_test_case_org_id` gains a 4th branch, `project_id`, inserted third — after both link-table branches, before the `TestSuiteTestCase` fallback; new bespoke retrofit route `POST /test-cases/{id}/link-requirement` lets a standalone case gain a `RequirementTestCaseLink` after creation, `404` cross-org / `422` cross-project / `409 already_linked_to_requirement` if the case already has any Requirement traceability. Closes a real, already-shipped-but-dead sidebar entry: the project-mode "Test cases" nav item (ADR-0051) has pointed at `/projects/:projectId/admin/test-cases` since that story, but the route has been a "Listing is not available for this entity" dead end the whole time — this story makes it work with zero frontend route change. No unlink route, no retrofit-to-`TestCondition`, no RBAC migration [`test_case.create`/`.read` already granted to every role that needs them since ADR-0028] — all explicit CTO scope decisions) + propagation across Requirements (new FR-REQ-5, new NFR-69, new §2.4 narrative paragraph, new traceability row) / ADR index (0069 row added) / Database (§3.6 `TestCase` section: new `project_id` column, resolver rewritten to 4 branches, factory-posture paragraph corrected in place, new retrofit-route paragraph) / API (§3 `TestCase` footnote corrected in place — `list`/`create` no longer both absent; §3's per-entity resolver-depth table row updated; new §4 bespoke-route table row, `link-requirement`) / UI Design (new REQ-5 UI Design Document — the project-scope generic admin surface's `test-cases` page starts rendering real content, `EntityFormPage`'s `test-case` edit page gains a conditional "Link to Requirement" action) / Sitemap (`test-cases` admin-surface row corrected in place — no longer "no create on this generic-admin page") / Test Plan (new risk row — resolver branch-ordering ambiguity once a case can carry both `project_id` and a later Requirement link) / Test Design (§57) / Test Cases (new TC-REQ-015..019, Requirement & Test Case Authoring 9/7 → **14/10**, grand total by direct summation, reconciled with MCP-6's own row **290/164**) — numbered `0069`/`11.49`/`§57`/`FR-REQ-5`/`NFR-69`/`TC-REQ-015..019`. **Renumbered from an initial `0068`/`11.48`/`§56`/`NFR-68` draft (2026-09-15)** — `origin/main` had independently merged MCP-6's own unrelated `ADR-0068`/WBS `11.48`/`§56`/`NFR-68` (row above) while this branch was in flight, found via `git fetch` merging main into this branch. **Docs-only pass, no code written this pass, per explicit CTO instruction** — implementation (the migration, the config/resolver/route changes, the frontend "Link to Requirement" action, all 5 TCs' automated tests) is a follow-on step. **Addendum (2026-09-14, same branch):** implementation landed (commit `8866d4e`) — migration + resolver 4th branch (plus a fix found while implementing it: the scope-resolution call passes a `types.SimpleNamespace` with no `.id`, which the original branch order would have 404'd on) + both new routes + MCP registry + the frontend section, plus 2 pre-existing tests corrected in place (their own literal claims — "`test-cases` has no `list`" — this ADR deliberately invalidated). Coverage re-verified literal-wording-against-test-file for all 5 new TCs, no gaps found. Verified against a fresh isolated stack (`testnexa-req5-test`, ports `48213`/`48214`/`48215`, DB cloned from `main`): backend unit 498/498, backend integration 344-346/346 (2 pre-existing, unrelated known-flaky/broken tests re-confirmed by rerunning alone — a login-throttle timing test, the already-documented hardcoded-LAN-IP MCP test), frontend `tsc --noEmit` clean + 538/538 Vitest, new e2e spec 3/3 clean reruns + req2/req3/req4/admin2-generic-crud e2e (9/9) rerun to confirm no regression. **Addendum 2 (2026-09-14→15, same branch):** live-manual-test follow-ups landed — `TestCase.description` field (all 3 create paths + update), `test_level_id`/`test_type_id`/`test_condition_id` render as native `<select>` dropdowns (`FieldMeta.select`, new `FkSelect` molecule), `description` promoted to a new `FieldConfig.type: "text"` (`FieldMeta.long_text`) rendered as a `<textarea>` (new `Textarea` atom), `FkSelect` switched to the shared `Select` atom for its small-variant default. No new ADR (field/UX completeness fix, not a new architecture decision). Backend 504 unit + 111 integration, frontend 548 Vitest + `tsc` clean, e2e green. **Merge reconciliation (2026-09-15):** this row and MCP-6's own row above diverged before either landed — renumbered per this row's own note above; the MCP tool registry (`tool_registry.py`) conflict was resolved by porting this story's `_test_case_create`/`_test_case_link_requirement` additions onto MCP-6's new per-entity-action registry shape (`_BESPOKE_EXECUTORS`/`BESPOKE_EXTRA_ACTIONS`), adding `test_case_link_requirement` as a new 100%-bespoke pseudo-resource (same posture as `test_suite_test_case`/`test_plan_test_suite`) generating `tn_test_case_link_requirement_create`; `test_mcp5_generic_crud.py` (unit) deleted per MCP-6's own architecture retirement, its one REQ-5-specific coverage claim already carried by `test_mcp_entity_dispatch.py`'s existing `test_test_case_list_is_registered_as_a_bespoke_nested_list`, with `test_case_link_requirement` coverage added fresh to that file and `test_mcp_tool_naming.py`'s `_EXPECTED_CONFIGLESS`/tool-count anchors updated to include it. A fourth independent instance of the stale-Total-row class was found and fixed during this reconciliation (Test Cases doc, unrelated to either branch's own change): AI Agent / MCP was undercounted at 23/4, the true count is 25/4 — see that doc's own reconciliation note. **A real functional gap was also found and fixed, not just a doc/test-naming one**: `_BESPOKE_EXECUTORS["test_case"]["list"]` (MCP-6's own bespoke row, wrapping REQ-2's nested `GET /requirements/{id}/test-cases`) unconditionally overrides whatever `_build_generic_registry` would otherwise register for `test_case`'s own `list` — meaning REQ-5's newly-enabled generic `list` (`GET /test-cases?project_id=...`, the standalone-authoring path) would have been silently unreachable over MCP the instant both branches combined, even though REST itself serves it correctly; a naive "resolve the conflict, keep both bespoke functions" merge would not have caught this, since neither branch's own test suite (each written in isolation) ever exercised the combination. Fixed by making `_test_case_list` a dispatcher over both list routes (branching on whether `scope` carries `requirement_id` or `project_id`), the same "one tool per entity per action" shape `_test_case_create` already established for three create paths — plus `_scope_hint`'s own generated-description text extended to name both scope options, not just the bespoke one, so an MCP client's `tools/list` reading doesn't have the same blind spot the dispatcher almost did. Caught by `test_mcp_tool_naming.py`'s own pre-existing description-completeness assertion actually failing on this exact gap, not by manual inspection. Backend unit re-run: 551/551 passing after the fix. | Done |
| 11.50 | [ADR-0070](../adr/0070-search-fields-audit-and-numeric-cast.md) (SEARCH-1 — both halves of what [ADR-0022](../adr/0022-generic-crud-router-factory.md)'s opt-in `?q=` design always implied. **(a) Mechanism:** `apply_filters_and_search`'s `?q=` clause-builder extracted into a new `_search_clause(model, column_name, search_term)` helper in `app/api/crud_factory.py`; a numeric column — `isinstance(column.type, (Integer, Numeric))`, complete by subclassing since `SmallInteger`/`BigInteger` subclass `Integer` and `Float` subclasses `Numeric`, so no enumerated list to drift — is now `cast(column, String).ilike(f"%{term}%")`; string columns are byte-for-byte unchanged. Not a cosmetic nicety: Postgres has **no `integer ~~* unknown` operator at all**, so an un-cast `ILIKE` against an int column is a hard `ProgrammingError` at query time, not a silently-empty result — numeric columns were therefore *structurally unusable* in `search_fields` before this, which is why every pre-existing tuple in the repo happens to be string-only (a constraint, not a style choice anyone recorded). Semantics are substring-on-rendered-digits, `?q=1` matching `sequence` 1/10/21 alike — deliberate, per the ADR's own Alternatives: one `?q=` param cannot carry two match semantics across a mixed-type tuple without the caller knowing each column's type, and `filter_fields` already serves exact match. **(b) Per-entity audit:** `search_fields` goes from **4 to 19** of the 27 registered `CrudEntityConfig`s. New: `test_plan` (`identifier`/`scope`/`approach`/`staffing_and_training`/`schedule` — **the originally reported bug**, its search box silently returned the unfiltered list), `test_step` (`action`/`expected_result`/**`sequence`**, the repo's first numeric search field), `test_condition`, `test_suite`, `test_execution`, `risk_item`, `attachment`, `entry_exit_criteria`, `environment`, `test_cycle`, `role`, `permission`, `test_design_technique`, `test_level`, `test_type`; extended: `test_case` **+`description`** (ADR-0069 added the column after the tuple was written), `project` **+`standards_profile`**; unchanged: `requirement`, `defect`. **8 entities keep none, each with a written reason rather than a silent absence**: `test_log`/`org_membership`/the 4 traceability link tables have **zero free-text columns** (all FK/UUID/enum/timestamp; `TestLog`'s only payload is a JSONB blob `ILIKE` cannot meaningfully address) — a structural, durable reason, which is why TC-ADMIN-013's corrected negative half now names `test_log`; `organization`/`role_assignment` have **no generic `list` route** (`methods` excludes `"list"`), so a tuple would be dead config `?q=` could never reach. Two deliberate column-level exclusions inside otherwise-searchable entities: `Attachment.size_bytes` (a `BigInteger` the new cast *could* handle — excluded because a byte count is a measurement, not an identifier; substring-matching its digits, `"1024"` also hitting `10240`, answers no real question, where `TestStep.sequence` is the opposite case, a short human-quoted ordinal) and `Defect.status`/`Approval.role` (plain `String` but closed vocabularies — `Defect.status` already exact-matchable via `filter_fields`). Secret-bearing columns (`password_hash`/`key_hash`/`key_prefix`/`token_hash`) need no exclusion rule at all — their models have no `CrudEntityConfig`, so there is no tuple to add them to; stated explicitly so a future auditor doesn't re-derive the absence. No schema/migration/route/query-param/RBAC/frontend change — `?q=` was already wired end-to-end by ADR-0022 + ADR-0055's schema-driven admin surface) + propagation across Requirements (NFR-32 **amended in place** — its "(`ILIKE` across those columns)" parenthetical is now only half-true, so it carries an inline ADR-0070 amendment naming the cast while keeping its original ADR-0022 reference; new NFR-70 for the numeric-search capability; new traceability row) / ADR index (0070 row added, trailing **Date:** line extended) / API (§3's `?q=` contract bullet **amended in place** — it stated the compiled form as plain `OR`-joined `ILIKE '%term%'`, now qualified with the numeric cast; §3's `Requirement` worked example needed no change, its tuple is unchanged and string-only) / Test Design (§58) / Test Cases (**TC-ADMIN-013 corrected in place** — its precondition and expected-result named `TestSuite` as the no-`search_fields` example, factually wrong as of this story since `TestSuite` now has `("name","purpose")`; re-pointed at `test_log`, verified to have a real generic `list` route *and* zero free-text columns, so the claim is durable rather than another entity one story away from invalidating it — no row-count change; new TC-ADMIN-044..046, Taxonomy & Generic Admin CRUD 43/26 → **46/28**, grand total by direct summation **293/166**) — numbered `0070`/`11.50`/`§58`/`NFR-70`/`TC-ADMIN-044..046`, confirmed next-free against `origin/main` immediately before writing (`git fetch` — exit 0, 0 commits behind; `0069`/`11.49`/`§57`/`NFR-69`/`TC-ADMIN-043` the true prior maxima). Database / UI Design / Sitemap / Test Plan reviewed, **N/A, none touched** — no schema or column change (pure query-building), no new or changed screen (the generic admin search box is pre-existing and unchanged), no new route or param (`?q=` already documented), and no new risk class (the `ILIKE`-scan performance shape is the one ADR-0022 already carries, applied to more entities). **Docs-only pass — the code changes described above landed separately and are not part of this pass; implementation and all 3 new TCs' automated tests are handled outside it.** | Done |
| 11.51 | [ADR-0071](../adr/0071-entity-table-column-preferences.md) (COLPREF-1 — per-entity column visibility + ordering on the generic admin entity list, persisted client-side. New "Columns" button in `EntityTable`'s own `.card-header .card-tools` [beside the existing search box and the caller-supplied `headerActions`, the same flex row ADMIN-4's header affordances already use — no new header region] opens a new `ColumnPreferencesModal` organism [`components/organisms/column-preferences-modal/`, per ADR-0043's tiering] listing every field the served schema exposes as a table column — the `showInTable !== false` set read from `GET /entities/{resource}/schema` [ADR-0055], the *same* source the rendered columns themselves come from, never a second independently-derived list, so this control can only ever narrow the served set, never widen it. Per field: a checkbox [show/hide] and Up/Down buttons [reorder], plus a "Reset to defaults" action that *clears* the stored preference outright rather than writing a preference equal to today's default [the distinction matters once the schema later changes]; first row's Up and last row's Down disabled. **Up/Down, not drag-and-drop** — no drag library is a frontend dependency today and adding one is its own dependency decision this story explicitly declines, and two `click`s are directly assertable in Vitest where a pointer-event harness is not. Persistence is `localStorage`, keyed per entity as `testnexa.column-prefs.<config.resource>` [the snake_case permission-code resource slug already on `EntityConfig`, so `requirement` and `test_case` can never collide], stored as `{"v":1,"order":[fieldName,...],"hidden":[fieldName,...]}`, with **every** read and write `try`/`catch`-wrapped — a private window, blocked/cleared site data, a quota failure, `null`, unparseable JSON or an unrecognised `v` all degrade to config defaults rather than throwing [NFR-71; there is no error boundary on this route, so an uncaught render exception here would take the whole page down]. **`EntityTable` owns this state itself**, deliberately unlike `page`/`pageSize`/`sort`/`filters`/`search`, which `EntityListPage` owns and which ADR-0056 had just established as the precedent: those four each map to a backend query parameter and therefore belong to the component owning the list query, whereas column visibility/order maps to nothing server-side [the same rows come back either way] and affects only what this one component paints — so owning it here means every current and future `EntityTable` caller gets it with zero prop plumbing, and **`EntityListPage.tsx` needs no change at all for this story**; in-repo precedent for a component owning its own `localStorage`-persisted preference is `AppHeader`'s color mode [ADR-0054]. Two locked [always-visible] classes, a deliberate judgment call documented as such: a config's `detailLinkField` when `detailPath` is set [ADR-0060 — `Project.name` is the only instance today; that cell carries the only navigation into the entity's real detail workspace, so hiding it strands the viewer on a list they cannot click through] and whichever field is the *last remaining visible* one [so an admin cannot reduce the table to an actions-only column] — both render as a **disabled, checked** checkbox rather than being silently absent from the modal's list, and the apply layer force-restores a locked field even when a stale or hand-edited stored preference marks it hidden. Locking governs visibility only; a locked field is still freely reorderable. Forward/backward-compatibility merge rules, applied every time a stored preference meets the live schema: a stored name the schema no longer serves is ignored; a schema field absent from the stored `order` is **appended** in config order [so an entity that gains a column later shows it rather than silently dropping it for everyone who ever opened the modal — the failure mode a naive "stored order is the order" implementation quietly has]; a corrupt/unparseable/wrong-`v` value falls back to config defaults wholesale rather than partially applying. Pure preference-application logic split into `frontend/src/lib/columnPreferences.ts` [`loadColumnPreferences`/`saveColumnPreferences`/`clearColumnPreferences`/`applyColumnPreferences`/`lockedFieldNames`, plus the key builder and parse/merge helpers they compose] — the merge/ordering/locking rules are where the real risk sits and are all decidable from plain data, so they are unit-testable with no DOM. **Explicitly out of scope, not deferred by omission**: no backend change of any kind [no route, no schema field, no migration, no permission code], no server-side/cross-device persistence [named as a real trade-off in ADR-0071's Consequences — `localStorage` is per-browser, per-device and invisible to everyone else, including support and this repo's own test suites], no per-user server-side preference profile, no column resizing, no drag-and-drop) + propagation across ADR index (0071 row added) / Requirements (new FR-ADMIN-4 with 7 acceptance criteria, new NFR-71, new §2.8 narrative paragraph, new traceability row) / UI Design (generic-admin-CRUD UI Design Document — new §9 for the Columns button + modal, and its §8 "Non-goals" line reading "no drag-reorder"/"no per-column sort UI" corrected in place, since ADMIN-4 and this story each landed one half of what that line ruled out) / Test Plan (new §14 risk row — a stale or corrupt client-side preference silently hiding data with no error and no marker) / Test Design (§59) / Test Cases (new TC-ADMIN-047..052 [3 P1, 3 P2], Taxonomy & Generic Admin CRUD 43/26 → **49/30**, grand total by direct summation **290/164 → 296/168** — the P1 delta is `+4`, not `+3`, because this pass also found and fixed a **fifth** independent instance of this file's stale-count class: a second, independent check of its own row [counting that section's actual `TC-ADMIN-NNN` table lines — 49 rows, 30 of them carrying `P1`] showed the area's printed P1 cell had been understated by one since ADMIN-5's pass, a drift the repo's existing "sum every row's literal value" rule structurally cannot catch since the literal values summed to the printed total correctly both before and after. The same actual-row-count check across every *other* feature area surfaced several further apparent discrepancies — Auth, Requirement & Test Case Authoring, Layout & Navigation, Project & Release — which are **flagged in that doc and deliberately not fixed here**: distinguishing a correctly-excluded struck-through row from a genuinely dropped count needs a row-by-row read of each area's own history, i.e. its own dedicated audit, not a clause in a feature pass). Database / API / Sitemap / User Stories assessed and marked **N/A with reason** — no schema, no route, no new URL, no new story-level actor goal beyond FR-ADMIN-4's own ACs. **Numbered `0071`/`11.51`/`§59`/`NFR-71`/`FR-ADMIN-4`/`TC-ADMIN-047..052` — next-free *above* the sibling `search-fields-audit` branch's own unmerged `0070`/`11.50`/`§58`/`NFR-70`/`TC-ADMIN-044..046` claims**, not merely next-free against `origin/main`, per root `CLAUDE.md`'s own numbering-collision convention and specifically its 7th recorded instance, which was caused by treating an in-flight sibling branch's claim as invisible. Re-check against `origin/main` again immediately before merging. **Docs and implementation landed together on this same branch, in the same pass** — the row originally closed by saying the implementation "is being written concurrently... and is not what this row records", which was true when written and is corrected in place here rather than left to go stale (`docs/CLAUDE.md`'s own note on that exact phrase class). **As landed:** 4 new files (`frontend/src/lib/columnPreferences.ts` + its test, `components/organisms/column-preferences-modal/` [component + barrel + test], `components/organisms/entity-table.columnPreferences.test.tsx`, `e2e/tests/colpref1-column-preferences.spec.ts`) and 2 edited (`components/organisms/entity-table/entity-table.tsx` — the Columns button, the preference state, the merged `tableFields`; `components/index.ts` — the new organism's barrel export). `EntityListPage.tsx` untouched, as the Decision predicted. **Zero backend files touched** (`git diff --stat origin/main -- backend/` empty), so the backend suites carry no regression risk from this story; confirmed anyway — backend unit **551 passed**. Frontend `tsc --noEmit` clean; full Vitest **84 files / 623 tests passing** (up from 548 on `origin/main`, +75 across the 3 new frontend test files). New e2e spec (7 cases) green against an isolated stack (`testnexa-colprefs-test`, DB cloned from `main`); the four neighbouring specs that also exercise this table (`admin2-generic-crud-ui`, `admin3-entity-schema`, `ds2-table-container`, `req5-standalone-test-case`) re-run as a regression check, 9/9 green — one of them needed the solo rerun this repo's own host-contention class already documents (`immich-server-1` at ~96% CPU on the shared host at the time), confirmed clean alone, not a regression from this story. **Two real coverage gaps were found and closed by re-reading this pass's own freshly-written tests against the TC rows' literal cells** (root `CLAUDE.md`'s "applies with zero discount to code you finished five minutes ago" rule, which held again here): TC-ADMIN-051's precondition names slugs "similar enough to catch a key-prefix bug" and the first draft used two unrelated slugs that would not have caught one (fixed — `test_case`/`test_case_step`/`test_condition`/`test` now asserted); TC-ADMIN-050's precondition names one column hidden **and** two reordered *in the same session*, where the first draft proved each half after a separate reload and would have passed with a write path that persisted `hidden` but dropped `order` (fixed — a dedicated combined case at both the Vitest and the e2e layer). **Two e2e-harness artifacts, neither a product bug, both fixed in the spec:** Playwright's `allInnerTexts()` returns CSS-`text-transform`-applied text, so Tabler's uppercase bare-`th` rule made the trailing "Actions" header read `ACTIONS` while every sortable header [whose label sits inside a `.btn`, which Bootstrap resets] read normally — switched to `textContent`; and a one-shot cell read immediately after `page.reload()` raced `EntityTable`'s own pre-existing batched FK-label resolution, which lands a moment after first paint — switched to an auto-retrying `expect(locator).toContainText`. | Done |
| 11.52 | [ADR-0072](../adr/0072-entity-table-filter-modal.md) (ENTITY-FILTER-1 — the generic admin list's fourth list-query capability gets its UI, and its backend allow-list stops being a per-entity opt-in. **Two independent gaps, closed in one pass.** (a) `EntityTable`'s own module docstring still claimed it rendered "a filter row for `filterFields`" and its props still declared `filters`/`onFilterChange` — an earlier refactor had dropped the per-column filter row and left the contract behind: neither prop was destructured in the component body, neither was rendered anywhere, while `EntityListPage` continued the whole time to own real `filters` state, include it in the list query's `queryKey`, and pass it to `listEntities(..., params: {...filters, ...scopeParams})`. The state-to-query pipeline was live and correct end-to-end; only the control that writes to it was missing. (b) `filter_fields` was a per-entity opt-in only **7 of 27** registered `CrudEntityConfig`s ever took [`attachment`, `test_case`, `risk_item`, `defect`, `test_execution`, `test_log`, `org_membership`], most naming one or two columns — so a modal built over today's tuples would be useful on 7 entities and empty or near-useless on the other 20, the same latent shape [ADR-0070](../adr/0070-search-fields-audit-and-numeric-cast.md) found and fixed for `search_fields` one capability over. Fix: `FieldMeta` gains `filterable: bool = True`, `derive_entity_schema` serves `"filterable"` per field alongside the existing `"sortable"` and its `"filterFields"` key becomes the derived list, and the list route computes its filterable set from `derive_entity_schema(config)["fields"]` at router-build time — **the identical mechanism, quoting `crud_factory.py`'s own existing comment on `sortable_fields` as the reason, that [ADR-0055](../adr/0055-admin-3-backend-driven-entity-schema.md)/[ADR-0056](../adr/0056-admin-4-generic-admin-crud-column-sort.md) already chose for sort**. The rule: every served field is filterable **except those whose derived type is `text`** ([ADR-0069](../adr/0069-req-5-standalone-test-case-with-optional-requirement-link.md)'s own `FieldMeta.long_text` is the mechanical marker for the one genuinely bad filter target, an unbounded free-text column). `CrudEntityConfig.filter_fields` survives as an optional **narrowing** override only [unset = derived]; the 7 existing declarations are deleted, each being a strict subset of what the derivation yields — keeping them would leave exactly those 7 entities *less* filterable than the other 20, the opposite of the intent. **Deliberately a derivation where ADR-0070 chose a hand audit**, on three differences that all cut the same way: equality is type-safe on every scalar column where an un-cast `ILIKE` against an `Integer` is a hard Postgres `ProgrammingError` [the constraint that forced ADR-0070 to reason column-by-column simply does not exist here]; "is this a useful filter" has a mechanical answer where "is this searchable" did not [ADR-0070 §4's own exclusions both point this way — a closed vocabulary is exactly what exact-match filtering is *for*, in its own words]; and a hand-kept list drifts, with `filter_fields` itself as Exhibit A. New: a malformed filter **value** is now a `422`, not a 500 — latent at 7 narrow tuples, reachable on the first typo once widened; `apply_filters_and_search` coerces each value against its own column type [UUID, boolean `true`/`false`/`1`/`0`, integer/numeric, date/datetime, enum-must-be-declared, everything else through as string] and a failed coercion raises `ValueError`, which the list route maps to a `422` with `field_errors` — byte-for-byte the envelope `apply_sort` already produces for an unsortable `?sort=` field, not a new error shape. `apply_filters_and_search` stays pure [no DB access], preserving the unit-testability its own docstring claims. **Widening exposes no information the list route did not already return**: the fields `derive_entity_schema` serves are exactly the entity's `summary_schema` fields, which the list route already serializes **in full** into every response body — filtering adds a way to *narrow* a result set, not a way to see a previously-hidden field — and composes onto a query the org/scope gate has already constrained, so a filter can only narrow *within* the caller's own tenant [NFR-1/[ADR-0007](../adr/0007-multi-tenancy-model.md) untouched]; ADR-0070 §5's secret-bearing-column reasoning carries over verbatim and needs no new rule [`password_hash`/`key_hash`/`key_prefix`/`token_hash` belong to models with **no `CrudEntityConfig` at all**, so they are not served and not filterable]. UI: one icon-only "Filter" button [Font Awesome `filter` via the `Icon` atom, `aria-label`/`title`] joins the `.card-header .card-tools` row beside [ADR-0071](../adr/0071-entity-table-column-preferences.md)'s sibling "Columns" button, opening a `FilterModal` organism composing the existing `Modal` molecule + `Button`/`Select`/`TextInput` atoms + `FkSelect`/`FkAutocomplete` molecules — no new raw `.modal`/`.btn`/`.form-select` markup, per root `CLAUDE.md`'s mandatory reuse check. Conditions are **AND-only** — the backend supports exact equality and nothing else and its chained `.where()` calls are `AND`ed unconditionally, so there is no `OR` to express and no operator to choose, and the modal renders **no operator control at all** rather than a single-option `<select>` implying a choice nobody has — and **at most one condition per field**: a second on a used field compiles to `field = a AND field = b`, always empty, which a user reads as "no results" rather than "contradictory query", so the field picker omits any field another row claims, making the state unrepresentable rather than merely discouraged. Value controls are typed, reusing **exactly `EntityForm`'s own branch** so a field is filtered through the same control it is edited through [`enum` → `Select` of its served `values`; `boolean` → Yes/No `Select`; `date` → `<input type="date">`; `fk` → `field.select ? FkSelect : FkAutocomplete`; else `TextInput`], with the `422` coercion as backstop rather than the user's first line of defence. Draft edits are local until Apply; Cancel and ESC discard, and the draft re-seeds from the applied filters on every open — same mechanism and reason as ADR-0071's own re-seed. **Filter state lives in `EntityListPage`, not `EntityTable` — the opposite of ADR-0071**, on ADR-0071's own stated rule [a parameter that maps to a backend query belongs to whoever owns the list query; column visibility/order map to nothing server-side, filters map directly to `?<field>=<value>`], so filters stay with `page`/`pageSize`/`sort`/`search` where they already were and `EntityTable` owns only the modal's open/closed boolean; the dead `onFilterChange?: (field, value)` prop is replaced by `onFiltersChange?: (filters)` because Apply commits the whole set at once and a removed condition must actually disappear rather than survive a per-key merge. **Filters are deliberately NOT persisted — an explicit divergence from ADR-0071's `localStorage` column preferences**, argued on an asymmetry rather than an oversight: a hidden column is visible [a row of headers with one missing announces itself], an active filter is not [a user returning to a silently-filtered list sees *fewer rows*, which reads as missing data, a broken query, or someone else's deletions], so persisting it optimizes a rare convenience at the cost of a recurring "where did my records go"; it also keeps filters consistent with the other four list-query parameters, all five of which `EntityListPage`'s own comments already declare "component state only — deliberately not persisted across navigation or reload" [TC-DS-018], where persisting one of five would be the anomaly. Mitigation for the stated invisibility risk: the Filter button carries an active-condition **count badge** and switches to `btn-outline-primary` while any filter is applied, so a narrowed list always has a visible cause in the header directly above it. Rejected alternatives worth noting: per-condition operators [`contains`/`>`/`<`/`in`] and `OR`/nested groups were both declined as **backend capabilities that do not exist** rather than UI choices — either means changing the `?<field>=<value>` wire format ADR-0022 established and every list caller uses including the MCP tool layer, and is its own ADR; the draft model carries an ordered list of per-row objects rather than a flat map *specifically* so a third control can be added per row without restructuring) + propagation across Requirements (new FR-ADMIN-5, new NFR-72, new §2.8 narrative paragraph, new traceability row) / ADR index (0072 row added) / API (§3 `filterFields` description corrected — derived, not a hand-declared tuple; new `filterable` per-field flag on `GET /entities/{resource}/schema`; §1's list-query filter paragraph gains the derivation rule + the new `422`-on-malformed-value case) / UI Design (§2/§3's **stale per-column "filter row for `filterFields`" claim corrected** — it described a control that had not existed since an earlier refactor, prose *and* the header layout sketch both updated to the real Filter button + modal) / Test Design (§60) / Test Cases (new TC-ADMIN-053..058) — numbered `0072`/`11.52`/`§60`/`NFR-72`/`FR-ADMIN-5`/`TC-ADMIN-053..058`, chosen against `origin/main` **and** both unmerged sibling branches' current claims (`search-fields-audit`: `0070`/`11.50`/`§58`/`NFR-70`/`TC-ADMIN-044..046`; `entity-column-prefs`: `0071`/`11.51`/`§59`/`NFR-71`/`TC-ADMIN-047..052`) — `origin/main` alone makes the lower numbers look free and they are not. `FR-ADMIN-4` is deliberately left unclaimed for `entity-column-prefs`, which has not yet written its own docs pass. Per root `CLAUDE.md`'s own numbering-collision recipe, **re-check immediately before merging** — a sibling can still renumber itself in flight (the MCP-6 case). Database/Sitemap/user-stories reviewed and **N/A with reason** (no schema change, no new route, no new screen — see the ADR's own "Neutral" consequence). **Docs-propagation pass; implementation is a concurrent, separately-owned workstream on this same branch** — the frontend implementation (`lib/entityFilters.ts`, `components/organisms/filter-modal/`, `EntityTable`/`EntityListPage`/`entityConfigs/types.ts` changes) and the backend changes (`crud_factory.py` + the 4 route modules dropping their now-redundant `filter_fields` tuples) are on disk with 36 passing unit tests across two files (`entityFilters.test.ts` 18, `filter-modal.test.tsx` 18); `backend/tests/integration/test_filter1_filter_fields.py` (TC-ADMIN-057, 7 tests) and `e2e/tests/filter1-entity-filter.spec.ts` (TC-ADMIN-058, 1 test / 3 claims) landed on disk during this docs pass and their assertions were read literally against the TC rows written here — but **neither was executed by this pass**, so no green-run count is claimed for either. Per root `CLAUDE.md`'s own "a WBS completion note narrating verification is not evidence a test exists, and reads identically confident either way" rule: run both against a live isolated stack before reporting this row's coverage as 100%. Two TC rows were **corrected against the real test files** rather than left as written-from-the-ADR descriptions: TC-ADMIN-057 originally named four coercible types (UUID/integer/enum/date) where the test asserts two representatives (UUID parse-failure, enum membership-failure), and omitted three claims the file does make (`AND`-not-`OR` at the route layer, a `text` column being *silently ignored* rather than `422`'d, and the org-boundary negative); TC-ADMIN-058 originally omitted the spec's own third claim, that a reload clears the filter — the live proof of §7's non-persistence, which Test Design §60 had initially (and wrongly) written off as having no test surface. | Done |
| 11.53 | [ADR-0073](../adr/0073-generic-entity-detail-page.md) (a generic, read-only **entity detail page** — the ADR-0025/ADR-0027 admin CRUD surface's third page component, `container/entity-crud/EntityDetailPage/`, on two new routes `/orgs/:orgId/admin/:entity/:id` and `/projects/:projectId/admin/:entity/:id` added inside ADR-0057's own `entityCrudRoutes()` so `App.tsx`'s two existing calls pick them up unchanged. It renders `config.fields` **unfiltered** — the single absent `.filter((f) => f.showInTable !== false)` IS the feature — making readable for the first time anywhere in the app the `showInTable: false` fields on `TestPlan` [`scope`/`approach`/`staffing_and_training`/`schedule`/`created_by_actor_id`], `TestCase` [`preconditions`/`expected_result`/`created_by_actor_id`], `TestExecution`, `Defect` and `RiskItem`, all of which were previously visible only by opening the *edit* form. Entry: `EntityTable`'s new optional `onRowClick` makes each `<tr>` keyboard-reachable [`tabIndex`, Enter/Space], with the Actions cell stopping propagation so Edit/Delete stay independent; omit the prop and the `<tr>` renders byte-for-byte as before. `EntityConfig.detailPath` [ADR-0060] takes precedence, so `Project` rows still open `ProjectDetail` — a branch on config data, not an entity name. `EntityTable`'s cell renderer + batched fk-label effect extracted to `components/molecules/entity-field-value/` and `pages/admin/useFkLabels`, shared by both pages — a **reuse extraction, not a decision of its own**, per `frontend/CLAUDE.md`'s "this is a reuse check, not a new ADR" rule, and output-identical [all 548 pre-existing Vitest tests passed unmodified across it]) + propagation across Requirements (new FR-ADMIN-6, new NFR-73, new narrative paragraph under FR-ADMIN-2's own section, new traceability row) / ADR index (0073 row) / Sitemap (both admin route-tree branches gain `/admin/:entity/:id`, plus a narrative note on the `detailPath` exception) / UI Design (new document, `2026-09-15-generic-entity-detail-page-ui-design.md` — prose and layout sketch checked against each other per root `CLAUDE.md`'s ADR-0032 note) / Test Design (§61) / Test Cases (new TC-ADMIN-059..064, Taxonomy & Generic Admin CRUD 43/26 → **49/29**, grand total by direct summation **296/167**). **No API/Database/RBAC doc applies** — reviewed, none touched: this story adds no route, no schema change, no permission code and no database change; `GET {path}/{id}` has existed for every entity since ADR-0022/ADR-0027. Numbered `0073`/`11.53`/`§61`/`FR-ADMIN-6`/`NFR-73`/`TC-ADMIN-059..064`, confirmed next-free against `origin/main` (`git fetch`, 0 commits behind; `0069`/`11.49`/`§57`/`NFR-69`/`TC-ADMIN-043` the true prior maxima) both before writing and again before merging, per the standing two-checkpoint rule. **Docs + implementation + all applicable tests in one pass.** Verified against the isolated stack `testnexa-deve77ffc-test` (port 34085, reachable via both `localhost` and the host's LAN IP): frontend `tsc --noEmit` clean, Vitest **565/565** (548 pre-existing + 17 new across 4 files), new e2e `admin6-entity-detail-page.spec.ts` **5/5** across three structurally different entity types (`test-plans` project-scoped with 5 hidden fields, `environments` project-scoped with none, `test-levels` org-scoped single-field catalog) plus the `Project`/`detailPath` case. Sibling-spec regression sweep (`admin2-generic-crud-ui`, `admin3-entity-schema`, `ds2-table-container`, `shell2-breadcrumb-coverage`, `req5-standalone-test-case`): 10/11 passed. **The one failure was proven pre-existing, not a regression, by a direct A/B revert** — `ds2-table-container.spec.ts`'s ProjectDetail-Releases case fails identically with every one of this branch's frontend source changes reverted to base and the Vite container restarted, and that screen uses `container/Table` directly [untouched here], never `EntityTable`. Root cause is the already-documented host-contention/cold-load class (`docker stats` showed unrelated `immich`/`nocodb` containers at 175%/109% CPU alongside four sibling testnexa stacks) — recorded in `e2e/CLAUDE.md` so a future session doesn't re-diagnose it. **Isolated stack deliberately left running for CTO manual verification**, per root `CLAUDE.md`'s handoff convention. | Done |
| 11.54 | [ADR-0074](../adr/0074-entity-detail-relationship-tabs.md) (**relationship tabs** on ADR-0073's detail page, partially superseding that ADR's own "related-record panels are out of scope" clause — an **Info** tab plus one tab per *inbound* relationship). The relationship graph is **derived on the backend, never hand-authored**: new `crud_factory.derive_entity_relations(config, all_configs)` walks `ALL_ENTITY_CONFIGS` and is served as a tenth key, `relations`, on `derive_entity_schema` — so it rides the schema request every admin page already makes, costs no extra round trip, and reaches the MCP `describe` tool for free (both call that one function). **One-to-many** = another entity's `FieldMeta.ref_entity` read backwards; **many-to-many** = an [ADR-0005](../adr/0005-traceability-link-dedicated-join-tables.md) link table, classified **structurally** (exactly 2 FK fields, no `create`/`update`) rather than by `_link` name, which correctly rejects the `TestExecution`/`RoleAssignment`/`RiskItem` two-FK near-misses. **Many-to-one deliberately gets no tab.** A relation is emitted only when `GET /{entity}?{scopeField}=` can actually serve it (the FK must be the child's own `scope_field` — `extract_scope_value` 422s otherwise), so **12 inbound FKs are excluded and flagged as a backend list-capability gap, not worked around**. n-n tabs list the link rows but are labelled/navigated by the far entity via `targetField`, with a `" (linked)"` suffix that resolves a real collision (`Requirement` reaches `TestCondition` both directly and via a link table — two genuine tabs that would otherwise share a label). Scoping column hidden via a non-mutating config copy; active tab in `?tab=`, validated, Info as fallback. New `Tabs` molecule (reuse check found no existing tab primitive anywhere in `frontend/src`) — stock Bootstrap 5 markup, **no `data-bs-toggle`** so Tabler's loaded JS can't fight React for panel ownership, and **no count badge** (would need N list requests, and `badge bg-*` is the known-invisible class ADR-0073 documented). `make_crud_router` switches to a new `derive_sortable_fields` because the full schema now needs the registry at a point where the registry is still importing — a genuine circular import, with a test pinning the two derivations in agreement. **22 relationships across 9 entities live.** + propagation across Requirements (new FR-ADMIN-7, NFR-74, NFR-75, a new narrative paragraph, new traceability row) / ADR index (0074 row + 0073 marked `Partially superseded by 0074`) / Sitemap (`?tab=` on the existing detail route — **no new route**) / Test Design (§62) / Test Cases (new TC-ADMIN-065..073, Taxonomy & Generic Admin CRUD 49/29 → **58/34**, grand total by direct summation **305/172**). **No Database/RBAC doc applies** — reviewed, neither touched: no schema change, no new permission code, no migration. The only API delta is an **additive** key on an existing route, so API Doc §3's route table is unchanged. Numbered `0074`/`11.54`/`§62`/`FR-ADMIN-7`/`NFR-74`-`72`/`TC-ADMIN-065..073`, confirmed next-free against `origin/main` (`git fetch`, 0 behind; `0073`/`11.53`/`§61`/`NFR-73`/`TC-ADMIN-064` the true prior maxima) both before writing and again before merging. **Found and fixed a real pre-existing defect in ADR-0073's own shipped code** — `useFkLabels` keyed its effect on the `rows` **array identity**, and `EntityDetailPage` passed a freshly-allocated `[row]` every render, so the effect re-ran → `setFkLabels` → re-render → **2913 `getEntity` calls in 400ms**, measured directly. Invisible to every existing test and to the live manual pass because the rendered output is identical whether the effect runs once or forever; it surfaced only because this story's new suite held the component mounted long enough to starve the test runner at 88% CPU. Fixed at the root (a primitive `fkIdFingerprint`, mirroring the `refConfigFingerprint` the same hook already used for exactly this reason), recorded as **ADR-0073 Amendment 1** (same-branch pre-merge correction, per `docs/CLAUDE.md`), with a new `useFkLabels.test.tsx` that **counts requests** rather than asserting output — the only kind of assertion that can see this class of bug. Verified against the isolated stack `testnexa-deve77ffc-test` (port 34085, both `localhost` and LAN IP): backend unit **577/577** (551 pre-existing + 26 new), frontend `tsc --noEmit` clean + Vitest **86 files / 584 tests** (565 pre-existing + 19 new across 3 files), new e2e `admin7-entity-relation-tabs.spec.ts` **5/5**, sibling-spec regression sweep clean. **Isolated stack deliberately left running for CTO manual verification**, per root `CLAUDE.md`'s handoff convention. | Done |
| 11.55 | [ADR-0075](../adr/0075-junction-table-registry-completeness.md) (**junction-table registry completeness**, completing ADR-0074's derivation and correcting its Decision §4 exhaustiveness claim). **Defect:** `GET /entities/test-suites/schema` returned `"relations": []` — `TestSuite`'s detail page rendered no tab strip at all — because `test_suite_test_case` (REQ-4/ADR-0030) and `test_plan_test_suite` (PLAN-1/ADR-0031) had bespoke membership routes but no `CrudEntityConfig`, so `derive_entity_relations` had nothing to walk. ADR-0074's own completeness test partitioned the FKs of the same registry the derivation walks, so both were in neither bucket and it held **vacuously**. **Fix (1):** both junctions registered as read-only entities (`methods={"list","get"}`, `create_schema=None`, `update_schema=NoSchema`, `FieldMeta(ref_entity=...)` on both FKs), shaped verbatim on `trace.py`'s four link tables, each living in the bespoke module that already owns its junction's HTTP surface; scoped on the **parent** side so the reverse directions join ADR-0074 §4's enumerated exclusions (14 now, was 12) rather than widening two of six link tables to a branching 2-tuple. **Fix (2), the durable half:** `tests/unit/test_adr75_registry_completeness.py` asserts completeness at the **model layer** against `Base.metadata` — which a model joins by declaration, unlike the hand-typed `_ALL_CONFIGS` tuple — failing on any FK-bearing table that is neither registered nor in a reasoned `EXPECTED_UNREGISTERED_CHILDREN` entry; **mutation-tested** (removing the two configs reproduces the exact failure). **Full audit, not just the reported symptom:** all 8 junction tables enumerated; 4 deliberately left unregistered with stated reasons and asserted consequences (`test_case_test_design_technique` — zero references outside the model/migration, so a config would ship a permanently-empty tab; `role_permission` — RBAC grant graph, security decision + system roles are `org_id IS NULL`; `approval`/`invite` — bespoke, `invite` carries a `token_hash`). **Mirror case audited and clean:** every `*_id` field cross-checked against the live database's `pg_constraint` rows, all 7 missing `ref_entity` values deliberate and documented in code — the entire defect was missing registry entries, not missing field metadata. **RBAC:** `test_suite_test_case`/`test_plan_test_suite` added to `READ_ONLY_RESOURCES` (29 -> 31 resources, 100 -> 102 catalog rows); `org_admin`/`auditor` gain them by their existing definitions, `test_manager`/`tester` explicitly (both already hold `test_suite.read`/`test_plan.read`, so without these the tab would `403` for the two roles that use those screens); `ai_agent_scoped` deliberately not granted. Migration `7d2c91af4e68` — the **first** RBAC extension here that inserts `Permission` rows, not just grants; verified idempotent by invoking `upgrade()` twice through a real `Operations` context, not by a second CLI call (which `backend/CLAUDE.md` documents as a no-op). **MCP:** tools 147 -> 153; resource count unchanged at 31, since both junctions were *already* config-less bespoke registry rows — part of why the gap hid. **Frontend:** both added to `registry.ts` **and** `PROJECT_EXCLUDED_ENTITY_KEYS`, matching the four link tables exactly (labels/routes, no sidebar slot); the tabs themselves need no frontend change. **Verification:** backend unit **593/593**, 4 new integration tests (create through the *real* bespoke route, then schema-read, then fire the relation's own list request — never a config-shape assertion), frontend `tsc --noEmit` clean + Vitest **594/594**, e2e **6/6** including new TC-ADMIN-075, plus a live click-through screenshotted on the isolated stack (`TestSuite` -> "Test cases (linked)" listing 3 real rows by resolved title, scoping column suppressed, `?tab=` in the URL). **Found, not fixed:** `main`'s own DB is missing one of the 5 `TestType` rows migration `854917c76ac5` seeds, failing 3 pre-existing tests on every clone — a second consequence of the vacuous-CLI-idempotency trap (a data seed can never self-heal a row deleted after it ran). Unrelated to this branch (zero `test_type` code touched); flagged in ADR-0075's Consequences | Complete |
| 11.56 | [ADR-0075 **Amendment 1**](../adr/0075-junction-table-registry-completeness.md#amendment-1-2026-09-15--every-junction-is-scoped-and-therefore-tabbed-from-both-ends) (**every junction is scoped, and therefore tabbed, from both ends**; NFR-77). An in-place `### Amendment` rather than a new ADR number, per `docs/CLAUDE.md`: Decision §3 did not reject this on its merits — it explicitly parked it and named the condition ("**widening all six later remains open**"), and its stated objection was *coherence across the six*, never capability, tenant safety or cost. Doing all six at once spends that objection exactly. **Symptom, confirmed live before any code changed:** `GET /entities/test-cases/schema` carried no `test_suite` or `test_plan` relation at all, and `GET /entities/defects/schema` returned `"relations": []` — `TestCase` is the entity three separate junctions point at and its detail page could show none of them; `Defect`'s page rendered no tab strip whatsoever, visually identical to `TestStep`, an entity that genuinely has no relationships. The same symptom 11.55 fixed for `TestSuite`, surviving in four other places because that fix registered the missing configs without revisiting which *direction* they scope. **Change (1):** all six n-n junctions' `scope_field` widened to the branching 2-tuple of both FK columns — `requirement_test_case_link` `("requirement_id","test_case_id")`, `requirement_test_condition_link` `("requirement_id","test_condition_id")`, `test_condition_test_case_link` `("test_condition_id","test_case_id")`, `test_case_defect_link` `("test_case_id","defect_id")`, `test_suite_test_case` `("test_suite_id","test_case_id")`, `test_plan_test_suite` `("test_plan_id","test_suite_id")` — the **pre-amendment arm declared first** in every pair, load-bearing twice over (`scope_validation_error` keys its `422` on `candidates[0]`, so existing clients see the same field name; and the resolver's first branch is the one a real row takes, keeping every item route's tenant walk byte-identical). **Change (2), the zero-line half:** `derive_entity_relations` is **unchanged, not one line** — it already kept FKs in `_scope_candidates`, which explodes a tuple, the same mechanism that has always made `RiskItem` a child of both `Requirement` and `TestPlan`. A genuine capability widening across six entities cost zero changes to the derivation, the schema route, the MCP `describe` tool or the frontend — the strongest evidence yet that ADR-0074 Decision §1's derivation was designed correctly. **Change (3), the whole risk:** a branching `scope_field` *requires* a branching resolver. `crud_factory._resolve_scope_for_write` calls `resolve_org_id` with a `types.SimpleNamespace` carrying **only the one arm the caller supplied**, never a full row — so a config widened to two arms but left with its original single-arm walk type-checks, derives a perfectly good relation, renders a tab, and then 404s every request that tab fires, reading `None` off the stand-in. New `crud_factory.branching_resolver(branches)` generalizes the shape `resolve_risk_item_org_id` has always hand-written; each of the six **composes existing** resolvers (`chain_resolver`, `resolve_via_test_case`) rather than adding a new walk, and each reverse arm deliberately reuses the *same* resolver that junction's own bespoke route already uses for that side, so the two surfaces cannot drift on what org a row belongs to. Each junction's `scope_selector` gains a second labelled option, RiskItem's shape. **Result:** six new tabs across four entities — `TestCase` +3 at once (Requirements / Test conditions / Test suites, all `(linked)`, six tabs total alongside Attachments / Test steps / Defects (linked)), `TestSuite` +"Test plans (linked)" (2), `TestCondition` +"Requirements (linked)" (2), and `Defect` **from zero tabs to one**; `Requirement`/`TestPlan` unchanged. Relations **22 across 9 entities → 30 across 10**. ADR-0074 §4's exclusion table loses its entire link-table block: **14 → 8**, and the struck row was the only entry describing a deliberate design limit rather than a genuine list-capability gap — the eight that remain are filter-field-only FKs plus `RoleAssignment`'s missing `list` route. **RBAC/migration: neither.** Permissions are per-**resource**, not per-direction, and all six junctions already hold their `.read` code from 11.55/ADR-0027 — nothing in `rbac_seed_catalog.py` moves, no new migration. **Accepted contract change:** an unscoped list on these six now returns `"exactly one of X or Y must be set"` instead of `"<X> is required."` — same `422`, same `validation_error` code, same keyed field name (the original arm), and no client in this repo matches on message text; supplying **both** arms is newly reachable and rejected with `"...not both"`, `RiskItem`'s XOR narrowing now applying here. **Verification:** new `tests/unit/test_adr75_amendment1_bidirectional_junctions.py` (**47** tests, including an **in-suite mutation check** that re-runs the same inputs through a reconstructed pre-amendment single-arm resolver and proves it genuinely fails them — the insufficiency is demonstrated, not asserted) and new `tests/integration/test_adr75_amendment1_bidirectional_junctions.py` (**5** tests, every one create-through-the-**real-bespoke-route**-then-read-back, covering all 6 junctions in both directions plus an NFR-1 cross-tenant `404` check on the *new* arm). Two pre-existing tests **inverted, not deleted**, because each asserted the absence of exactly these tabs and so pinned the superseded Decision §3: `test_adr74_entity_relations.py::test_registering_the_junctions_did_not_change_test_cases_own_tabs` and the reverse-direction half of `test_adr75_junction_relations.py::test_a_junction_list_without_its_scope_value_is_a_422_not_an_unscoped_dump` (both TC-ADMIN-076, amended in place); the zero-arm and both-arms `422`s they also cover remain true and still assert. New TC-ADMIN-078/079. **Found, not fixed:** two of the new `scope_selector` arms point at ref entities the shared `ScopeSelector` molecule cannot search today — `test-condition` (its own list is scoped by `requirement_id`) and `defect` (by `test_execution_id`) — a limitation `frontend/src/components/molecules/scope-selector/scope-selector.tsx` already documents **by name for exactly these two**: it threads only `project_id` through to its `FkAutocomplete`, and anything needing a different parent needs a cascading multi-step picker. Pre-existing, not introduced here, and it affects only the **generic admin list page's** scope-gate picker — **never** the relationship tabs, which pass the parent id directly and render no picker at all | Done |
| 11.57 | [ADR-0076](../adr/0076-relationship-tab-write-actions.md) (**write actions on the relationship tabs**, from a route-declared link-create action; FR-ADMIN-8, NFR-78). **Extends 11.54/11.55/11.56 and supersedes none of them** — the derivation, the exclusion rules, the `?tab=` posture and the hidden scoping column all stand, and `derive_entity_relations` is untouched. **Gap:** 11.56 finished at **30 relationship tabs across 10 entities, every one read-only**, and for four of [ADR-0005](../adr/0005-traceability-link-dedicated-join-tables.md)'s traceability tables the tab is the *only* place the relationship is visible at all — so a `RequirementTestCaseLink` row could only ever be written as a side effect of creating a brand-new `TestCase` (REQ-2's `POST /requirements/{id}/test-cases`), `RequirementTestConditionLink`/`TestConditionTestCaseLink` only through REQ-3's authoring routes, and `TestCaseDefectLink` only while *raising* a new defect (EXEC-3's `POST /executions/{id}/defects`). "Link a test case that already exists" / "this failure is the defect we already have" had **no route anywhere**: the traceability matrix could be grown, never *assembled*. The one-to-many tabs had the mirror-image gap with a much smaller cause — the child's generic `create` already existed and was already used by `EntityListPage`; what the tab lacked was the affordance plus a way to fix the parent id. **Decision (1) — the route declares itself.** A generic tab cannot derive two facts: *which route writes a link row* (every link table is `list`/`get` only through the factory and its rows come from a bespoke route whose URL shape is arbitrary — `/test-suites/{id}/test-cases/{case_id}`, `/test-plans/{id}/test-suites/{suite_id}`, and four that did not exist) and *which permission it gates on* (REQ-4/PLAN-1 gate on the **parent's** `test_suite.update`/`test_plan.update`, not on anything named after the junction). New optional `CrudEntityConfig.link_create` = `LinkCreateAction{path_template, permission}`, set on link entities only, served as an **eleventh, additive `linkCreate` key** on `derive_entity_schema` (`null` for the 23 non-link entities) beside 11.54's `relations`, so it rides the schema request the tab already makes and costs no extra round trip. Two properties make it generic rather than a map in disguise: placeholders are named after the link row's **own FK columns**, so one declaration serves a tab at *either* end of a junction (load-bearing precisely because 11.56 made all six bidirectional — the tab substitutes `relation.scopeField` and `relation.targetField` by name); and each declaration lives in the module that defines the route (`trace.py`, `test_suite_membership.py`, `test_plan_membership.py`), so a route change and its declaration are one diff. The rejected alternative is the one that matters: a hand-authored frontend map from link entity to route is exactly the per-entity map ADR-0074 Decision §1 exists to forbid, would sit on the far side of the network boundary from the routes it describes, and could never be completeness-tested against them. **Decision (2) — four new bespoke routes**, all in `trace.py` beside the configs they serve: `POST /requirements/{id}/test-case-links/{test_case_id}` (`requirement_test_case_link.create`), `POST /requirements/{id}/test-condition-links/{test_condition_id}`, `POST /test-conditions/{id}/test-case-links/{test_case_id}`, `POST /test-cases/{id}/defect-links/{defect_id}`. Each is the byte-identical boundary sequence [ADR-0030](../adr/0030-req4-test-suite-membership-bespoke-routes.md) fixed — fetch the path parent, unresolvable org **or** no `OrgMembership` → `404`; permission missing → `403`; far row in a different org or absent → `404`, **never** `403` and never `422` (NFR-1/[ADR-0007](../adr/0007-org-scoped-multi-tenancy.md) — existence is not confirmable across a tenant boundary); same org but different **project** → `422 validation_error` (past the tenant boundary there is no existence left to hide, only an invalid relationship); insert, translating the pair's unique constraint into `409`. `_gate_parent`/`_load_same_org`/`_insert_link` are shared rather than written four times — a fifth independently-reasoned copy of that sequence is how boundary semantics end up disagreeing between routes that are supposed to be identical. Membership gated with `_actor_membership_exists`, not the older `User`-only `_org_membership_exists`, per `backend/CLAUDE.md`'s rule for any *new* resource-gating route, so an MCP/`AIAgent` caller is not 404'd unconditionally; the two older membership routes are deliberately left on the older helper (its own fix). **No generic-surface change** — `methods` stays `{"list","get"}`, `create_schema` stays `None`; links remain immutable, delete-and-recreate (ADR-0005). **Decision (3) — RBAC.** New `LINK_CREATE_RESOURCES` tuple, a fourth resource grouping alongside `CRUD_RESOURCES`/`READ_ONLY_RESOURCES`/`SPECIAL_PERMISSIONS`, one `.create` code per traceability link (catalog **102 → 106**); modelled as its own tuple rather than by promoting the four into `CRUD_RESOURCES`, which would mint `.update`/`.delete` codes for routes that do not exist and never will, and the four stay in `READ_ONLY_RESOURCES` because that tuple's real meaning — no generic CRUD surface, no update, no delete — is still exactly true of them. Bundles: `org_admin` all 4 (its bundle is "every permission that exists", by definition); `test_manager` all 4 **+ the 3 `.read` codes it lacked** — read and write together, because the tab's own list request gates on `.read`, so granting the write alone ships a button on a tab that `403`s before it can render; `tester` `test_case_defect_link.create` **only** (it already holds that link's `.read` and full `defect` create/read/update, so that one write is a workflow it performs — the three requirement-level links are `test_manager`'s activity and granting them would be a silent scope expansion, the same restraint [ADR-0033](../adr/0033-plan3-test-cycle-creation-and-execution-scope-check.md)/[ADR-0044](../adr/0044-exec-3-raise-defect-from-execution.md) took); `auditor` nothing (read-only by definition, already holds all four `.read`); `ai_agent_scoped` nothing (reaches none of the linked entities). **REQ-4's and PLAN-1's shipped routes are deliberately not re-gated** — re-gating a live contract for symmetry would break every existing caller and buy nothing, and `LinkCreateAction` *declares* its permission rather than deriving it precisely so both shapes coexist. Migration `3e6b08c5da71` (down_revision `7d2c91af4e68`) backfills — the **second** RBAC extension in this repo that inserts `Permission` rows rather than only grants — with a deliberately **asymmetric `downgrade()`**: it removes the four new codes and every grant of them, but for the three *pre-existing* `.read` codes it revokes only `test_manager`'s grants, never the catalog rows, which predate it and other roles hold. Idempotency proven by invoking `upgrade()` **twice through a real `alembic.operations.Operations` context**, never a second `alembic upgrade head` — which `backend/CLAUDE.md` (ADMIN-5/TC-ADMIN-041) documents as a bookkeeping-level no-op that never re-enters the function body, making the obvious `before == after` assertion vacuous. **Decision (4) — one action per tab, chosen by `relation.kind`, never both** (the two kinds mean structurally different things, and an n-n tab's rows are link rows, which there is no form to fill in): one-to-many → **"New"**, opening the same `EntityForm` create modal `EntityListPage` already hosts, with `relation.scopeField` passed as `lockedValues` (which `EntityForm` already renders as a disabled display field, keeps out of the Zod schema, and merges into the payload itself) so the row lands under this parent and cannot be retargeted — **no new form component**, and deliberately *not* a route to `EntityFormPage`, which is the `/edit` route only ([ADR-0027](../adr/0027-generic-admin-crud-ui-and-backend-completion.md)'s two-page split); many-to-many → **"Link existing &lt;far entity&gt;"**, a modal wrapping the searchable `FkAutocomplete` picker REQ-5/[ADR-0069](../adr/0069-req-5-standalone-test-case-with-optional-requirement-link.md) already built for `EntityFormPage`'s "Link to Requirement" section, POSTing to `config.linkCreate.pathTemplate` interpolated from `{scopeField: parentId, targetField: pickedId}`. Still **no per-row Edit or Delete** — every listed record is fully editable on its own screen one click away, and a link row has nothing to edit. Both actions gated **twice**, answering different questions — *can the API do this at all* (`config.methods`/`config.linkCreate`) and *may this actor* (`usePermissions`, fail-closed while loading) — and a missing permission makes the button **absent, not disabled** (UI Design Document §5's hide-don't-disable posture, identical to `EntityListPage`'s own `canCreate`). **Decision (5) — picker scoping derived, three ordered cases, no per-entity branch and no new picker component:** no `scopeField` → search unscoped; `scopeField === "project_id"` and the route carries `:projectId` → supply it (**9 of the 12 live link directions**); otherwise render the far entity's own declared `scopeSelector` through the shared `ScopeSelector` molecule *inside the modal, above the picker* — the same "pick a parent row before the list can fetch" step `EntityListPage` shows for the same entities, in the same component. **MCP: tools 153 → 157** — each new route also gets its registry executor, `BESPOKE_EXTRA_ACTIONS` row and generated-tool description entry in the same change, per `backend/CLAUDE.md`'s rule, so the capability is reachable over MCP and REST identically (**verified live against the running registry rather than inferred: 157 rows = 128 CRUD actions across 31 resources + 29 `describe`**). **Found and fixed: standalone `TestCase`s could not be added to a `TestSuite` at all.** `test_suite_membership.py` carried a private three-branch copy of the `TestCase`-project walk (`_resolve_test_case_project_id`), written for REQ-4 before `TestCase` had a `project_id` column; REQ-5/ADR-0069 added that column and a fourth branch to the *org* resolver, and nothing pointed at the project-side copy — so it returned `None` for every standalone case and `add_test_case_to_suite` rejected **all** of them with `422 "This test case belongs to a different project."` even when the suite and the case sat in the same project. That copy's own docstring had predicted this exact failure mode and argued the duplication was safe because it never gates tenancy — safe in that narrow sense and still wrong, because the duplication is what made the REQ-5 change *look* complete. Found while building this ADR's own routes, which need the same walk; fixed by promoting it to `crud_factory.resolve_test_case_project_id` beside its org-side sibling, mirroring its branch order exactly, with `test_suite_membership.py` delegating. **Regression asserted against REQ-4's own route** (TC-ADMIN-086), not against any new one, since that is the route that was broken — a test against a new route would pass on a codebase where the defect is still live. Recorded inside this ADR rather than getting its own number: it is a resolver-branch completeness fix inside this ADR's necessary surface, found *before* shipping, which is not the shape the ADR-0039/ADR-0040 "found and fixed the same day, still gets its own ADR" convention covers (`docs/CLAUDE.md`). **Also accepted, not reconciled:** `POST /test-cases/{id}/link-requirement` (REQ-5) and `POST /requirements/{id}/test-case-links/{case_id}` now both write `RequirementTestCaseLink` under different rules — REQ-5's is a one-shot *retrofit* that `409`s if the case already has **any** requirement link (at-most-one, from the `TestCase` side), this one is the table's real many-to-many contract and `409`s only on the *pair*; reconciling them would change REQ-5's shipped contract, which no acceptance criterion asks for. **Unlinking deliberately out of scope** — two of six junctions have a `DELETE` route already and four have none; "what does removing a traceability link mean for an already-exported RTM?" is a real decision for its own story, so the tabs grow monotonically today. **Seventh ad hoc extension of `test_manager`'s seeded bundle**, flagged again and deliberately not absorbed — `backend/CLAUDE.md` has had this down as overdue a dedicated bundle audit since the fifth. + propagation across Requirements (new FR-ADMIN-8, new NFR-78, a new narrative paragraph under §2.8, new traceability row) / ADR index (0076 row + Date line) / API (§3.1 gains a `linkCreate` subsection — and, in the same edit, the `relations` key ADR-0074 added but never propagated here; §4 gains the 4 new route rows + a boundary-semantics paragraph; §5's four link-table rows lose their now-false "row created only as a side effect of ..." clause; §6.1's stale tool count corrected by direct count) / Database (§3.3's seeded-catalog paragraph, 102 → **106**, plus the new `create` action on the four link resources — **no schema change**: no new table, no new column, no new constraint) / Sitemap (one-line narrative addendum on the existing `?tab=` paragraph — **no new route and no new screen**, the change is two buttons and two modals inside an existing tab panel) / UI Design (`2026-09-15-generic-entity-detail-page-ui-design.md` gains §6, prose and sketch agreeing, plus a forward pointer on §3's now-superseded "no related-record panels" line) / Test Design (§65) / Test Cases (new TC-ADMIN-080..095, Taxonomy & Generic Admin CRUD 64/40 → **80/52**, grand total by direct summation **327/190**). **Verification — everything below was executed against the isolated stack `testnexa-deve77ffc-test` (frontend `:34085`, backend `:38217`, Postgres `:41873`, both `localhost` and the host's LAN IP), 2026-09-15.** Backend unit **693/693** (649 pre-existing + 44 new across `tests/unit/test_adr76_link_create_actions.py` and `tests/unit/test_adr76_seed_trace_link_create_permissions.py`, including the in-suite mutation checks on both directions of the completeness partition). Backend integration **16/16** for this story (`test_adr76_link_create_routes.py` 9 — every positive one create-through-the-real-route then fire the relation's *own* list request, never a create-response assertion; `test_adr76_link_create_permissions.py` 7), against a database migrated to `3e6b08c5da71`. Frontend `tsc --noEmit` clean + Vitest **614/614** (594 pre-existing + 20 new). New e2e `admin8-relation-tab-write-actions.spec.ts` **3/3**, run twice, the second run capturing an end-of-test screenshot per flow as the live click-through record. **Seven pre-existing tests corrected in place, none deleted**, each having pinned a claim ADR-0076 makes false: the schema-envelope key list (ten → eleven, unit + integration), `test_mcp_entity_dispatch.py`/`test_mcp_entity_tools.py`'s "`requirement_test_case_link.create` is never advertised" rows (its `update`/`delete` siblings stay — a link row is still immutable), the MCP tool-count anchors (153 → **157**), and `test_rbac_seed_catalog.py`'s catalog-size/read-only-action assertions, narrowed rather than loosened so a *fifth* code on any junction still fails. **Full-suite integration classified, not waved through:** 374 passed / 6 failed, and all 6 shown unattributable — four are this cloned database's own taxonomy-catalog state (the missing `Confirmation Testing` row ADR-0075 already recorded as found-not-fixed; two siblings failing on `FileNotFoundError: 'alembic'` from a host-side `subprocess` call; and 8 leaked fixture `TestLevel` rows whose names identify their owners as ADMIN-2/ADMIN-7/PLAN-3/REQ-5 — the three this story's own first, cleanup-failing run leaked were identified by tag and removed), reproduced identically on a solo rerun; the other two passed immediately on a solo rerun, the documented host-contention class. This story touches no taxonomy code at all. Numbered `0076`/`11.57`/`§65`/`FR-ADMIN-8`/`NFR-78`/`TC-ADMIN-080..095`; `TC-ADMIN-079`/`NFR-77`/`11.56`/`§64` re-confirmed as the true prior maxima by grepping rather than eyeballing, per `docs/CLAUDE.md`, and re-checked against `origin/main` (`git fetch`, 0 behind) both before writing and again before merging. **Isolated stack deliberately left running for CTO manual verification**, per root `CLAUDE.md`'s handoff convention | Done |
| 11.58 | [ADR-0076](../adr/0076-relationship-tab-write-actions.md) **Amendment 1** (**both write actions on every many-to-many relationship tab**, and "Create new &lt;far entity&gt;" as a compound create-then-link; FR-ADMIN-8 corrected in place, new NFR-79). **A same-branch, pre-merge correction to 11.57's own Decision (4), handled as an in-place `### Amendment` rather than a new ADR number** per `docs/CLAUDE.md`: it changes no route, no permission code, no schema key, no migration and no backend surface whatsoever — only which buttons a tab renders and one client-side sequence, which is the size of decision that convention reserves for an amendment (contrast ADR-0039 → ADR-0040, a schema change to already-merged behaviour). **Gap:** 11.57 shipped "one action per tab, never both", which is right about *kind* and wrong about *count* — an n-n tab could only link a record that **already existed**, so the commonest authoring case ("this requirement needs a test case, and that test case does not exist yet") still meant leaving the record, creating the row on the far entity's own list page, navigating back, and only then linking. The original reasoning ("an n-n tab's rows are link rows, and there is no form to fill in for a link row") is sound about the *link* row and silent about the far row, which does have a form. **Found by a live click-through, not by any failing test** — the same post-hoc-reversal class [ADR-0063](../adr/0063-mcp-integration-screen.md)'s and [ADR-0048](../adr/0048-brand-identity-and-logo-system.md)'s own amendments record, and the class root `CLAUDE.md` names when it says a fully-specified, fully-tested decision stays provisional until a human uses it. **Decision (1) — both n-n actions, always both when permitted.** `Link existing &lt;far entity&gt;` (solid `btn-primary`, unchanged) and `Create new &lt;far entity&gt;` (`btn-outline-primary`, `entity-relation-create-link`) render side by side in the **one** existing right-aligned `card-body` strip, separated by `gap-2` since two adjacent `.btn`s carry no margin of their own. Not conditioned on whether any linkable far rows exist: hiding the create action until a search came back empty would hide it exactly when it is least discoverable and make the strip's contents depend on a request the user has not made. **1-n tabs unchanged** — the mirror action there would be re-parenting an existing child, which moves a row out from under whatever else references it, a different and riskier operation explicitly out of scope. **Decision (2) — one compound action, not two user-visible steps.** One modal holding the **far** entity's own `EntityForm` (its schema, never the link entity's — a link row is two FK columns, one of which is the record being viewed, and it has no `create_schema` at all), whose submit runs the far entity's generic `create` and then **the same** `config.linkCreate` route `Link existing` already calls, with the id the create returned. No new backend route: this is exactly the two requests a user could already make by hand, sequenced for them. The far entity's own scope field is prefilled and **locked** from the same `pickerScopeParams` that scopes the picker — a scope field is a query param on `list` and a body field on `create`, so one derivation serves both rather than growing a second notion of "which project is this" — and left editable, not locked to a guess, where the route can derive none (scoping case 3). **Decision (3) — the two calls are not one transaction, and that is accepted rather than overlooked (NFR-79).** Atomicity would need a *new* backend route combining two separately-gated operations, with its own permission question and its own boundary semantics — a larger decision than the residual risk warrants, so it is rejected explicitly in the ADR's Alternatives and the risk is handled from both ends in the client instead. **Before:** the button is gated on **both** the far entity's own `&lt;resource&gt;.create` **and** the declared `linkCreate.permission`, fail-closed, so the one predictable cause of a half-completed write (an actor who may create but may not link, producing a `201` then a `403`) can never start it — an actor holding only the far-create code therefore sees **neither** button, worth stating because the intuitive expectation is that it sees `Create new` alone. The API-capability half is gated too and is not hypothetical: 3 of the 12 live link directions point at an entity with no generic `create` (`TestCondition`/`Defect` are authored only through bespoke routes). **After:** if the link half fails regardless (a race, a duplicate-pair `409`, a cross-project `422`), the modal **closes** — a resubmit would otherwise mint a second row for one intent — and a persistent `alert-danger` above the table (`entity-relation-create-link-error`) names the created record by its **own label and its id**, quotes the API's **own** reason rather than a generic failure string, states plainly that it was saved and is **not** linked, and names the recovery: `Link existing ...`, the sibling button already on screen, which now needs no form because the record exists. No bespoke "retry link" state was added for exactly that reason. An **ordinary** create failure (nothing written) gets the opposite handling — form stays open, input intact — which is why the two are distinguished by error *type*, not by message. **Found and fixed: `Card.Body` silently dropped every `data-testid`.** The `entity-relation-actions` hook the UI Design Document §6.2 has documented since 11.57 had **never rendered**: the `Card` atom's section props declared only `children`/`className`, and TypeScript does not excess-property-check a JSX attribute whose name contains a hyphen, so `&lt;Card.Body data-testid="..."&gt;` compiled cleanly and the attribute was discarded — invisible to `tsc`, to the type system, and to every test that had not yet queried it. Surfaced only by a new assertion querying the documented hook, which is itself an argument for asserting on documented testids over equivalent structural queries. `components/atoms/card` now declares and forwards `data-testid` on `Card`/`Header`/`Body`/`Footer`/`Title`; every `data-testid` in this repo is load-bearing (root `CLAUDE.md`), so an atom that drops one is a hole nothing static can see. **Found, not fixed, and worked around at the call site with the reason stated:** `EntityForm` maps every blank optional field to `null` before submitting, which a *defaulted-and-non-nullable* Pydantic field rejects — leaving `Status` blank in the new modal `422`s with `"Input should be 'draft', 'reviewed', 'approved' or 'deprecated'"` because `CreateStandaloneTestCaseRequest.status` is `TestCaseStatus = "draft"`. **Pre-existing and reachable today from `EntityListPage`'s own "New Test case" modal** — nothing about a relationship tab causes it — and the real fix (omit a blank optional rather than sending `null`) is a change to the shared form affecting every entity and every create path on the generic surface, so it belongs to its own story rather than a drive-by here (root `CLAUDE.md`'s rule against mass-fixing unrelated pre-existing findings). TC-ADMIN-099 selects a Status explicitly and says why at the call site. + propagation across Requirements (FR-ADMIN-8 corrected in place with the superseded clause struck through rather than deleted, new NFR-79, a new narrative paragraph under §2.8, new traceability row) / UI Design (§6.1 rewritten with the correction called out above it, §6.2's sketch and prose both updated for two buttons plus the new alert strip, §6.3 gains the four-cell permission matrix, new §6.7a for the compound modal, §3's forward-pointer corrected from "exactly one write action") / Test Design (new §65a, and §65's own now-stale "no frontend unit-test class / no e2e class" exclusions given a forward-pointing correction rather than a rewrite, per `docs/CLAUDE.md`'s docs-pass-status-claim staleness note — clauses (3) and (4) of it remain true) / Test Cases (new TC-ADMIN-096..099, Taxonomy & Generic Admin CRUD 80/52 → **84/56**, grand total by **direct column summation** **331/194**, with the prior 327/190 re-verified by the same summation first so no inherited drift was carried forward). **No ADR-index row** (the ADR itself is unchanged in number and status), **no API doc change** (no route, no schema key), **no Database doc change** (no schema, no new permission code), **no Sitemap change** (no new screen or route). **Verification — executed against the isolated stack `testnexa-deve77ffc-test` (frontend `:34085`, backend `:38217`, Postgres `:41873`), 2026-09-15.** Frontend `tsc --noEmit` clean; Vitest whole-suite **626/626** (614 → 626, the 12 new cases in `EntityDetailPage.relationActions.test.tsx`, 14 → 26). E2E `admin8-relation-tab-write-actions.spec.ts` **4/4** including the new TC-ADMIN-099, then the whole ADMIN-6/7/8 detail-page family re-run together as a regression sweep, **16/16**. **No backend test changes and no backend test runs claimed** — the amendment calls no new route and adds no permission code, so every backend row under 11.57 still covers exactly what it covered; saying so explicitly is more honest than re-running them and implying they attest to this change. **Isolated stack deliberately left running** per root `CLAUDE.md`'s handoff convention | Done |
| 11.59 | [ADR-0077](../adr/0077-relationship-tab-unlink-action.md) (**per-row unlink on many-to-many relationship tabs**, closing the item ADR-0076 deferred by name; new FR-ADMIN-9, NFR-80). **A new ADR rather than an ADR-0076 Amendment**, deliberately: `docs/CLAUDE.md`'s in-place `### Amendment` convention covers a same-branch correction to a story's own Decision, and this is neither a correction nor same-story — ADR-0076's Consequences explicitly scoped unlinking *out* ("belongs to its own story") and its Alternatives listed it as **deferred, not rejected**. It also ships real new surface: 4 routes, 4 permission codes, a migration and a 12th schema key. ADR-0076 gets a **forward-pointing addendum**, not strikethrough — its text is still the accurate record of what it scoped out and why. **Backend:** `LinkDeleteAction` + `CrudEntityConfig.link_delete`, `link_create`'s exact mirror, on all 6 junction configs, served as `linkDelete` (12th key, `null` for the 23 non-link entities). 4 new `DELETE`s in `trace.py` at the **same URLs** as ADR-0076's `POST`s, each reusing that ADR's own `_gate_parent` — the same function, not a copy, so an entity's link and unlink cannot drift on the NFR-1 boundary — plus a shared `_delete_link`. `404`-not-idempotent-`204` when the pair is not linked (ADR-0030's asymmetry, inherited), and **no** cross-project `422` / far-side org walk, because an existing link row already passed both at insert time so re-deriving them could only reject rows the system itself created. `LINK_DELETE_RESOURCES` (catalog **106 → 110**), kept a separate tuple from `LINK_CREATE_RESOURCES` despite equal contents — equal by coincidence of scope, not by rule. Migration `8c1d5a7b93e2`, **symmetric `downgrade()`** (unlike `3e6b08c5da71`'s) because it grants nothing it does not also create, asserted so a future backfill fails a test rather than silently leaving the downgrade wrong. **Frontend:** per-row **Remove** on n-n tabs only (1-n rows are records — removing one would mean deleting the child, which its own screen offers); `EntityTable` gains one narrow `onUnlink` prop rather than reusing `onDelete`, which is gated on `config.methods.includes("delete")` — false for every link entity and required to stay so. Gated twice, hidden-not-disabled, the delete permission checked **independently** of the create one. Confirms through the same `Modal` shape `EntityListPage`'s row delete uses (not a native `confirm()`, which cannot render an `ApiError`), and the confirm stays **open** on failure — the opposite of ADR-0076 Amendment 1's compound create, because nothing was written so re-confirming is a retry. **Deliberately not extended to MCP**, flagged rather than silently skipped: the generated `_make_delete_tool` takes a single `id: UUID` and cannot address a pair-keyed link row, so an MCP unlink is a new *action kind*, not a registry row — and REQ-4's/PLAN-1's own `DELETE`s have been MCP-unreachable since they shipped for the identical reason, so all six junctions stay consistent rather than newly inconsistent. **Found, not fixed:** the live permission catalog holds 112 rows where `build_permission_catalog()` returns 110 — `ai_agent.create`/`ai_agent.update` are seeded by migration `d33d66f4b3c3` (MCP-4) but `ai_agent` appears in no catalog tuple. Nothing is currently broken (each count test compares like with like) but a fresh and a migrated database genuinely differ; surfaced while verifying this ADR's migration live, left as its own item per root `CLAUDE.md`'s no-drive-by-fixes rule. **Verification, all run against the isolated stack `testnexa-deve77ffc-test` and quoted from the actual runs** (2026-09-16): backend unit **808/808** (762 → 808), backend integration **16/16** for this story (7 route + 9 permission/migration), `tsc --noEmit` clean, Vitest **753/753** (738 → 753), e2e `admin8-relation-tab-write-actions.spec.ts` extended with a live link-then-unlink flow. Four pre-existing tests **corrected in place, none deleted** — the schema envelope's eleven-key list and three RBAC catalog assertions (catalog size, read-only-resource action sets, `org_admin` bundle size), each **narrowed to the new exact set** rather than relaxed to a subset check, so an *unplanned* code on a junction resource still fails them. **Known failures on the full integration run, both independently confirmed NOT caused by this change:** 4 taxonomy-seed count tests (`test_admin5_seed_test_level.py`, `test_admin_testtype_seed.py`) fail against leaked fixture rows this long-lived shared stack accumulated on **2026-09-15** — every extra row is tagged `ADMIN-2`/`ADMIN-7`/`PLAN-3`/`REQ-5`/`Custom Regression Suite`, zero are tagged `a77-*`, and their `created_at` all predate this session; and 1 (`test_adr75_amendment1_bidirectional_junctions::test_defect_gains_its_first_relationship_tab_from_the_defect_arm`) **passes on a solo rerun**, the host-contention class root `CLAUDE.md` documents. Neither cleaned up, per the no-drive-by-fixes rule. **Isolated stack deliberately left running** per root `CLAUDE.md`'s handoff convention | Done |
| 11.60 | [ADR-0078](../adr/0078-compound-create-through-bespoke-routes.md) (**"Create new" on every relationship tab, by borrowing the far entity's bespoke create route**; new FR-ADMIN-10, NFR-81). **A new ADR rather than an ADR-0076 Amendment 2**, same reasoning 11.59 spells out: this is not a correction to ADR-0076's Decision — that Decision is accurate, and its "3 of the 12 live link directions… the action correctly never appears for them" clause was a true description of the surface it built — it is a *later* decision that the word "correctly" was doing too much work, taken on its own terms and shipping its own new declaration point. ADR-0076 gets a **forward-pointing addendum** in FR-ADMIN-8 and NFR-79, not strikethrough. **Backend:** new `CompoundCreateAction` dataclass + `CrudEntityConfig.compound_creates`, a **tuple** (directional — a junction has two ends and they disagree) carrying `far_field` / `path_template` / `permission` / `links_automatically` plus the four optional `parent_*` picker fields, served as a **thirteenth** `compoundCreates` key on `GET /entities/{resource}/schema` beside `linkCreate`/`linkDelete` and deliberately `[]` rather than `null` when empty, since a client searches it by direction (`farField === relation.targetField`) so "declares none" and "declares some, but not yours" already collapse into one miss — 3 of the 6 link entities serve `[]` for the second reason. Three declarations, all in `trace.py`: `RequirementTestConditionLink` (→ `POST /requirements/{requirement_id}/test-conditions`, `test_condition.create`, `links_automatically=True`, no picker — REQ-3's atomic route is already parented by exactly the record the tab is on, so one request finishes the job and a following `link_create` would `409` on the pair it just wrote), `TestConditionTestCaseLink` (→ the **same** route, `links_automatically=False`, Requirement picker labelled by `description` reused verbatim from the sibling config's own `FieldMeta` — the atomic route writes the *requirement* link, not this junction's, so this direction is genuinely two calls and ADR-0076 Amendment 1's entire created-not-linked error path applies unchanged), and `TestCaseDefectLink` (→ `POST /executions/{test_execution_id}/defects`, `defect.create`, `links_automatically=True`, execution picker labelled by `executed_at` — **not** `_DEFECT_CONFIG`'s own `result`, since every option in a failed-only picker would read "fail" — and `parent_filters=(("result","fail"),)`, because EXEC-3's route `422`s a non-failed execution so offering a passed one is offering a guaranteed rejection). **`TestExecution.scope_field` widened** from `"test_cycle_id"` to the branching `("test_cycle_id","test_case_id")` with a `branching_resolver` whose **cycle arm is declared first**, keeping every item route's (`GET`/`PATCH`/`DELETE`) tenant walk byte-identical, plus a matching second `scope_selector` option — without it the admin list page could scope by the cycle arm only while the route served both. Both columns are `NOT NULL` on every row, so only *list* scoping changes; no caller in the repo passes both arms (audited across `backend/tests/`, `e2e/tests/`, `frontend/src/` before the change, not after). Two knock-on effects, both checked ahead rather than discovered: **`TestCase` gains a one-to-many "Test executions" tab** — a test case's own execution history, which nothing else in the app surfaces — so relation totals move **30 → 31**, and ADR-0074 §4's exclusion table **8 rows → 7**, `("test-executions","test_case_id")` having stopped being an exclusion the moment the scope widened. **Frontend:** a compound branch in `EntityRelationTab`'s "Create new" modal that posts to the declared bespoke route instead of the far entity's generic `create` and follows with `linkCreate` **only** when `links_automatically` is false; an optional parent-picker step above the form and **gating** it (the form does not render until a parent is picked); and three new exported helpers — `scopeArmsOf` (normalizes a `scopeField` that may now be a string *or* a tuple, fixing a `pickerScopeParams` comparison that silently answered `false` for an array, changing no behaviour today and therefore cheap to fix now rather than at the next widening), `compoundParentField` (reads the template's single placeholder, which is what *derives* whether a picker is needed instead of declaring it) and `compoundParentScopeParams` (scopes that picker from the tab's own scope column, from `project_id`, or via `ScopeSelector`). Gating turns on `createLinkNeedsLinkCall`: the action's **own declared** `permission` always, and the link permission **only when a second, separately-gated call is actually owed** — ADR-0076 Amendment 1 required it unconditionally to foreclose a `201`-then-`403` stranding a row the tab cannot display, and where no second call is made that reason does not exist, so requiring it would hide a button for a request never issued. **No new routes, no new permission codes, no migration** — every route invoked already shipped (ADR-0028/ADR-0044/ADR-0076), every code named already exists (`test_condition.create`, `defect.create`), the catalog stays **110** and MCP tools stay **157**; no bundle is extended either, deliberately, since ADR-0076 already flagged `test_manager`'s seeded bundle as overdue a dedicated audit and an eighth ad hoc extension would make that worse. **Pre-existing tests corrected in place, none deleted:** `test_adr74_entity_relations.py`'s `EXPECTED_EXCLUSIONS` row for `("test-executions","test_case_id")`, and — in the same file — `test_filter_field_only_fk_is_excluded` **inverted in place** (ADR-0074 §2's *an FK must be a scope arm, not merely a filter field* rule is untouched; `TestExecution.test_case_id` simply stopped being an example of it), with `test_filterable_alone_is_still_not_enough` added beside it asserting the original claim against `TestCase.test_level_id` — because a rule with no live example is a rule nothing defends, ADR-0075 Amendment 1's own precedent; `test_adr75_amendment1_bidirectional_junctions.py`'s **30 → 31** relation total and **8 → 7** exclusion count; `test_entity_schema_derivation.py`'s **twelve → thirteen** schema keys; and `e2e/tests/admin7-entity-relation-tabs.spec.ts`'s **positional** tab-strip assertion, which the new `TestCase` tab would otherwise have broken in a browser rather than in the same diff. **Flagged again rather than absorbed:** the blank-optional-enum `422` ADR-0076 Amendment 1 found and deliberately did not fix is reachable from these modals too — `TestCondition.priority` and `Defect.severity` are required by their bespoke request schemas but derive as `required: false` (their configs carry `create_schema=None`, so the derivation has no create schema to read required-ness from), so leaving either blank `422`s with the field's own message rather than being caught client-side; same reason for not fixing it here (the fix is a change to the shared form affecting every entity and every create path), with both fields selected explicitly in the tests and the reason stated at the call site. **Verification, all run against the isolated stack `testnexa-deve77ffc-test` and quoted from the actual runs** (2026-09-16): backend unit **853/853** (810 → 853, +43 from `test_adr78_compound_create_actions.py`), backend integration **6/6** for this story (`test_adr78_compound_create.py`, re-run independently after the sub-agent that wrote it reported green — same result); full integration **412 passed / 4 failed**, the 4 being the *pre-existing* taxonomy-seed count drift row 11.59 already documents — re-derived rather than recited, by listing `test_level`/`test_type` directly: every extra row is tagged `ADMIN-2`/`ADMIN-7`/`PLAN-3`/`REQ-5`/`Custom Regression Suite` and **zero** carry this story's own `a78-`/`admin10` prefixes, `tsc --noEmit` clean, Vitest **777/777** (753 → 777, +24 from `EntityDetailPage.compoundCreate.test.tsx`), e2e **13/13**: the new `admin10-compound-create.spec.ts` (1/1, green first run) plus `admin7-entity-relation-tabs.spec.ts` + `admin8-relation-tab-write-actions.spec.ts` (12/12) re-run as the regression sweep for the new tab and the untouched generic create-then-link path. **Five pre-existing tests corrected in place, none deleted** — the fifth found by this pass's own regression sweep rather than by the change itself: `tests/integration/test_adr53_entity_schema.py`'s own `ENVELOPE_KEYS` set is a *second* copy of the schema-envelope key list (the unit suite has the other), so the new 13th key failed all 31 of its per-entity parametrisations at once — the "every test fails the same way → suspect one shared thing" shape `backend/CLAUDE.md` teaches, each narrowed to a new exact value rather than relaxed to a subset check. **One real defect found by the test pass and fixed**: `EntityRelationTab`'s mutation body branched on `compoundCreate` while every gate around it branched on `createLinkMode`, so a junction whose far entity had *both* a generic `create` and a compound declaration would have permission-gated and locked-value-filled for the generic path while sending the compound request. Latent (no live config produces the shape, and the backend's own `test_no_declaration_is_dead_code` forbids it) but fixed at the source with a single `activeCompoundCreate` derivation every consumer reads; the characterization test written for it was **inverted, not deleted**. **Isolated stack deliberately left running** per root `CLAUDE.md`'s handoff convention | Done |
| 11.61 | [ADR-0079](../adr/0079-child-compound-create-for-one-to-many-tabs.md) (**"New" on a one-to-many tab whose child has no generic create, via the child's own bespoke route**; new FR-ADMIN-11, NFR-82). Found by the CTO clicking `?tab=test-conditions` (the **direct** one-to-many tab) on the same `Requirement` screen ADR-0078 had just closed for `?tab=requirement-test-condition-links` (the n-n one) — the identical underlying gap (`TestCondition` has no generic `create`) on a tab ADR-0078's own mechanism never looked at, because it only ever reads `compound_creates` off a *link* entity's config. A live audit of every entity's schema found **seven** one-to-many tabs with the same shape; three closed here, two named as an explicit open gap, two asserted as deliberate exceptions — see the ADR's own classification table. **A new ADR, not an ADR-0078 amendment**: ADR-0078's Decision is unchanged and accurate for the many-to-many case it built; this is a *sibling* mechanism for a different relationship kind, not a correction. **Backend:** new `CrudEntityConfig.child_compound_creates` — same `CompoundCreateAction` dataclass, **deliberately a separate field from `compound_creates`**, because the two disagree about what `far_field` means: `compound_creates`'s own completeness suite derives "the tab's own known field" as *"the other of this entity's exactly two FK columns"* — true for every link entity — and a one-to-many child is not reliably two-FK (`TestCondition` has exactly one relevant FK, `requirement_id`); reusing the field and branching its own derivation on entity shape would leave a test suite built entirely around "a junction has two FKs" quietly false for one class of declarer. Served as a **fourteenth** additive `childCompoundCreates` key, matched by `farField === relation.scopeField` (never `relation.targetField`, always `null` on a one-to-many relation — the entity's own already-known scope column, not a second FK to solve for). Three declarations: `TestCondition` (`_TEST_CONDITION_CONFIG`, `assets.py`) → `POST /requirements/{requirement_id}/test-conditions`, `test_condition.create`; `Defect` (`_DEFECT_CONFIG`, `execution.py`) → `POST /executions/{test_execution_id}/defects`, `defect.create`; `TestCycle` (`_TEST_CYCLE_CONFIG`, `planning.py`) → `POST /test-plans/{test_plan_id}/test-cycles`, `test_cycle.create` — all three `links_automatically=True` (a one-to-many row's presence in the list *is* the relationship; there is never a second call to make), all three with the route's own path placeholder equal to `far_field`, so **no parent picker is built for the one-to-many case in this pass** — pinned by `test_path_template_placeholder_equals_far_field`, which also asserts `parent_entity`/`parent_label`/`parent_label_field`/`parent_filters` are all absent on every declaration. **Frontend:** widened `canCreateChild` (a new `canCreateChildViaCompound`, mutually exclusive with it by construction — one requires the child to have generic `create`, the other requires it not to, so exactly one "New" action still renders, the CTO's own "1-n has only add" requirement unaffected by which route serves it) and a one-line branch in the **existing** "New" mutation: `activeChildCompoundCreate` present → `createViaCompoundRoute`; absent → the existing `createEntity`. **Zero new JSX** — the existing modal's `lockedValues={{ [relation.scopeField]: parentId }}` already produces the exact right request, since `config === farConfig` on a one-to-many tab (the same fact ADR-0076's own docstring already establishes for the generic case). **Two directions explicitly left open, not silently passed over:** `TestCase`/`TestCycle` → "Test executions", each needing the *other* of `TestExecution`'s two real parents picked — a materially larger mechanism than this pass's three (the picked value would fill a **body** field, not the path; every existing picker in this codebase fills a path placeholder). ~~**Two documented as deliberate exceptions:** `TestExecution` → "Test logs" (append-only by schema, no route could exist) and `Organization` → "Org memberships" (Invite+Accept, a flow a plain "New" form cannot represent) — both asserted explicitly (`test_the_two_exception_directions_have_no_generic_create_and_no_declaration`) so a future change quietly adding either doesn't merge unreviewed.~~ **Corrected same day, pre-merge, ADR-0079's own `### Amendment 1` — `TestExecution` → "Test logs" was not actually an exception; closed instead, see WBS 11.62.** Only `Organization` → "Org memberships" remains a genuine exception. **No new route, no new permission code, no migration** — every route invoked already shipped (ADR-0028/ADR-0044/ADR-0033), the catalog stays **110**. **Completeness**, mirroring ADR-0075's own lesson: `derive_entity_relations` is the oracle for which one-to-many directions exist, never a hand-list; both "every closed direction actually works" and "no spurious declaration exists" are mutation-tested in-suite in both directions, plus a `CLASSIFICATION` table cross-checked against the oracle so the open/exception/closed split cannot silently drift from the real gap set. **Verification, all run against the isolated stack `testnexa-deve77ffc-test` and against the live rebuilt backend** (2026-09-16): backend unit **889/889** (853 → 889, +36 — 35 from `test_adr79_child_compound_create.py`, 1 from `test_entity_schema_derivation.py`'s new fourteenth-key assertion), `tsc --noEmit` clean, Vitest **780/780** (777 → 780, +3 from `EntityDetailPage.childCompoundCreate.test.tsx`). Live click-through: `Requirement` → "Test conditions" (the direct 1-n tab), New → modal shows the `Requirement` field locked to the parent's own title, real `201 POST /api/v1/requirements/{id}/test-conditions`, row visible in the list immediately after. `TestPlan` → "Test cycles" and `TestExecution` → "Defects" confirmed reachable and correctly validating at the API layer directly (a request missing the real form's other required fields `422`s exactly as the generic path already does elsewhere, rather than `404`/`403`/`405`). **Two pre-existing tests corrected in place, none deleted:** the schema-envelope key-count assertions in both `test_entity_schema_derivation.py` (unit) and `test_adr53_entity_schema.py` (integration) — thirteen keys → fourteen, the identical "second copy of the same list" shape ADR-0078's own row already found once in this same pair of files. **Isolated stack deliberately left running** per root `CLAUDE.md`'s handoff convention | Done |
| 11.62 | [ADR-0080](../adr/0080-standalone-list-page-compound-create.md) (**standalone admin list-page compound create, plus an ADR-0079 correction**; new FR-ADMIN-12, NFR-83). Found in the same CTO message thread as 11.61's own trigger — the CTO named `test-conditions`'s **standalone** list page (`/admin/test-conditions`, not the relation tab 11.61 closed), then six more URLs in one follow-up: `entry-exit-criteria`, `test-cycles`, `test-executions`, `test-logs`, `defects`, `risk-items`. A live audit (API calls with a minted token, no credential write) sorted these into three states: (1) `entry-exit-criteria`/`risk-items` **already fully working** — both already have generic `create` gated behind their own `scope_selector`, confirmed live with real `201`s (cleaned up after), zero code change; (2) `test-conditions`/`test-cycles`/`defects` had **no "New" at all**, because `EntityListPage.tsx`'s `canCreate` gate never once looked at `child_compound_creates`, even though 11.61 had already declared all three — ADR-0079 wired the mechanism into exactly one host (`EntityRelationTab`), never the standalone list page, the *other* place a "New" button lives; (3) `test-executions` correctly has none (11.61's own already-named open gap, unchanged); `test-logs` incorrectly has none — see the Amendment below. **The fix needs no new picker UI**: every entity declaring `child_compound_creates` also declares a `scope_selector` for the identical field (the generic `list` route requires it too), so by the time any row renders, `useEntityScope`'s already-resolved `scope.field`/`scope.value` are exactly what the declaration's `far_field` needs — the same "no picker needed, the value is already known" reasoning 11.61 established for a tab, just reading a different already-known value. `canCreateViaCompound = !canCreate && config.childCompoundCreates?.find(a => a.farField === scope.field)`, permission-gated on the declared code; `createMutation` branches to `createViaCompoundRoute` when present, the existing `createEntity` otherwise — mirroring `EntityRelationTab`'s own branch verbatim. **Amendment to ADR-0079, same commit:** `TestExecution` → "Test logs" was misclassified an exception ("append-only, no route could exist") — a real route already exists, `POST /executions/{id}/comments` (EXEC-2), gated `test_execution.update`, structurally identical to the other three closed directions. Closing it needed one thing the original three didn't: `_TEST_LOG_CONFIG` had **no writable schema at all** (`create_schema=None`, `update_schema=NoSchema`), so every field derived `readOnly: true` and the generic create form had nothing to render — fixed by setting `create_schema=AddTestLogCommentRequest` **without** adding `"create"` to `methods` (the factory only ever registers a create route when both are true — `"create" in methods and create_schema is not None` — so `TestLog`'s actual REST surface, `POST /test-logs` still `405`ing, is byte-identical to before), which supplies three real writable fields (`text` required, `attachment_url`/`file_name` optional) for the compound-create form. An explicit `search_fields=()` override on the same config keeps `?q=` a documented no-op, since ADR-0070's own default would otherwise have silently picked up the two new plain-string fields — a side effect of adding fields for an unrelated reason, deliberately headed off rather than left to surprise a later reader. `TestExecution` → "Test logs" moves from "exception" to "closed" in both the tab AND the standalone list, in one declaration; only `Organization` → "Org memberships" remains a genuine exception. **No new route, no new permission code, no migration** — every route invoked already shipped (ADR-0028/ADR-0044/ADR-0033/EXEC-2), the catalog stays **110**. **Two pre-existing test literals corrected in place, not deleted:** `test-logs`' `filterFields` list, in both `test_crud_factory.py` (unit) and `test_filter1_filter_fields.py` (integration) — the same "second copy of the same claim" shape this repo's docs have hit before, both copies found and fixed together. **Verification, against the isolated stack `testnexa-deve77ffc-test` and a rebuilt live backend** (2026-09-16): backend unit **898/898** (889 → 898, +9 — the `test_adr79_child_compound_create.py` `CLASSIFICATION`/`EXPECTED_DECLARATIONS` updates plus the corrected `test_crud_factory.py` literal, net across parametrized-test count changes), `tsc --noEmit` clean, Vitest **783/783** (780 → 783, +3 from `EntityListPage.childCompoundCreate.test.tsx`), targeted backend integration re-run **116/116** (`test_filter1_filter_fields.py`, `test_search1_search_fields.py`, `test_admin2_execution_trace.py`, `test_exec2_append_only_test_log.py`, `test_mcp_entity_tools.py`, `test_adr53_entity_schema.py`). Live, real-HTTP proof (a minted access token, no credential write — the harness's own Secret-Store-Writes block, `backend/CLAUDE.md`'s documented workaround): a full `Requirement`→`TestCondition`→`TestCase`→`TestPlan`/`Release`/`Environment`→`TestCycle`→`TestExecution` chain seeded, `POST /executions/{id}/comments` → `201`, `GET /test-logs?test_execution_id=` → the new row immediately, `GET /entities/test-logs/schema` serving the new `childCompoundCreates` entry and its three writable fields exactly as declared; `entry-exit-criteria`/`risk-items` verified the same way. All probe rows cleaned up afterward. **No live browser click-through this pass** — stated plainly rather than silently skipped, per this ADR's own Verification section: the isolated stack's one known human login has no recoverable password, and minting a fresh one is exactly the credential-write class the harness blocks; backend-route and Vitest-component proof stand in its place. **Isolated stack deliberately left running** | Done |
| 11.63 | [ADR-0081](../adr/0081-scope-selector-cascading-via.md) (**cascading scope-selector pickers, `ScopeSelectorOption.via`**; new FR-ADMIN-13, NFR-84). Found immediately after 11.62 shipped: the CTO's own live click-through still reported "not follow crud" for `test-executions`/`defects`/`test-logs` even with `child_compound_creates` correctly wired — the actual cause was one level lower, in `ScopeSelector`'s own `FkAutocomplete`: its search for these three could **never find anything to select at all**, no matter what was typed, so the "New" button my own 11.62 fix added was correct and simply unreachable. Not a new bug — `scope-selector.tsx`'s own docstring already documented it, undated, as a known "two-hop" limitation (a `refEntity` whose own list route needs a scope param this page's route context doesn't supply): the one-hop fix from years earlier (threading `project_id` through) covered `TestPlan`/`Requirement`/`TestCase` (all directly `project_id`-scoped), leaving `TestCycle` (needs `test_plan_id`) and `TestExecution` (needs `test_case_id`/`test_cycle_id`) genuinely broken. Live-verified **before** touching code exactly which of the six flagged URLs were already fine (`test-cycles`/`entry-exit-criteria`/`risk-items`, all one-hop) versus actually broken (`test-executions`' default "By test cycle" arm — its "By test case" arm already worked, confirmed live, so the page was half-usable and a user hitting the broken default first would reasonably conclude the whole page was dead; `defects`/`test-logs`, wholly broken). **Backend:** new `ScopeSelectorOption.via: ScopeSelectorOption | None`, recursive by shape though every real declaration is one level deep, serialized by the same `_serialize_scope_selector_option` helper recursing into itself. Three declarations, all reusing an entity the caller can already search in one hop: `test-executions`' "By test cycle" option → `via=(test-plan, test_plan_id)`; `defects`/`test-logs`' own single-option selector → `via=(test-case, test_case_id)` (picked over a `test-cycle` chain specifically because `TestCase` is `project_id`-scoped directly and `TestCycle` would need its own second hop — the fewest-hops close, not a three-level chain nothing else in this codebase has). **Frontend:** `ScopeSelector` renders `active.via` as a preceding picker step; once resolved, its value merges into the outer `FkAutocomplete`'s own `extraParams` as `{[via.paramName]: pickedId}` — never reported through `onResolved`, which still only ever fires for the real scope field the page's list route needs. A "Change `<via>`" link returns to the via step, clearing both values; switching arms in a multi-option selector (`test-executions`' own toggle) clears a stale `via` pick too, so a stale intermediate value can never silently scope the new arm's search. **Found, flagged, not fixed in this same pass:** `ScopeSelector` never passes `labelField` to its own `FkAutocomplete` at all, so every picker — including the three this ADR never touches — displays a raw UUID rather than the friendly label every calling config already declares elsewhere (`field_meta`'s own `label_field` for the identical FK), a real, contained, cosmetic follow-up deliberately not folded into this pass. **No new route, no new permission code, no migration.** **Verification, against the isolated stack `testnexa-deve77ffc-test`, rebuilt live** (2026-09-16): backend unit **907/907** (898 → 907, +9 — `test_adr81_scope_selector_via.py` new, covering TC-ADMIN-139/140, plus 2 new `via`-serialization cases in `test_entity_schema_derivation.py`), `tsc --noEmit` clean, Vitest **788/788** (783 → 788, +5 — `scope-selector.test.tsx`, a new file, this component's first dedicated test coverage of any kind). Live proof against real, seeded data (a full `Requirement`→...→`TestExecution` chain, cleaned up after): the exact browser-fired request (`GET /test-executions?q=&project_id=&test_case_id=`) confirmed via direct inspection to carry the `via`-threaded param and return the real seeded row; a full, patient (no fixed-timeout) browser run of `defects`' entire cascading flow — via-pick → outer-pick → "New" renders — passed clean end to end. Repeated live-browser attempts at all six URLs together were genuinely slow/flaky under this session's own severe host contention (60+ containers running concurrently on this host, confirmed via `docker stats`, including several sibling isolated `testnexa-*-test` stacks) — reported honestly as inconclusive noise on top of already-deterministic proof, not silently smoothed over or hidden. **Isolated stack deliberately left running** | Done |
| 11.64 | [ADR-0090](../adr/0090-agent-org-self-discovery.md) (**`AIAgent` org self-discovery, `GET /agents/me/orgs` + `tn_agent_org_list`**; new FR-MCP-7, NFR-85). Found live: an MCP session with a real `AIAgent` bearer key had no way to discover which org(s) it may act within before calling any org-scoped list tool (all require `org_id`/`project_id` up front, NFR-1) — `GET /auth/me/orgs` (ADR-0036) exists for exactly this question but explicitly `403`s any `AIAgent` caller (`OrgMembership.user_id` FKs `user.actor_id`, an `AIAgent` has no row there). New route `GET /agents/me/orgs` (`app/api/routes/agents.py`) is that route's `AIAgent`-only mirror: same `MeOrgsResponse`/`OrgSummary` shape, reuses `_active_orgs_for_user` (`auth.py`) rather than a second copy, gates inverted (`403 actor_forbidden` for a `User`, active-only orgs for an `AIAgent` via `acting_on_behalf_of_user_id`). Deliberately outside `/auth/*` — ADR-0065 Decision §2's MCP-surface exclusion of human-identity/token flows is unaffected, this is a new, narrow, additive route, not a widening of that exclusion. New MCP pseudo-resource `agent_org: {"list": _agent_org_list}` (no `CrudEntityConfig`, same shape as `test_case_link_requirement`) generates `tn_agent_org_list`, no `scope`/`fields`/pagination arguments. Registry resource count 31 → 32, CRUD-action count 129 → 130, total generated tools 158 → 159 — `tests/unit/test_mcp_tool_naming.py`'s literal anchor and `_EXPECTED_CONFIGLESS` updated in the same commit. **No schema change, no migration, no new permission code.** New integration test `test_adr90_agent_org_discovery.py`: agent → `200` active-only, human → `403` `actor_forbidden` (no `orgs` key leaked), unauthenticated → `401`, zero active memberships → `200` empty (not an error) — plus the identical call proven reachable over MCP and byte-identical to the REST response, real `tools/call` JSON-RPC round trip. **Verification, against an isolated stack cloned from `main` (`testnexa-adr90-test`)**: backend unit **915/915** unchanged, `test_mcp_tool_naming.py` **28/28**, new integration file **2/2**, `test_shell6_me_orgs.py` (the human-side sibling) **3/3** unchanged, full suite **1328/1338** — the 10 failures confirmed structurally unrelated (this change's diff touches only `agents.py`/`tool_registry.py`/`test_mcp_tool_naming.py`, none of which any failing test's own code path passes through; one of the 10 is the already-documented bare-`/health`-via-nginx known-harmless case). Isolated stack torn down clean (`docker compose down -v`, verified empty) after verification, then the same code deployed live via `docker compose build backend && up -d backend` against the main `testnexa` stack | Done |
| 11.65 | [ADR-0091](../adr/0091-actor-membership-gate-fix-bespoke-routes.md) (**seven bespoke routes' `AIAgent`-blind membership gate fix**; defect fix, no new FR). Found immediately after using 11.64's own new tool: `tn_test_suite_test_case_create` `404`'d as the same agent against a real, `GET`-able suite. Root cause: `test_suite_membership.py`/`test_plan_membership.py` and five more bespoke routes across `projects.py`/`org_memberships.py`/`role_assignments.py`/`releases.py`/`test_cycle_creation.py` (plus REST-only `roles.py`/`rbac_routes.py`) all gated on the plain, `AIAgent`-blind `_org_membership_exists(db, org_id, actor.actor_id)` instead of the canonical `_actor_membership_exists(db, org_id, actor)` `backend/CLAUDE.md` already documents as the standing rule for any `User \| AIAgent`-typed route — `create_project` itself was completely unusable by any `AIAgent` until this fix. All seven call sites swapped; four modules' own now-dead local copy of the helper deleted outright, not left commented-out. `projects.py`'s separate, already-correct `_actor_has_org_standing` (RoleAssignment-based, used by `get_project`/`update_project`) was audited and deliberately left as its own distinct mechanism, not reconciled — a separate design question outside this fix's scope. `agents.py`'s three same-shaped call sites audited and confirmed unaffected (human-only-gated before the membership check ever runs). New integration test `test_adr91_actor_membership_gate_fix.py`: 3/3, each proving a previously-`404` path (`tn_project_create`, `tn_test_suite_test_case_create`, `tn_test_plan_test_suite_create`) now `201`s for a real `AIAgent` whose only standing is its `acting_on_behalf_of_user_id`'s active membership + an inherited (ADR-0083 bootstrap-fallback) `org_admin` grant. One pre-existing unit test (`test_plan3_route_boundaries.py`'s `_patch_cycle_route` helper) updated in place to monkeypatch the new attribute name, same mock signature. **No schema/migration/permission change.** **Verification, against an isolated stack cloned from `main` (`testnexa-adr91-test`)**: backend unit **915/915**, new integration file **3/3**, full suite **1331/1341** — the same 10 pre-existing failures ADR-0090's own verification pass already found and confirmed structurally unrelated, re-confirmed identical here. Deployed live to the main `testnexa` stack's backend container (`docker compose build backend && up -d --no-deps backend`) immediately after verification, so `tn_project_create`/`tn_test_suite_test_case_create`/`tn_test_plan_test_suite_create` etc. work for real over the live MCP connection, not just in the isolated stack | Done |
| 11.66 | [ADR-0092](../adr/0092-relation-tab-sort-filter-columns.md) (**relationship tab gets sort/Filter/Columns, same as a standalone `EntityListPage`**; new FR-ADMIN-14, NFR-86). CTO report against `/projects/{id}/admin/test-suites/{id}?tab=test-suite-test-cases`: the tab should have the same CRUD-entity table capability a standalone list page has. Investigation (`Explore` subagent) found `EntityRelationTab` already rendered the real `EntityTable`, not a hand-rolled table — the gap was `EntityTable`'s `bare` prop (ADR-0074's Amendment) returning early with no `Card.Header` at all, which is where the Columns/Filter/search controls happened to live, not because they're `Card`-shaped; sort was a narrower second gap, since the click-to-sort header logic already runs inside `bare` mode's own `sections` — `EntityRelationTab` simply never passed `onSortChange`/`sortField`/`sortDir`, and held no local sort state at all. **Fix, `entity-table.tsx`:** extracted the Columns/Filter/search controls into one `toolbarControls` value and both modals into one `modals` value, both computed unconditionally and rendered identically in `bare` and non-`bare` — `bare` still renders no `title`/`Card`/`Card.Header` (a `bare` caller has nowhere to put a title), everything else now renders regardless. **Fix, `EntityRelationTab.tsx`:** added local `sort`/`filters` state (`sortResource` tracks `relation.entity` the same way `EntityTable`'s own `preferencesResource` tracks `config.resource`, resetting both on change — this component is not remounted on tab switch, `EntityDetailPage` renders it with no `key`, so without the reset a sort/filter chosen on one tab would silently keep narrowing the next tab's query); threaded into `listQuery`'s `queryKey`/`queryFn` (`sort` param, `params: {...filters, [scopeField]: parentId}`, scope last so a filter can never shadow it) and into `EntityTable`'s existing props. **No backend/schema change** — `FieldConfig.sortable`/`filterable` and `EntityConfig.filterFields` already arrive on the exact schema the tab already fetches (`useEntitySchema(relation.entity)`); this was a pure frontend wiring gap. **Explicit exclusion, named not silently deferred:** sort/filter/columns operate on the *currently-rendered* config's own fields — the link entity's (e.g. `test_suite_test_case`'s `test_case_id`/`created_at`) for a many-to-many tab, not the far entity's richer field set (`TestCase`'s own `title`/`priority`/`status`) — merging those onto each row would need the relation's backend list route to join and expose far-entity fields, a materially larger, separate change. New Vitest file `EntityDetailPage.relationSortFilterColumns.test.tsx`, 3/3 (Columns button present in a bare tab; Filter button renders + applying a filter re-queries with the param; clicking a sortable header toggles asc→desc, re-queries with `?sort=`/`?sort=-field`, re-querying the button fresh after each click since the query-key change remounts it). `EntityTable`'s own `bare` prop doc comment corrected in place (previously claimed search/Columns/Filter don't render in `bare` mode). **Verification**: `tsc --noEmit` clean, full Vitest suite **798/798** (795 pre-existing + 3 new), zero regressions — every existing `bare`-mode assertion (`entity-table.test.tsx`'s own suite, `EntityDetailPage.tabs.test.tsx` and siblings) passed unmodified, since none asserted the *absence* of Columns/Filter as their claim. **No backend test layer applies** — no route/schema/permission/database change | Done |
| 11.67 | [ADR-0093](../adr/0093-retire-test-conditions-standalone-admin-page.md) (**retire the standalone `TestCondition` admin list page**; new NFR-87, no new FR — a removal, not a capability). CTO instruction: `/admin/test-conditions` (registry entry + sidebar "Test Design" link) is redundant with `TestCondition`'s two other working surfaces — `ProjectDetail`'s inline section (ADR-0028) and the Requirement detail page's "Test conditions" relation tab (ADR-0074/0079) — retire it entirely, sidemenu included. Investigation found the generic `:entity` route isn't registry-gated at the fetch level (`GET /entities/{resource}/schema` resolves straight from the URL param, and the backend must keep serving that schema for the relation tab), so a registry-only edit would leave the page reachable by typed URL — CTO explicitly asked for full frontend removal, not nav-only hiding (rejected the `ORG_EXCLUDED_ENTITY_KEYS`/`projects`-style "excluded from nav, route still works" precedent, since unlike `projects` there is no replacement page at that URL to justify leaving it reachable). Decision: 3-file frontend change — `registry.ts` (drop the entry), `app-sidebar.tsx` (drop from `PROJECT_ENTITY_GROUPS`'s Test Design group, 4 children → 3), `useAdminRouteContext.ts` (gate `:entity` against `ADMIN_ENTITY_KEYS` before the schema fetch, reusing the existing "Unknown admin entity" render path every other bad key already gets — no new UI). Backend/model/MCP/RBAC and both remaining `TestCondition` surfaces untouched — confirmed via `Explore` subagent that `EntityRelationTab` fetches schema independently of the frontend registry (ADR-0075's own comment already documented this for the junction-link entities) and has no click-through navigation to the standalone entity pages at all, so nothing routes through the surface being removed. Doc propagation: Requirements (new NFR-87, forward-pointer addenda on FR-ADMIN-12's own two rows — its `test-conditions` finding was correct when written, the page it describes is retired now, `test-cycles`/`defects` unaffected)/WBS (this row; forward-pointer on 11.34)/ADR (new ADR-0093, listed as amending ADR-0051/0080/0081/0087/0089)/Sitemap (`test-conditions` row struck through, superseded note)/UI Design (Amendment appended to the SHELL-10 UI Design Document's own ASCII mockup)/Test Design (§42's TC-SHELL-035 partition-count corrected 20→19; §69's TC-ADMIN-134 gets a forward-pointer; new §73)/Test Cases (TC-SHELL-035/TC-ADMIN-134 corrected in place; new TC-SHELL-040, TC-ADMIN-144). Database/ERD/API Document: confirmed zero relevant hits (`Explore` subagent grep) — no schema, no route, no migration, explicitly N/A. Test Plan: confirmed zero standalone-admin-page-specific scope named — N/A. **Docs-only pass, no code written this pass, per explicit instruction** — implementation (the 3-file frontend change + e2e-spec audit) is a follow-up task against this same ADR | Done |
| 11.68 | Implementation + all 3 test layers for [ADR-0093](../adr/0093-retire-test-conditions-standalone-admin-page.md) (11.67's own follow-up, same ADR). **Isolated env** `testnexa-retire-test` (worktree `retire-test-conditions`, port `36073` random 5-digit, bound `0.0.0.0` for both-IP access, DB cloned from `main` — row counts + `alembic_version` `a1c4e8f92b3d` spot-checked equal) — postgres port also exposed (`17193`, added to the override *after* first stand-up, backend restarted per this file's own §2a note, re-verified both-IP health after). **Code**: the 3 files exactly as 11.67 planned — `registry.ts` (entry dropped), `app-sidebar.tsx` (Test Design group 4→3), `useAdminRouteContext.ts` (`ADMIN_ENTITY_KEYS` gate before the schema fetch, `overrideEntityKey` callers exempt). **Unit**: `registry.test.ts` (new, registry-absence class), `useAdminRouteContext.test.tsx` (new, proves `getEntitySchema` is never called for `test-conditions`/an unknown key, mocking only the HTTP boundary same as `useEntitySchema.test.tsx`'s own established scope), `app-sidebar.test.tsx` (new TC-SHELL-040 case). Full Vitest **806/806** (798 baseline + 8 new across the 3 files), `tsc --noEmit` clean. **API/backend**: zero backend files touched (`git diff --stat -- backend/` empty) — re-ran the existing suite unmodified as the regression proof TC-ADMIN-144 itself names. Backend unit **915/915** (baseline, unchanged). Backend integration, first full run misread the environment (host-side `pytest` run had no `DATABASE_URL`, silently attempted `postgres@localhost:5432` — this file's own PLAN-1-era note on exactly this failure shape; fixed by adding the postgres port to the override and setting `DATABASE_URL` explicitly) — corrected full run: **414 passed, 12 failed, 5 skipped**. All 12 failures classified, none caused by this change (proven by the empty backend diff, not just asserted): 2 already-documented nginx-routing artifacts (`test_health_api.py`, `test_rbac_seed.py`, bare `/health` via nginx, `e2e/CLAUDE.md`), 2 confirmed host-contention flakes (`test_admin2_execution_trace.py`, `test_adr75_junction_relations.py` — both pass clean alone), 3 already-documented pre-existing `TestType` seed gap (root `CLAUDE.md`'s own "8 leaked fixture rows"/"missing Confirmation Testing row" note — `seeded_count == 4` not `5`), and 5 a **newly-found** pre-existing bug (`test_adr53_entity_schema.py` ×3, `test_adr78_compound_create.py`, `test_req5_standalone_test_case.py` — `project_id` incorrectly carries `select: true` on `TestCase`'s served schema) confirmed reproducible alone (2/2) and confirmed structurally unrelated to this story (no FK-select/schema-derivation code exists in this branch's diff at all) — **flagged here, not fixed**, per this file's own drive-by-fix rule; worth its own follow-up story. **E2E**: new `adr93-retire-test-conditions.spec.ts` (TC-SHELL-040/041) — first run hit a real bug in the spec's own wait strategy (a bare `page.goto` racing `ProtectedRoute`'s boot spinner before `AppShell`/`AppSidebar` mount, the exact race `shell9-project-nav-context.spec.ts`'s own `gotoProtected` helper already documents and guards against), fixed by adopting the same helper — **2/2 stable across two reruns** after the fix (47s clean, down from a racy 1.7-3min). The 4 originally-flagged pre-existing specs re-run unmodified: **7/10 passed** in the combined run; the 3 failures resolved to 1 already-documented pre-existing drift (`admin2-generic-crud.spec.ts`'s own `e2e/CLAUDE.md`-documented broken case, calls the pre-ADR-0028 generic create route) + 1 confirmed host-contention flake (`req3-test-condition-rigor-path.spec.ts`, passes clean alone) + 1 newly-found, consistently-reproducible (2/2 alone) pre-existing bug in `EntityRelationTab`'s tab-selection/traceability-link flow (`admin10-compound-create.spec.ts` TC-ADMIN-128) — confirmed structurally unrelated to this story (zero overlap between this branch's 3-file diff and `EntityRelationTab`/`EntityDetailPage`), **flagged here, not fixed**. **Net: zero regressions attributable to this change**, two real pre-existing bugs found and documented for a future story. Isolated stack deliberately left running for CTO manual verification, per this file's own handoff convention | Done |

## Critical path

1 → 2 → 3 → (4 and 5 in parallel) → 6 → 7 → 8 → 9 → 10. Documentation (§11) has no code dependency and is delivered ahead of implementation per user instruction ("don't write code" for this pass).
