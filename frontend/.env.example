# Local non-compose dev only. When running under the docker-compose topology
# (ADR-0010), leave this unset — nginx routes /api/* to the backend and the
# frontend falls back to same-origin requests.
VITE_API_BASE_URL=http://localhost:8000
